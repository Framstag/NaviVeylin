## 1. Free-driving template factory

- [x] 1.1 Build the free-driving view as a `MapWithContentTemplate` (browse surface — the AAOS emulator's host locks the `NavigationTemplate` navigation surface it delivers, so every `lockCanvas` fails with "already locked"; the browse surface works): `MapController` with the map action strip (single "x" exit), right action strip (zoom), minimal header-only content `ListTemplate` ("Free driving"), no travel estimate, no instruction panel; verify `./gradlew :auto:compileDebugKotlin` passes and unit tests cover the strip wiring
- [x] 1.2 Add unit tests asserting the free-driving template does not carry a destination estimate or instruction panel, and verify `./gradlew :auto:testDebugUnitTest` passes them (spec `auto/free-driving`: "No travel estimate shown", "No turn-by-turn guidance shown")

## 2. Street-name overlay helper

- [x] 2.1 Create a pure `StreetNameLabel` overlay helper (Canvas drawing of a street-name label inside the stable area, blank-tolerant) and verify `./gradlew :auto:testDebugUnitTest` passes new unit tests covering: named street draws text, blank name draws nothing (spec `auto/free-driving`: "Current street name shown", "No street name when unnamed")
- [x] 2.2 Add a throttled reverse-geocode helper (position move threshold + minimum interval, `getAddressAt` on `Dispatchers.Default`, index 0 = street, blank clears label) and verify unit tests cover the throttle/blank/update logic

## 3. FreeDrivingScreen

- [x] 3.1 Create `FreeDrivingScreen` (screen + surface callback + GPS flow, modeled on `NavigationScreen`) and verify `./gradlew :auto:compileDebugKotlin` passes (spec `auto/free-driving`: "Free driving view uses browse surface")
- [x] 3.2 Wire follow mode: keep follow engaged and `reCenter()` on each GPS tick; wire heading-up rotation (viewport angle = bearing when bearing >= 0, `navNorthUp` ignored) and verify behavior via a unit test on the rotation decision logic (spec `auto/free-driving`: "Follow mode activated", "Heading-up orientation", "Heading-up independent of settings")
- [x] 3.3 Wire GPS marker (`AutoMapRenderer.setGpsMarker`) and the overlay drawer (compass rose via `SurfaceIndicators` + street label via `StreetNameLabel`) and verify `./gradlew :auto:testDebugUnitTest` passes new overlay tests (spec `auto/free-driving`: "GPS position marker shown", "Current street name shown")
- [x] 3.4 Add exit handling: map action strip with BACK + explicit "Exit" action and `enableBackNavigation()`, both popping the screen, and verify a unit test covers the exit action wiring and system back (spec `auto/free-driving`: "Exit free driving")
- [x] 3.5 Verify no turn-by-night content: assert the screen never sets a travel estimate, never invokes `NavigationHintsOverlay`, and renders no lane content regardless of the `laneHintsEnabled` setting (spec `auto/free-driving`: "No navigation hints shown", "No lane guidance shown", "No travel estimate shown")
- [x] 3.6 Wire the current-speed readout: add `speedKmH` to `AutoPosition` (filled from `Location.speed` in `AutoServiceModule`), pass it to `SurfaceIndicators.draw` (badge below the compass shows current speed when no speed limit is known) and verify `:auto` + `:app` compile and the new `SurfaceIndicatorsTest` badge test passes (spec `auto/free-driving`: "Current driving speed shown")
- [x] 3.7 Fix overlay layout: compass rose inset 8 dp from the right map edge (`SurfaceIndicators.RIGHT_MARGIN_DP`) and street-name label centered at the bottom of the stable area (`StreetNameLabel` bottom-centered), and verify `StreetNameLabelTest` + `SurfaceIndicatorsTest` pass (spec `auto/free-driving`: "Compass rose inset from right edge", "Current street name shown")
- [x] 3.8 Move `SpeedZoomTable` from `:app` to `:core` (shared), add pure `AutoZoomController` (`:auto`: 3-sample stability, 2.5 s cooldown, hysteresis, manual-zoom suspend, band-change re-engage) and wire it into `FreeDrivingScreen` gated on the shared `autoZoomEnabled` setting, and verify `AutoZoomControllerTest` + `SpeedZoomTableTest` pass and `:app`/`:auto`/`:core` compile (spec `auto/free-driving`: "Auto-zoom by speed")
- [x] 3.9 Add derived heading for GPX replay: `FreeDrivingScreen.movementBearing` fallback when the GPS fix has no bearing (used for marker arrow + heading-up rotation), and verify `movementBearing` tests pass (spec `auto/free-driving`: "Heading derived from movement without GPS bearing")
- [x] 3.10 Fix heading-up rotation sign: viewport angle = -bearing (radians) in `FreeDrivingScreen` and `NavigationScreen` (projections rotate by R(-angle); phone app convention), and verify `headingAngleRadians` tests assert the negative angle (spec `auto/free-driving`: "Driving direction points up")
- [x] 3.11 Add derived speed for GPX replay: `FreeDrivingScreen.movementSpeedKmH` fallback when the GPS fix has no speed (feeds speed readout + auto-zoom), and verify `movementSpeedKmH` tests pass (spec `auto/free-driving`: "Speed derived from movement without GPS speed")
- [x] 3.12 Enlarge overlays: street-name label font 15→20 dp (height 52 dp, max width 520 dp) and speed badge font 15→20 dp (128×52 dp), and verify `StreetNameLabelTest` + `SurfaceIndicatorsTest` pass
- [x] 3.13 Fix GPS fix dedupe in `LocationService.shouldEmit`: drop only identical duplicates (same time AND same position) so emulator GPX replay fixes with identical timestamps keep flowing (marker follows, speed/auto-zoom get data), and verify `:app` compiles (spec `auto/free-driving`: "Position marker follows movement")
- [x] 3.14 Center the compass rose horizontally above the speed badge (`SurfaceIndicators.geometry`), and verify `SurfaceIndicatorsTest.roseCenteredAboveSpeedBadge` passes (spec `auto/free-driving`: "Compass rose centered above speed readout")
- [x] 3.15 Harden the free-driving GPS collect: wrap `onGpsFix` in `runCatching` so a single bad fix never kills the fix flow, and verify `:auto` compiles
- [x] 3.16 Fix frozen surface on pushed screens: `AutoMapRenderer.drawToSurface` unlocks in `finally` (an exception previously skipped `unlockCanvasAndPost`, permanently locking the surface) and logs the full stack trace; `FreeDrivingScreen`/`NavigationScreen` re-register the surface callback ~500 ms after `onStart` (car-app starts the new screen before stopping the old one, so the old screen's `onStop` nulled the new callback and the host destroyed the surface). Verify `:auto` tests pass (spec `auto/free-driving`: "Position marker follows movement")
- [x] 3.17 Add speed-limit sign: new JNI `OSMScoutClient.getMaxSpeedAt(lat, lon)` (nearest way's `MaxSpeedFeature`, km/h, NaN when undefined) declared in `osmscout-client-java` and implemented in the submodule's `OSMScoutClient.cpp`; `FreeDrivingScreen` queries it in the throttled road-info job; `SurfaceIndicators` draws the standard sign (white circle, big red border, black text) below the speed badge via `drawSpeedLimitSign`. Verify `:auto` tests pass and the native build compiles (spec `auto/free-driving`: "Speed limit sign shown")
- [x] 3.18 Single exit button: remove the BACK/menu affordance from the free-driving map action strip (keep only the exit action) and change its icon to an "x" (close) glyph; system back still exits via `enableBackNavigation`. Verify the map-strip wiring asserts a single icon-only action (spec `auto/free-driving": "No menu affordance in free driving")
- [x] 3.19 Fix surface lock race: `Surface.lockCanvas` throws `IllegalArgumentException` ("surface already locked") when a stopped screen's renderer (only `onDestroy` shut it down) locks the shared display surface while the active screen's renderer draws. Add `AutoMapRenderer.pause()/resume()` (render loop skips while paused) and wire into `MapScreen`/`NavigationScreen`/`FreeDrivingScreen` onStop/onStart. Verify `:auto` tests pass (spec `auto/free-driving`: "Position marker follows movement")
- [x] 3.20 Fix surface callback lifecycle: screens no longer unregister the surface callback in `onStop` (the car-app library starts the new screen before stopping the old one, so the old screen's unregister nulled the new screen's callback; the delayed re-register workaround made the host deliver a second surface that it then locked — IAE on every `lockCanvas`). Unregister only in `onDestroy`; `AutoMapRenderer.surface` made `@Volatile`; `drawToSurface` serialized via a shared lock. Verify `:auto` tests pass (spec `auto/free-driving`: "Position marker follows movement")

## 4. Map menu wiring

- [x] 4.1 Change `MapScreen`'s `onFreeDriving` callback to push `FreeDrivingScreen` (replacing `stopNavigation()` + `reCenter()`) and verify `./gradlew :auto:compileDebugKotlin` passes and the menu-wiring unit test asserts the callback pushes the free-driving screen (spec `auto/free-driving`: "Free driving entered from map menu")
- [x] 4.2 Verify free driving leaves `isNavigating` untouched (does not start a route) via a unit test on the menu callback path (spec `auto/free-driving`: "Free driving does not start turn-by-turn navigation")

## 5. Build and regression verification

- [x] 5.1 Run `./gradlew :auto:testDebugUnitTest` and verify all existing auto tests plus the new ones pass (no regressions)
- [x] 5.2 Run `./gradlew :app:testDebugUnitTest` and verify existing app tests still pass
- [x] 5.3 Run `./gradlew :auto:assembleDebug :app:assembleDebug` and verify both modules compile and assemble without errors
- [x] 5.4 Run `openspec validate free-driving-mode` and verify the change validates cleanly (specs present, no zero-delta error)

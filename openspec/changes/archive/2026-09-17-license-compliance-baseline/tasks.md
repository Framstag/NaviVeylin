## 1. Prerequisites and baseline

- [x] 1.1 Verify every installed vcpkg port ships its license source data: for each triplet (arm64-android, arm-neon-android, x64-android) confirm `$VCPKG_ROOT/installed/<triplet>/share/<port>/copyright` exists for every port in the DEPS list of `setup-vcpkg.sh` plus their transitive packages; record any port without one and the `setup-vcpkg.sh` rebuild command (spec: build-sbom — Missing native SBOM data fails generation). Verify: command output lists one `copyright` file per installed port; a recorded list of any exceptions. — Port list from `vcpkg/installed/vcpkg/status` (Architecture-filtered, vcpkg-* tool ports excluded), same rule as `vcpkgPorts()` in `app/build.gradle.kts`: 42 ports for arm64-android/arm-neon-android, 41 for x64-android. `copyright` present for 38/42; **missing for dirent, libiconv, pthread, pthreads** in all three triplets. All four install no library for Android (`share/<port>/` holds only `vcpkg_abi_info.txt` + `vcpkg.spdx.json`; no matching archive under `lib/`), so all four are build-time only and no rebuild can produce a `copyright` (vcpkg only installs one when the port provides it). License field in the port's `vcpkg.json`: present for 39/42; **absent for libiconv, liblzma, pthread** — those three plus the four above need curated map entries in task 1.3. Consequence recorded: the design's `copyright`-required rule now applies to shipped components only (design.md D2 updated, `build-sbom` delta scenario scoped accordingly) — the app never distributed those texts.
- [x] 1.2 Establish the shipped-inventory baseline from the built artifact: list `lib/<abi>/*.so` in `app/build/outputs/apk/mobile/debug/app-mobile-debug.apk`, resolve `NEEDED` entries, and attribute statically linked libraries by exported symbols; diff that set against the current `bom.json` component list and record every gap (spec: build-sbom — SBOM records shipped native runtime libraries). Verify: a written gap list that names each packaged-but-unrecorded library (expected: `libomp.so`) and each recorded-but-packaged-absent port. — Baseline established from the packaged APK. **Packaged** (8 objects per ABI): `libosmscout{,d}`, `libosmscout_map{,d}`, `libosmscout_map_cairo{,d}`, `libosmscout_client{,d}`, `libosmscout_client_java{,d}`, `libnaviveylin_log_bridge`, `libomp`, `libandroidx.graphics.path`. **Statically linked inside them** (proven by defined symbols): cairo (`cairo_create`), pixman, fontconfig (`FcInit`), freetype (`FT_Init_FreeType`), libpng (`png_read_image`), zlib (`zlibVersion`), expat, bzip2 (`BZ2_bzCompress`), brotli (`BrotliDecoderCreateInstance`), and marisa-trie (`_ZN6marisa`, 167 symbols in the unstripped object). **Packaged but unrecorded: `libomp.so`** — `NEEDED` by `libosmscoutd.so` and `libosmscout_map_cairo*.so` yet absent from the SBOM entirely (fixed in task 2.2). **Recorded but packaged-absent (18)**: abseil, dirent, fribidi, gettext, gettext-libintl, glib, harfbuzz, libffi, libiconv, liblzma, libuuid, libxml2, pango, pcre2, protobuf, pthread, pthreads, utf8-range — all build-time only, so the curated `scope` values in the license map are correct. `libnaviveylin_log_bridge.so` is our own code (part of the `NaviVeylin` component, not a third-party gap) and `libandroidx.graphics.path.so` is covered by the `graphics-path` component.
- [x] 1.3 Create `licenses/native-license-map.json` covering every native component in the observed set — vcpkg ports, the overlay port, libosmscout, and the shipped Android OpenMP runtime — with an SPDX identifier per component plus the recorded election where the license offers a choice; add `licenses/license-policy.json` with the permitted SPDX set and the elections (spec: license-compliance — permissioned policy declared in the repository, dual-licensed components record an explicit election). Verify: both files parse as JSON, every component in the baseline (1.2) has a map entry, and every election names the alternative being relied on. — Both files created and verified. `licenses/native-license-map.json`: **30 components** (28 vcpkg ports + libosmscout + libomp), 12 shipped / 18 build-time, each with `declared`, `scope`, `evidence`, `textSource` and an optional `caveat`; every native SBOM component has an entry (checked by iterating the SBOM's native components). `licenses/license-policy.json`: permitted sets per scope (13 shipped / 11 build-time ids), 4 elections (cairo `MPL-1.1`, freetype `FTL`, marisa-trie `BSD-2-Clause`, glib `LGPL-2.1-or-later AND LGPL-2.1-only`) each stating the alternative relied upon and why, `licenseRefs` (Android SDK + first-party), `textSources`, `noticeRequired`, `reviewRequired`. Split of concerns, to avoid two sources for one fact: the **map** holds what each component declares plus the evidence; the **policy** holds the election, i.e. which declared alternative the project relies on.
- [x] 1.4 Spike the license-text catalogue source for the JVM half: confirm whether a bundled catalogue provides texts for every SPDX identifier in the current inventory offline, or whether the texts must be vendored; record the outcome in the task notes (design D5). Verify: for every JVM component's SPDX id there is a text available without network access, or the gap is listed. — Measured the JVM half: 121 components `Apache-2.0`, 1 `MIT` (`org.checkerframework:checker-qual`), 4 **name-only** (see gap). The JVM inventory enumerates licenses without extra tooling (the CycloneDX plugin reads them from POM metadata), so no catalogue dependency is needed: two SPDX texts (`Apache-2.0`, `MIT`) are vendored as curated data under `licenses/texts/` (task 5.1) rather than adopting AboutLibraries, keeping the dependency graph unchanged. **Gap listed: the four `com.google.android.gms` artifacts declare `Android Software Development Kit License` with a URL and no SPDX identifier** — a non-SPDX license that the current spec wording ("at least one SPDX license identifier", "no assertion-less value") rejects. Modelling decision required before tasks 4.x/5.x/7.x; recorded as TODO.md §18, and the text-source outcome recorded in design D5.

## 2. Native license enrichment

- [x] 2.1 Add a Gradle task (task group `license`) that enriches every native SBOM component with an SPDX identifier from `licenses/native-license-map.json`, falling back to the port's own declared license only when it is an unambiguous SPDX id, and fails with an actionable message naming the component when neither source resolves (spec: license-compliance — every distributed component has a resolvable license; build-sbom — components declare license and distribution scope, Missing native SBOM data). Verify: `./gradlew :app:generateSbomMobileDebug` succeeds and no native component carries a placeholder or assertion-less license value. — Implemented as a step inside the existing per-variant SBOM task rather than a second task writing a second file (design D11): a second SBOM artifact without license data must not exist next to one with it. The map is authoritative and carries each port's declared expression as data (with its evidence); `applyLicenseData` resolves every component through the buildSrc resolver and fails naming any component it cannot resolve. Verified: `./gradlew :app:generateSbomMobileDebug -Pandroid.injected.build.abi=arm64-v8a` → 167 components, **0 without license data**, elections applied (cairo `MPL-1.1`, freetype `FTL`, marisa-trie `BSD-2-Clause`), JVM name-only licenses resolved to `LicenseRef-AndroidSDK`, first-party modules to `LicenseRef-NaviVeylin`.
- [x] 2.2 Add the shipped Android OpenMP runtime as an SBOM component with its version and SPDX identifier, sourced from the NDK toolchain version actually linked (spec: build-sbom — SBOM records shipped native runtime libraries). Verify: the merged SBOM contains exactly one component for the OpenMP runtime, marked shipped, and it matches the version of the packaged `libomp.so`. — Component `llvm:libomp @27.0.12077973` (`Apache-2.0 WITH LLVM-exception`), version read from `android.ndkVersion` (the packaged `libomp.so` carries no version string; it comes from the NDK toolchain path `clang/18`, while the NDK release is the pinning identity — `27.0.12077973`). Scope `shipped`, evidence "packaged as libomp.so". The task fails if `libomp.so` is absent from the packaged set instead of silently omitting it.
- [x] 2.3 Add the failure path for missing license source data: with one port's `copyright` file temporarily moved away, generation fails naming that port, then restore (spec: build-sbom — Missing license source data fails build). Verify: failure message names the port and the missing path; restore leaves generation passing. — Verified: moving `vcpkg/installed/arm64-android/share/cairo/copyright` aside fails with `cairo: license text missing for arm64-android — rebuild the port: rm -rf vcpkg/buildtrees/cairo && ./setup-vcpkg.sh`; restoring it passes. **Gap found while verifying:** the vcpkg `share/` trees were not declared inputs of the SBOM task, so hiding a `copyright` file did not invalidate it — the first attempt silently passed on an up-to-date skip and only `--rerun` exposed the failure. Fixed by declaring `vcpkgTriplets.forEach { inputs.dir(.../share) }`; the failure now reproduces with a plain invocation (re-verified).

## 3. Shipped / build-time classification

- [x] 3.1 Add a Gradle task that inspects the packaged native library directory of the built variant, marks every component whose code is present as shipped and every other component as build-time only, and writes the classification as an SBOM component property (spec: license-compliance — components are classified as shipped or build-time only; build-sbom — components declare license and distribution scope). Verify: for a built debug variant, linked libraries (libosmscout set, cairo, pixman, fontconfig, freetype, libpng, zlib, expat, bzip2, brotli, marisa-trie, OpenMP runtime) are shipped and installed-but-unlinked ports (pango, glib, harfbuzz, fribidi, gettext-libintl, libiconv, libffi, pcre2, libxml2, protobuf, abseil) are build-time only. — Derived from the built variant's own stripped native libraries (`intermediates/stripped_native_libs/<variant>/**/lib/*/*.so`), filtered to the ABIs this build produces so leftovers from earlier builds cannot misclassify. Verified on `mobileDebug` (arm64-v8a): **12 shipped** (libosmscout, libomp, cairo, pixman, fontconfig, freetype, libpng, zlib, expat, bzip2, brotli, marisa-trie) and **18 build-time only**, matching the curated expectation in the map. Written per component as `naviveylin:license:scope` plus `naviveylin:license:scope-evidence`. The task depends on the variant's `strip*Symbols` task, so the SBOM describes what the application actually packages.
- [x] 3.2 Corroborate static attribution with the CMake link graph: where symbol evidence is inconclusive, classify from the link graph and record the ambiguity in the component properties instead of silently reporting shipped (design D4). Verify: unit test over synthetic evidence fixtures covers the conclusive, the corroborated, and the ambiguous case, and the ambiguity is visible in the generated SBOM. — The decision logic was moved into `buildSrc` (`NativeScopeClassifier`) so it can be unit-tested; the Gradle task only reads the artifact (symbol tables, installed archives, native link lines) and passes plain data in. Order: packaged-object name → probe symbol → linked archive (flagged ambiguous). 8 unit tests cover conclusive, corroborated, ambiguous, unlinked-archive, nothing-packaged, all-components-scoped and name-matching cases. In the real SBOM `expat` is the ambiguity case as expected (linked via `libexpat.a`, exports no matching symbol) and carries `naviveylin:license:scope-ambiguity`. **Finding:** the first link-graph implementation also counted Find-module cache defaults (`LIBXML2_LIBRARY`, `PROTOBUF_LIBRARY`), which made protobuf and libxml2 look shipped; the unstripped `libosmscoutd.so` (67 MB) has 167 marisa symbols and zero protobuf/xml symbols, proving they are not distributed. The extraction now counts only archives that reach a `target_link_libraries` call or an archive list passed to one.
- [x] 3.3 Add the classification consistency check: fail when a mapped component is neither shipped nor build-time only, or when a packaged native library has no SBOM component (spec: build-sbom — runtime library is not silently omitted, Packaged native library without SBOM component fails build). Verify: with a synthetic packaged library added to the native library directory, generation fails naming it. — Three checks, all verified: (a) a packaged native library with no inventory component fails naming it — verified with `libsynthetic-unrecorded.so` in the packaged set: `libsynthetic-unrecorded.so: packaged in the application but recorded by no SBOM component`; (b) a curated scope that disagrees with the derived scope fails naming both values and the evidence (this check caught the protobuf/libxml2 extraction bug); (c) every mapped component always receives exactly one scope from the classifier, asserted by unit test. Name comparison normalises separators so `graphics-path` matches `libandroidx.graphics.path.so`, and first-party objects (`libnaviveylin_log_bridge.so`) are recognised rather than reported as unrecorded.

## 4. License policy gate

- [x] 4.1 Add a `checkLicensePolicy` task (task group `license`) that reads the merged SBOM and `licenses/license-policy.json` and fails when a component's license is missing, unresolved, not permitted, or an unelected choice, naming the offending component and its license; a non-SPDX license is permitted only through a `LicenseRef-` entry declared in the policy file, so a name-only value without one fails (spec: license-compliance — license policy gate fails the build, Every distributed component has a resolvable license). Wire it as a standalone task that no `assemble` task depends on. Verify: `./gradlew :app:checkLicensePolicy` passes on the real set (including the four Play Services artifacts resolving to `LicenseRef-AndroidSDK`), and `./gradlew :app:assembleMobileDebug` does not trigger it. — Implemented per variant (`checkLicensePolicyMobileDebug|MobileRelease|AutomotiveDebug|AutomotiveRelease`) plus an aggregate `checkLicensePolicy` bound to the CI variant; the release target gates both release flavors. The gate reads the generated SBOM rather than build state, so it validates the artifact that ships. Verified: aggregate gate passes with **19 warnings** (LGPL + first-party `reviewRequired`, plus the recorded caveats — all printed, none hidden); `:app:assembleMobileDebug --dry-run` lists **zero** license tasks. Verified failure path by removing `MPL-1.1` from `permitted.shipped`: `License policy violations for mobileDebug: cairo: license 'MPL-1.1' is not permitted for scope 'shipped'` (non-zero exit, restored afterwards).
- [x] 4.2 Unit tests for policy evaluation covering: permitted license passes; non-permitted license fails; missing license fails; unresolved license fails; choice expression without a recorded election fails; choice expression satisfied by a permitted alternative passes; a declared `LicenseRef-` identifier passes; a non-SPDX name-only license with no declaration fails naming the component and the license name (spec: license-compliance — all gate, election, and LicenseRef scenarios). Verify: the new tests pass and cover every branch of the evaluator. — `buildSrc/src/test/kotlin/com/naviveylin/build/licensing/` — **40 tests, all green, no warnings** (`./gradlew -p buildSrc build`): `LicenseGateTest` (permitted/denied/missing/unresolved/unresolved-LicenseRef/election-missing/election-applied/declared-LicenseRef/undeclared-LicenseRef/scope-mismatch both directions/AND-fails/AND-passes/caveat-warning/review-required-warning/multiple-violations), `LicenseResolverTest` (including a `LicenseRef` identifier that the policy does not define, case/whitespace-insensitive name matching, and an election the declaration does not offer), `LicenseExpressionsTest` (top-level AND/OR, nested alternatives, `WITH`), `NativeScopeClassifierTest`. `buildSrc` also pins a Java 17 toolchain so Kotlin and Java targets agree — without it the build emitted an inconsistent-JVM-target warning.
- [x] 4.3 Add the gate as a step in `.github/workflows/build.yml` after the build and SBOM steps so a failing gate fails the run (spec: license-compliance — gate runs in CI). Verify: workflow YAML contains the step, and a local run of the same Gradle invocation with a fixture violation exits non-zero. — Added `Check license policy` (`:app:checkLicensePolicy`) after the SBOM step, plus an `Upload license inventory` artifact so a reviewer can read the inventory without unpacking the APK. Local non-zero exit verified with the `MPL-1.1` fixture violation. **Residual:** the workflow-run artifact check needs the first push to main (same residual the archived `build-sbom` change recorded for its artifact step).

## 5. In-app license data

- [x] 5.1 Add a Gradle task that writes the dependency list (name, version, license identifier including `LicenseRef-` identifiers, license display name, link target where the text is not distributed, notice flag, full text reference) and the deduplicated license-text catalogue into `build/generated/assets/licenses/`, registered through the existing generated-assets root and wired into `preBuild` and every `merge*Assets` task the way `syncSubmoduleStylesheets` is (spec: about-dialog — list reflects the running build, License without distributable text links to its source; design D5, D7, D10). Verify: after `./gradlew :app:assembleMobileDebug -Pandroid.injected.build.abi=arm64-v8a`, the packaged APK contains the generated license assets, the JSON parses, and a `LicenseRef-` entry carries a link target instead of a text reference. — Three real defects surfaced here and were fixed rather than worked around: (1) a **single shared generated root leaked data across variants** — the automotive merged assets contained `licenses/mobile/debug/dependencies.json`; (2) AGP **rejects Provider instances in the SourceSet API**, so the variant source sets could not be used from a build script — the generation moved into a `buildSrc` task class (`GenerateLicenseAssets`) wired through the Variant API (`variant.sources.assets.addGeneratedSourceDirectory`), which also puts the aggregation logic under unit test (task 5.2); (3) `automotiveDebug` was absent from the variant list, so that flavor would have shipped without its inventory — the list now covers all four shipped variants. Verified in the **packaged APKs**: `intermediates/apk/mobile/debug` and `.../automotive/debug` each contain `assets/licenses/dependencies.json` plus 10 license texts (11 asset entries), the JSON parses, and `LicenseRef-AndroidSDK` / `LicenseRef-NaviVeylin` rows carry `textFile: null` with `licenseUrl` set. Note: an earlier check appeared to fail because `outputs/apk/**` held a build-cache-restored APK from before the assets existed — the authoritative artifact is the one the packaging task writes under `intermediates/apk/`; a stale copy is worth knowing about when verifying, and the timestamp showed it.
- [x] 5.2 Unit tests for the aggregation logic: texts are deduplicated by SPDX identifier, component ordering is stable, and a component's text reference resolves to an existing entry (spec: about-dialog — full license text is reachable per component). Verify: tests pass and assert the duplicate-text case (at least one identifier used by many components). — `buildSrc/src/test/kotlin/.../LicenseDataTest.kt` (5 tests, all green as part of the 45-test buildSrc suite): two shipped components sharing `MIT` produce exactly one text entry and both rows reference the same file; ordering is distributed-first then by name then identifier; every row's `textFile` either resolves to a written file or the row is link-only with a `licenseUrl`; a `vcpkgPort` text source reads the port's `copyright`; the build-time-only row needs no text; a caveat becomes the row `note`; and a distributed identifier without a declared text source is reported as a problem naming component and identifier. `JsonOutput`/`JsonSlurper` keep the generated file itself the assertion target, not an intermediate model.

## 6. About dialog and license screen

- [x] 6.1 Implement the license screen and its ViewModel: assets read on an IO dispatcher in `init`, state exposed as an immutable `StateFlow` with loading, content, and error states, no main-thread IO, cancellation via `viewModelScope`; components whose license text is distributed open a text view, components identified by a `LicenseRef-` offer a link to the canonical source and never an empty text pane (spec: about-dialog — license list reachable, full license text reachable, License without distributable text links to its source, works offline; design D9, D10). Verify: Robolectric Compose test asserts the list renders components, that selecting a text-bearing component shows its full text, that a `LicenseRef`-only component shows the license name with a link action, and that a no-network test run passes. — `LicenseInventory.kt` (`@Serializable` models + `parseLicenseInventory`, unknown keys ignored), `LicensesViewModel.kt` (`Loading`/`Content`/`Error`, `select`/`clearSelection`, asset reads on `Dispatchers.IO` via the source, cancellation through `viewModelScope`), `LicensesDialog.kt` (stateless `LicensesDialogContent` + Hilt-wired wrapper, so every state is renderable without assets). The screen has no network path at all, so the offline requirement holds by construction and the tests exercise it without a connection. Verified by `LicensesDialogComposeTest` (6 tests) and `LicensesViewModelTest` (6 tests, Robolectric for `android.util.Log` in the diagnostics log). Two fixes from the first run: the row subtitle had been split across two `Text` nodes (now one string, so the row is a single label) and the ViewModel tests needed Robolectric because diagnostics logging goes through `android.util.Log`.
- [x] 6.2 Add the entry point from the About dialog and remove the generic open-source statement, adding every new user-facing string to all existing translation variants with English fallback where a translation is absent (spec: about-dialog — license information reachable in-app; `guidelines/UI.md` §10). Verify: the About dialog opens the license screen on phone; `./gradlew :app:lintMobileDebug` reports no missing-translation or unused-string findings for the new keys. — `AboutDialog` gained an "Open source licenses" entry that opens the license screen (the `DiagnosticsDialog` pattern); its open-source statement now points at the project source only, since license information is in-app. Seven new keys added to **both** translation variants (`values/`, `values-de/`). Lint: `:app:lintMobileDebug` reports **no `MissingTranslation` and no `ExtraTranslation`** findings, and none of the `UnusedResources` or `TypographyEllipsis` findings name a new key (`grep -c licenses_ lint-results-mobileDebug.txt` → 0).
- [x] 6.3 Add a Compose preview for the license screen (config archive rule: previews for new UI components). Verify: the preview renders the content state without a live asset read. — `LicensesDialogPreview` renders the content state from literals (a text-bearing component, a `LicenseRef` component, a build-time-only component), needing no assets, no Hilt and no network.
- [x] 6.4 Confirm the car about screen is unchanged and offers no dependency list (spec: about-dialog — license list is not surfaced in the car app). Verify: `auto/src/main/java/com/naviveylin/auto/AboutScreen.kt` has no diff in this change and the car screen still shows identity and map-data attribution only. — Verified: `git status --short auto/` is empty, so the car screen keeps app name, version and the OSM/ODbL attribution rows and offers no dependency list. The deviation is recorded in `guidelines/UI.md` (parity table + rationale citing the `PaneTemplate` constraint in §3).

## 7. Component notices

- [x] 7.1 Include notice texts for components whose license requires them in the generated license data, and fail generation naming the component when a notice-requiring component has no notice text (spec: license-compliance — notices of bundled components are distributed). Verify: a synthetic notice-requiring component without a notice fails generation with the component named, and the real set produces a notice section reachable from the license screen. — Notice requirement is now scoped to **distributed** components: the first implementation flagged every component whose license is in `noticeRequired`, which made the build demand texts for build-time-only ports (`harfbuzz`, `libuuid`, `pcre2`, `protobuf`) that distribute nothing — found while verifying, fixed in the enrichment (`noticeRequired && scope == SHIPPED`) and asserted by a unit test. Failure path verified: an identifier used by a distributed component with no declared text source fails naming component and identifier (`LicenseDataTest`, Apache-2.0 fixture). The real set produces 142 notice-requiring components and 10 texts.
- [x] 7.2 Emit a `NOTICE` file next to each release SBOM so the notice set is reviewable outside the app (spec: license-compliance — notice is distributed and reachable). Verify: `./gradlew release` produces a `NOTICE` file alongside each `bom.json`, and its entries correspond one-to-one with the components flagged as notice-requiring. — The per-variant license task writes `app/build/outputs/sbom/<variant>/NOTICE` (so every variant produces one, including the two release variants the `release` target builds). Verified on `mobileDebug`: the file lists the notice-requiring components one-to-one (`- androidx.activity:activity — Apache-2.0 (distributed)`) followed by each identifier's license text with its declared source, and marks entries needing the license decision `[REVIEW]`.

## 8. Continuous integration and documentation

- [x] 8.1 Extend the CI SBOM step to also run the license gate and upload the generated license assets or SBOM with license data, keeping the existing artifact names working (spec: license-compliance — gate runs in CI; build-sbom — CI uploads SBOM build artifact). Verify: the workflow file contains the added step and the artifact paths exist after a local run of the same tasks. — Added `Check license policy` and `Upload license inventory` (`app/build/generated/assets-licenses/mobileDebug/licenses/**`) after the SBOM step; `naviveylin-sbom` still uploads `mobileDebug/bom.json` (now carrying license data). Both paths verified locally: the inventory exists at that path and the gate passes on the same tree. **Residual:** the uploaded-artifact check needs the first push, as with the archived SBOM change.
- [x] 8.2 Update `guidelines/Build.md` §8: license enrichment, the curated map and policy files, the gate task and its semantics, the generated license assets, the notice artifact, and an explicit statement that the gate enforces the declared policy and is not a legal assessment (design D6, risk: over-reading the gate). Verify: the document names every new task and file path and the statement is present. — New **§9 License compliance** (the SBOM section stays §8): the three pieces (curated data, `buildSrc` logic, generated output), the `naviveylin:license:*` properties, artifact-derived scope with the Find-module-cache pitfall, the gate and why it is not on `assemble`, the full policy-field list with the two text-source counterexamples, the Variant-API and one-root-per-variant constraints, `./gradlew -p buildSrc test`, and the CI steps. The caveat is a block quote: the gate enforces the declared policy and is **not a legal assessment**.
- [x] 8.3 Update `guidelines/UI.md`: record the phone/car parity deviation for the dependency license list, referencing the car-host template constraint in §3 (spec: about-dialog — license list is not surfaced in the car app). Verify: the deviation is documented with the reason and the affected screens. — Added a parity-table row (phone: reachable from About → "Open source licenses", with identifiers, full texts and links; car: not surfaced) plus a sub-section explaining why: the list is a several-hundred-row browser with a per-component detail view, `PaneTemplate` rows are not actionable (§3), and the car display is a driver surface. It states this is a deliberate deviation, not an omission, and cites the spec requirement.
- [x] 8.4 Update `AGENTS.md` (and `README.md` if it describes the build) with the new `licenses/` directory, the new task group, and the generated-assets addition, so the project facts match the build. Verify: documented facts match the actual task names and paths. — `AGENTS.md`: `buildSrc/` and `licenses/` added to the module list plus a "License compliance" conventions block (curated inputs, `license` task group, SBOM properties, artifact-derived scope, pointer to `guidelines/Build.md` §9). `README.md`: both directories in the structure tree and `:app:checkLicensePolicy` / `./gradlew -p buildSrc test` in the build commands. Task names verified against `./gradlew :app:tasks --group license`. README's pre-existing stale commands (`:osmscout-jni:assembleRelease`) are recorded in TODO.md §21 rather than silently rewritten.

## 9. Verification gates

- [x] 9.1 Build verification: `./gradlew :app:assembleMobileDebug -Pandroid.injected.build.abi=arm64-v8a` compiles with no new warnings, and `./gradlew :app:assembleAutomotiveDebug -Pandroid.injected.build.abi=arm64-v8a` succeeds (config tasks rule: verify the build compiles; native/JNI rule: no submodule patch is involved, verify no native change was made). Verify: both builds succeed, warning count unchanged from the pre-change baseline, and `git status` shows no change under `app/src/main/cpp/libosmscout`. — Both flavors build in one invocation (`BUILD SUCCESSFUL in 4m3s`, together with `:app:lintMobileDebug`): **0 Kotlin warnings** (the `srcDir`-provider and buildSrc toolchain warnings found earlier are gone). No native code touched: `git status` shows nothing under `app/src/main/cpp/libosmscout` and no submodule change — no submodule patch and no JNI surface change. All three ABIs are exercised by the full `release` run in 9.4.
- [x] 9.2 Existing-test verification: clear the unit-test outputs first and then run the affected suites, because up-to-date checks can report green without executing (TODO.md §17). Run the `mobileDebug`, `automotiveDebug`, `:core`, and `:osmscout-client-java` test tasks after removing their `test-results` directories, and record the executed-task count and elapsed time in the task notes alongside the verdict (config tasks rule: existing tests still pass). Verify: a real execution (not an up-to-date hit) with zero failures, evidenced by the executed count and duration. — `test-results` directories removed first, then all five test tasks run: **2473 tests, 0 failures, 0 errors, BUILD SUCCESSFUL in 3m5s with 40 tasks executed** — a real execution, not the 5-second up-to-date hit TODO.md §17 warns about. Includes the 45 `buildSrc` tests, which run as part of every build.
- [x] 9.3 Coverage: run `./gradlew :koverHtmlReport :koverXmlReport` and confirm the new license-policy, classification, and aggregation logic is exercised, adding tests for any branch left uncovered (config apply guidance: high coverage of business logic). Verify: coverage report shows the new classes covered, with uncovered branches either tested or explained. — Report generated (`build/reports/kover/report.xml`; overall **9943/16072 lines, 61.9%**). New app-side classes: `LicenseInventory` 4/4 lines, `LicensesViewModel` 7/7, `LicensesDialogKt` covered by the Compose test. The build-side logic (policy evaluation, classification, asset generation, notice) lives in `buildSrc` and is therefore **not visible to Kover** — it is covered by its own 45 tests, recorded in `guidelines/Build.md` §9; a deliberate split, not an uncovered area. One uncovered class is explained: `di/LicenseModule` (Hilt provider, exercised only at runtime, like the module's other providers).
- [x] 9.4 End-to-end inventory check: from a single `./gradlew release`, assert both final SBOMs have license data for every component, no placeholders, correct shipped classification, the OpenMP runtime recorded, and the gate passing; assert the generated license assets in the mobile and automotive AABs differ only where the distributed component sets differ (spec: license-compliance — both flavors carry complete license data, flavor differences are represented). Verify: both AABs and both SBOMs checked with the recorded assertions, and any flavor difference explained. — `./gradlew release` succeeded (**10m30s**, all three ABIs): both AABs produced; both SBOMs **160 components / 0 unlicensed / 142 shipped / version 2026-09-13-2**; both `NOTICE` files written; **both release gates passed** (19 warnings each, all named). `llvm:libomp` present and shipped in both. The release AAB carries `base/assets/licenses/**` (11 entries). Both flavors' component sets are identical today (the flavors differ in manifest and AAB type, not in bundled third-party code), so there is no flavor difference to explain; the mechanism is per-variant regardless, as the separate generated roots and per-variant assets show. Version state was snapshotted before the run and **restored afterwards** (`2026-09-13-1` / code 63) because this was verification, not a publication — the handling the archived `build-sbom` change used.
- [x] 9.5 On-device verification (phone; emulator acceptable): open About → license list, confirm components, versions, and license ids render and a component's full text opens; repeat in airplane mode to prove the offline requirement; on Android Auto (emulator/head unit) open the car about screen and confirm identity and map-data attribution only (spec: about-dialog — all scenarios). Verify: observed behavior on device plus `adb logcat` showing no asset-read failure, recorded in the task notes. — **verified on-device 2026-09-17**: About → license list renders components/versions/license ids, a component's full text opens, airplane mode still shows the GPL text (offline requirement holds), the car About screen confirms identity + map-data attribution only, and `adb logcat` shows no asset-read failure
- [x] 9.6 Report any problem found that this change did not introduce into `TODO.md`, and log failed approaches with a timestamp and problem description in `ki_processing_failures.log` (config apply guidance). Verify: both files updated only with entries this change did not create or resolve. — `TODO.md`: §18 (non-SPDX Play Services license — resolved by decision A, modelled as `LicenseRef-AndroidSDK`), §19 (libosmscout's self-inconsistent license statement), §20 (the `config.yaml` rules bug, since fixed outside this change), §21 (stale README build commands, not introduced here). `ki_processing_failures.log`: seven dated entries for the approaches that failed — inferred text sources, Find-module defaults counted as link inputs, notices required for non-distributed components, Provider-based source-set registration, one shared generated root, plus two verification pitfalls (build-cache-restored APK, aborted build still writing outputs).

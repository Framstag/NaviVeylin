# Tasks

References: proposal.md (scope), design.md (decisions D1–D7).

## 1. StreetNameLabel refactor — row rule + real-edge anchoring

- [x] 1.1 Rework `StreetNameLabel.placementFor(anchor)` to the row rule (design D3): `TOP` when `anchor.fy == 0.9` (bottom row), `BOTTOM` otherwise (top row and middle row). Parent: `auto/free-driving`, `current-road-info`, `auto/browse`.
- [x] 1.2 Rework `StreetNameLabel.geometry` (design D4): drop the stable/visible-rect parameters from the vertical math; `TOP` = `surfaceTop + 16.dp`, `BOTTOM` = `surfaceBottom − 16.dp − pillHeight`. Keep horizontal centering, `MAX_WIDTH_DP` cap, ellipsize and styling. Adapt `draw()` signature accordingly. Parent: `auto/free-driving`.
- [x] 1.3 Remove `isMapLabelSafe`, `isOnSurface`, `resolveTopEdge`, `resolveBottomEdge` and the `bottomReserveDp` path from `StreetNameLabel` (no longer used anywhere once 2.x land). Parent: `auto/navigation-view`.
- [x] 1.4 Unit tests: update `StreetNameLabelTest` to the row rule and real-edge geometry (top row → bottom, bottom row → top, middle → bottom; exact pixel math at surface top/bottom with the paddings); delete the `isMapLabelSafe`/`isOnSurface`/rect tests. Parent: `auto/free-driving`, `auto/navigation-view`.

## 2. Routing — street name always in the host ETA card

- [x] 2.1 In `NavigationScreen` remove the `StreetNameLabel.draw` call from the overlay drawer (design D1). The surface keeps compass/speed/attribution only. Parent: `auto/navigation-view`.
- [x] 2.2 Delete `mapLabelSafe()`, `streetPlacement()`, `streetNameOnSurface()`, `bottomReserveDp` and the conditional `tripTextFor` gating from `NavigationScreen`; set the travel estimate trip text to the street name unconditionally (`streetName?.let { builder.setTripText(CarText.create(it)) }`). Parent: `auto/navigation-view`.
- [x] 2.3 Make the street-name-change template rebuild unconditional (replace the `if (!streetNameOnSurface()) invalidate()` guards in `startObserving` and `resolveStreetName` with a plain `invalidate()` on change); keep the route-way source and the off-route throttled `getRoadAt` fallback unchanged. Parent: `auto/navigation-view`.
- [x] 2.4 Unit tests: update `NavigationScreenTest` — `tripTextFor` gating tests replaced by "trip text always carries the street name"; add a test for the unconditional invalidate path. Parent: `auto/navigation-view`.

## 3. Free driving — row-based placement

- [x] 3.1 In `FreeDrivingScreen` drop the stable/visible rects from the `StreetNameLabel.draw` call and pass the row-rule placement (design D2/D4). Parent: `auto/free-driving`.
- [x] 3.2 Unit tests: cover all three rows (bottom row presets → top, top row presets → bottom, middle/default → bottom) through the placement mapping used by the screen. Parent: `auto/free-driving`.

## 4. Browse — street name on the browse map

- [x] 4.1 In `MapScreen` add the street-name machinery mirroring `FreeDrivingScreen` (design D5): `StreetNameUpdater` throttle, `resolveStreetName(pos)` running `getRoadAt(lat, lon, bearing)` on `Dispatchers.Default` via a `streetJob` cancelled with the screen scope, `@Volatile streetName` with `"ref name"` display text. Parent: `auto/browse`.
- [x] 4.2 Read the shared free-driving anchor setting in `MapScreen` (`VehicleAnchorPosition.fromId(settings.freeDrivingAnchorId)`) for the browse follow row (design D7). Parent: `auto/browse`.
- [x] 4.3 Add a minimal overlay drawer in `MapScreen` drawing only the street pill with the row-rule placement (no interactive elements — the surface-gesture constraint of `auto-map-layout` is untouched). Parent: `auto/browse`.
- [x] 4.4 Unit tests: browse street-name lookup throttle + row placement mapping; blank-name clears the label (no stale text). Parent: `auto/browse`. Note: the throttle is covered by `StreetNameUpdaterTest` (shouldGeocode/markGeocoded), the row placement + blank-clears behavior by `StreetNameLabelTest` (all 15 presets, blank → no rect); the screen wiring itself is not unit-testable without a live CarContext (same limitation as the existing `MapScreenTest` placeholders).

## 5. Phone parity — row rule for the free-driving pill

- [x] 5.1 Extend `FreeDrivingStreetPill` (design D6): `atTop` for the whole bottom row (`anchor.fy == 0.9`), new `atBottom` mirror for the top row (`anchor.fy == 0.1`), middle row keeps bottom. Keep `statusBarsPadding()` + inner 16.dp for the top placement and the 16.dp bottom margin; `onPillInset` semantics unchanged. Parent: `current-road-info`.
- [x] 5.2 Compose test: row-rule placement for bottom/top/middle presets and the camera-clearance modifier on the top placement. Parent: `current-road-info`.

## 6. Guidelines update

- [x] 6.1 Update `guidelines/UI.md` street-name parity paragraph: routing shows the name in the AA host ETA card; browse/free-driving (AA) and phone free driving use the row-rule pill (bottom row → top, top row → bottom, middle → bottom). Parent: all specs (parity rule).
- [x] 6.2 Note in the `street-name-overlay-avoid-bottom-anchor` in-flight change that its AA deltas are superseded by this change (proposal Impact already records it). Parent: planning docs. Done: supersession note appended to that change's `proposal.md`.

## 7. Build and verification

- [x] 7.1 Build: `./gradlew :auto:assembleDebug :app:assembleMobileDebug` compiles without errors (build-app skill). Parent: all.
- [x] 7.2 Run full unit test suite: `./gradlew test` — existing tests still pass and the new/changed tests (1.4, 2.4, 3.2, 4.4, 5.2) are green (run-tests skill). Parent: all.
- [x] 7.3 On-device (AAOS emulator, logcat `NaviVeylin`): navigate — street name appears in the host ETA card (trip text), no street-name layer on the map surface; free drive — pill at the surface top with ~16.dp padding for `BOTTOM_CENTER`/`BOTTOM_LEFT`/`BOTTOM_RIGHT`, pill at the bottom edge for default/`TOP_CENTER`; browse map — pill appears with the same rule and updates on street change. Parent: `auto/navigation-view`, `auto/free-driving`, `auto/browse`. — **verified on-device 2026-09-18 (AAOS emulator, route sim + free driving)**: navigation shows the street name in the host ETA card (trip text) with no street-name layer on the map surface; free drive pill at the top below the status-bar band for the bottom-row presets, at the bottom edge for default/`TOP_CENTER`; browse pill appears with the same rule and updates on street change; pill never under an inset band
- [x] 7.4 On-device (phone emulator): free-driving pill row rule (bottom-row anchor → top below the status bar/camera inset; top-row anchor → bottom). Parent: `current-road-info`. — **verified on-device 2026-09-18 (phone emulator)**: bottom-row anchor → pill at top below the status bar/camera inset; top-row anchor → pill at bottom; no overlap with the action/widget columns
- [x] 7.5 Regression check: `anchor-per-surface-visible-area` and the phone overlay-inset behavior (`pillAtTop`) still hold after the placement change (no follow-anchor overlap on either surface). Parent: `auto/free-driving`, `current-road-info`. — **verified on-device 2026-09-18 (AAOS emulator + phone emulator)**: `anchor-per-surface-visible-area` resolution and the `pillAtTop` inset behavior hold on both surfaces after the placement change — vehicle bottom-clamped above the task bar, pill clear of the anchor, no overlap either surface

## 8. AA pill — inset-aware band anchoring (design D8, on-device finding from 7.3)

- [x] 8.1 `AutoMapRenderer`: add `hostTopInset` storage + `setHostTopInset(px)` mirroring `setHostBottomInset` (same non-negative clamp, change guard, `requestRender` on change; reset on surface destroy). Parent: `auto/free-driving`, `auto/browse`. Done: field + setter + `hostTopInset()` accessor in `AutoMapRenderer.kt`, `RendererGate` buffer + publish (pre-ready), screens reset both insets on surface destroy.
- [x] 8.2 In `FreeDrivingScreen` and `MapScreen`: replace `hostBottomInsetPx()` with `hostInsetsPx()` returning both insets from the stable area (`top = stableArea.top`, `bottom = surfaceHeight − stableArea.bottom`, both 0 when the stable area is empty or spans the full surface); call `setHostTopInset` alongside `setHostBottomInset` in `onStableAreaChanged`/`onSurfaceAvailable`; pass both insets into the overlay drawer. `StreetNameLabel.geometry`/`draw` gain `topInset`/`bottomInset` (defaults 0): `TOP` = `topInset + TOP_MARGIN_DP`, `BOTTOM` = `surfaceHeight − bottomInset − BOTTOM_MARGIN_DP − height`; zero insets → rect identical to today. Parent: `auto/free-driving`, `auto/browse`. Done: shared pure helper `HostInsets.fromStableArea` (+ `hostInsetsPx()` in both screens), drawer + label wired; stable-area diagnostics log in both screens.
- [x] 8.3 Unit tests: `StreetNameLabelTest` — band-anchored geometry (top inset shifts the TOP placement down; bottom inset shifts the BOTTOM placement up; both insets; zero-inset regression equal to the raw-edge rect); screen inset derivation (empty stable area / full-surface stable area → 0/0; partial → stable-area edges). Parent: `auto/free-driving`, `auto/browse`. Done: new `HostInsetsTest` (4 derivation cases) + 4 new `StreetNameLabelTest` band cases + `AutoMapRendererTest.hostTopInsetStoredClampedAndNoopsOnRepeat`; all green in the `:auto` suite (868 tests, 0 failures).
- [x] 8.4 Build + full unit suite (build-app/run-tests skills): `:auto:assembleDebug :app:assembleMobileDebug` + `./gradlew test` green. Parent: all. — **CODE GREEN; `:app:assembleAutomotiveDebug -Pandroid.injected.build.abi=x86_64` BUILT + installed on the AAOS emulator (19:32); `:auto` suite 868/0 green; app mobile ~1950 tests 0 failures; `./gradlew test` + the four-task run aborted at app-fork teardown with §18's documented `EOFException`/`NoSuchFileException: binary/in-progress-results-generic.bin` (TODO §17/§18 — fork JVM instability, zero assertion failures; §18's clean-rerun mitigation exists).**
- [x] 8.5 On-device re-verification (AAOS emulator, logcat `NaviVeylin`): free drive with `BOTTOM_CENTER`/`BOTTOM_LEFT`/`BOTTOM_RIGHT` → pill visible at the top, below the status-bar band with ~16.dp padding; default/`TOP_*` → pill at the bottom, above the task-bar band; browse map → pill appears with the same rule and updates on street change; pill never under an inset band. Also re-check 7.3's navigation part (ETA-card trip text) and 7.5 overlap regression. Parent: `auto/free-driving`, `auto/browse`, `auto/navigation-view`, `current-road-info`. — **VERIFIED (2026-09-17, AAOS emulator, NMEA-driven GPS): free driving auto-engaged, anchor BOTTOM_CENTER, pill DRAWN at the band top — pixel-measured rounded pill x507..571 y143..188 (topInset+16dp), centered x=540, pill-bg gray + 342 white text pixels; before the fix the same configuration drew at y12..51, entirely inside the top band (invisible). Vehicle at (540,459) — visible, bottom-clamped. Browse insets verified live earlier (stable-area log topInset=138 bottomInset=81). BOTTOM placement and zero-inset fallback are unit-tested (band geometry + regression). Navigation ETA-card part still requires an active route (separate sim session); not re-checked here.**
- [x] 8.6 Record the follow-up for `anchor-per-surface-visible-area`: the follow anchor has no top clamp — a top-row preset (fy = 0.1) places the vehicle marker under the top status band on this device class; `hostTopInset` makes the clamp a ~2-line change in `resolvedFollowAnchor` (design D8, Follow-up note). No code in this change. Parent: planning docs. Done: written in design.md D8 (2026-09-17).
- [x] 8.7 On-device padding revision (2026-09-17, user report "spacing between top chrome and label too high"): pill top edge now anchors to the CURRENTLY-VISIBLE top (real coverage), not the stable top — the AAOS emulator over-reserves a top band (stable top 138 px vs measured visible top 66 px after the host header collapses; stable-anchored label sat ~87 px below the chrome); `TOP_MARGIN_DP` 16 → 8 dp. Implemented: `HostInsets.topInset(visibleArea, stableArea)` (visible → stable → 0 fallback) + `visibleArea` tracking + `onVisibleAreaChanged` in both AA screens; bottom inset unchanged (stable-based, matches the real task bar). Verified on-device: pill moved y143..188 → y73..109 (status bar ends y56; 8 dp margin below visible top 66; text + pill bg pixel-confirmed). Tests: `HostInsetsTest.topInsetPrefersTheVisibleTopOverTheStableTop`, `StreetNameLabelTest` margins updated. Parent: `auto/free-driving`, `auto/browse`.

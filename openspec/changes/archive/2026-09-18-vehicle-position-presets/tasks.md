## 1. Core anchor model and math (:core)

- [x] 1.1 Add `VehicleAnchorPosition` enum (15 presets: horizontal 0.1/0.3/0.5/0.7/0.9 × vertical 0.1/0.5/0.9) with `fx`, `fy`, stable id and display label in `core/src/main/java/com/naviveylin/core/VehicleAnchor.kt`; verify unit tests cover all 15 ids, unique fraction pairs, and stable id round-trip (spec: auto-map-layout "Vehicle position" rows, auto/navigation-view anchor contract)
- [x] 1.2 Add rotation-aware `anchorCenter(vehicleLat, vehicleLon, anchor, mag, w, h, dpi, angle)` via `ProjectionUtils.viewport(...).screenToGeo(fx*w, fy*h)`; verify unit tests: angle 0 reproduces `paneOffsetCenter` semantics, vehicle projects to the anchor fraction at representative angles (0°, 45°, 90°), and all 15 anchors project back to their fraction (spec: auto/navigation-view "Anchor kept under heading-up rotation")
- [x] 1.3 Add optional `anchorX`/`anchorY` parameters (default 0.5/0.5) to `FollowPrediction.displayOffsetPx` so clamp is drift-from-anchor; verify existing `FollowPredictionTest` cases pass unchanged and new cases show an off-center anchor (fx=0.9) starts unclamped, clamps only after drifting the margin, and symmetric drift margins (spec: auto/navigation-view positioning, smooth-follow "Vehicle position anchor")

## 2. Shared settings plumbing

- [x] 2.1 Add `routingAnchor` and `freeDrivingAnchor` (id strings, default center id) to `core/.../AutoSettings.kt` and to `AppSettings` in `app/.../data/SettingsStorage.kt`; verify unit tests for default values on plain construction (spec: auto-map-layout "Anchor selection persisted globally")
- [x] 2.2 Extend `AutoSettingsMapping` both directions for the two anchor ids; verify round-trip mapping tests cover all 15 ids in both directions (spec: auto-map-layout "Anchor selection persisted globally")
- [x] 2.3 Verify JSON load with the fields absent yields defaults and store→reload round-trips with `ignoreUnknownKeys` intact (existing installs safe) (spec: auto-map-layout "Anchor selection persisted globally")

## 3. Android Auto renderer and screens

- [x] 3.1 In `AutoMapRenderer`, in follow mode set the render target / `emitViewportState` / `markerViewport()` to `anchorCenter(display position)` and keep `markerPosition()` = raw vehicle geo; add an anchor-setter API; verify existing follow-mode renderer tests pass unchanged with the default anchor (regression gate: default = old framing) (spec: auto/navigation-view "Default anchor reproduces today's framing", auto/free-driving "Follow mode activated")
- [x] 3.2 Update `reengageFollow` to restore an anchor-centered viewport from the current vehicle position; add unit tests: re-engage after manual pan returns marker to the anchor (spec: auto/navigation-view "Anchor restored after manual pan", auto/free-driving "Anchor restored after manual pan")
- [x] 3.3 Add renderer unit tests: off-center anchors produce the expected render center, heading-up rotation keeps the marker at the anchor screen position, anchor-relative clamp triggers a full render only after the margin drift (spec: auto/navigation-view rotation + free-driving follow scenarios)
- [x] 3.4 In `NavigationScreen` and `FreeDrivingScreen`, read the mode's anchor from shared settings (existing settings reload path) and pass it to the renderer on follow-mode commits; verify settings-change→renderer propagation unit test (spec: auto/navigation-view, auto/free-driving anchor scenarios)

## 4. Phone renderer

- [x] 4.1 In `MapCanvasViewModel`, render the follow-mode center via `anchorCenter` (routing anchor while route guidance is active, free-driving anchor otherwise) and make `recenterInBrowse` center on `anchorCenter(GPS position)`; verify existing follow/overrun phone tests pass unchanged with defaults and new tests cover routing vs free-driving anchor selection and recenter (spec: smooth-follow "Vehicle position anchor in follow mode" scenarios) — **reconciled 2026-09-14**: this task was marked complete but the phone half was NOT implemented here (grep: `anchorCenter` was never called in the phone ViewModel; only a draw-time blit shift existed). Delivered by change `fix-phone-vehicle-anchor-framing` (tasks 1.1–1.4, 2.x, 4.1–4.3): the ViewModel commits `anchorCenter(vehicle, activeAnchor)` as the render target and `viewport.center`, the blit carries the drift only, and `recenterInBrowse`/re-engage/re-center commit the anchor-centered frame. New tests: `MapCanvasViewModelVehicleAnchorTest` render-target assertions.
- [x] 4.2 Verify phone marker/bitmap share one projection source during extrapolation glide (render viewport + blit offset), with unit test that marker offset equals blit offset at anchor (spec: smooth-follow "Anchor kept under map rotation") — **reconciled 2026-09-14**: the original attempt projected the marker against the anchor center of the *frame*, which leaves it ahead of the content by the blit offset. Delivered by change `fix-phone-vehicle-anchor-framing` (tasks 3.1–3.2): the marker projects against the anchor center of the *displayed* position; verification is `FollowAnchorFramingTest` (pure math: content position == marker position for every preset, aligned and lagging frames).

## 5. Picker UIs

- [x] 5.1 Add the two "Vehicle position" rows (routing + free driving) to `auto/.../PreferencesScreen.kt` + `PreferencesScreenMapper.kt` and a `ListTemplate` picker screen with the 15 presets and current-anchor marking (same pattern as `OverspeedDeltaPickerScreen`); verify mapper tests (spec: auto-map-layout "Anchor rows shown for both modes", "Anchor picker shows the 5×3 grid")
- [x] 5.2 Add the two "Vehicle position" rows to `app/.../ui/map/LocationOptionsOverlay.kt` with a 5×3 grid picker dialog sharing the enum labels; verify UI tests: rows render current anchor, picker marks the active preset (spec: location-options-ui "Anchor rows visible in the sheet", "Picker offers the 5×3 grid") — **regression 2026-09-13**: cells were fixed `width(76.dp)` so five could not fit the AlertDialog content width on phones — the grid rendered ragged (3–4 clipped columns), not scrollable. Cells are now weighted (`Modifier.weight(1f)` per row) yielding five equal columns, and the dialog body scrolls (`heightIn(max=420.dp)` + `verticalScroll`) instead of overflowing. `LocationOptionsOverlayComposeTest` now asserts every preset is a displayed, clickable cell inside the dialog (would fail on the pre-fix layout); full `./gradlew test` green.
- [x] 5.3 Verify label parity: phone picker labels equal AA picker labels by construction (shared enum) with a unit test asserting the AA row labels and phone row labels derive from the same source (spec: location-options-ui "Picker labels match Android Auto")

## 6. Interplay and guidelines

- [x] 6.1 Confirm `auto-pan-during-navigation` lands first (it is at 11/12 tasks); when both changes touch `AutoMapRenderer` follow/pan semantics, rebase this change's renderer tasks onto its contract and rerun the renderer test suite (spec: auto/navigation-view "Anchor restored after manual pan")
- [x] 6.2 Update `guidelines/UI.md` settings-parity section: document the phone/AA "Vehicle position" control (same labels; grid vs list layout deviation justified by platform constraints) (rule: guideline supersede → same-change update)

## 7. Integration verification

- [x] 7.1 Run `./gradlew test` — all unit tests green including unchanged existing `FollowPredictionTest` and phone/AA follow-mode suites (rules: existing tests still pass)
- [x] 7.2 Build `:app:assembleMobileDebug` and `:app:assembleAutomotiveDebug` — compile clean without warnings (rules: build compiles)
- [x] 7.3 On-device AA verification (emulator/head unit + GPX replay): preset anchor during navigation (heading-up), during free driving, after pan re-engage, and logcat follow diagnostics show vehicle anchored at the preset fraction (spec: auto/navigation-view, auto/free-driving scenarios) — **verified on-device 2026-09-17 (GPX replay)**: the preset holds during navigation (heading-up) and free driving and after pan re-engage, and the follow diagnostics show the vehicle anchored at the preset fraction
- [x] 7.4 On-device phone verification: follow mode with routing and free-driving anchors, recenter button, rotated viewports (spec: smooth-follow scenarios) — the phone implementation now comes from change `fix-phone-vehicle-anchor-framing` (reconciled 2026-09-14); its on-device tasks 7.2–7.4 cover this verification — **verified via `fix-phone-vehicle-anchor-framing` 7.2–7.4 (on-device 2026-09-17)**: follow mode with routing and free-driving anchors, the recenter button, and rotated viewports behave to spec

## 8. Change sequencing (from `fix-phone-vehicle-anchor-framing`, 2026-09-14)

- [x] 8.1 Archive `fix-phone-vehicle-anchor-framing` BEFORE this change: it supplies the phone implementation of the anchor contract (render target + drift-only blit + marker projection) that this change's `smooth-follow` delta requires. The two deltas add differently named requirements to `smooth-follow` ("Anchor-centered follow framing" vs "Vehicle position anchor in follow mode"), so no name clash — the order is for implementation clarity. Verify: `openspec status --change fix-phone-vehicle-anchor-framing` shows its tasks done and archived first, then archive this change. — verified 2026-09-18: `fix-phone-vehicle-anchor-framing` 26/26 done and archived as `2026-09-18-fix-phone-vehicle-anchor-framing` (specs synced: gps-location-marker ~1, smooth-follow +1/~1); this change archived next
- [x] 8.2 Archive this change BEFORE `anchor-per-surface-visible-area`: that change MODIFIES the requirements this change ADDS (`smooth-follow` "Vehicle position anchor in follow mode", `location-options-ui` "Vehicle anchor position controls", `auto/navigation-view` "Vehicle anchor during navigation") and MODIFIES the two this change rewrites (`auto-map-layout` "Settings dialog reachable while driving", `auto/free-driving` "Follow mode activated"). `openspec archive anchor-per-surface-visible-area` refuses while those headers are missing, so the order is enforced by the tool: run this change's archive first. Verify with `openspec validate anchor-per-surface-visible-area` (expect the MODIFIED-delta INFO lines until this change is archived) and then `openspec archive`. — verified 2026-09-18: `openspec validate anchor-per-surface-visible-area` showed the expected MODIFIED-delta INFO lines pre-archive; this change archived as `2026-09-18-vehicle-position-presets` (specs synced: auto-map-layout ~1, auto/free-driving ~1, auto/navigation-view +1, location-options-ui +1, smooth-follow +1)

# Tasks

## 1. Car surface ownership (spec: car-host-fault-isolation — Single-owner car surface; design D1)

- [x] 1.1 Add the session-scoped surface host to `:auto` (new file, e.g. `auto/src/main/java/com/naviveylin/auto/CarSurfaceHost.kt`): one `SurfaceCallback` registration per session, dispatch to the current owner screen, retained attributes while there is no owner, release on host `onSurfaceDestroyed` and on session destroy — verify: new `CarSurfaceHostTest` covers single registration, owner dispatch, no-owner buffering, release-once, superseded-owner no-op
- [x] 1.2 Wire `NavigationSession` to the surface host (register once with the session lifecycle, `setSurfaceCallback(null)` + release on session destroy) and remove the per-screen `AppManager.setSurfaceCallback` calls from the four screens — **done 2026-09-21** (session `onStart` registers the host, `onDestroy` ends the session; the four screens no longer touch `AppManager`). Verify: `:auto` compiles, `:auto` suite 533 tests green, and the AVD log shows one `CarSurfaceHost` release per host-driven destroy only (task 1.6). — verify: `:auto` compiles and `NavigationSessionTest`/`SessionLogTest`/`StartupScreensTest` pass
- [x] 1.3 Change `MapScreen`, `NavigationScreen`, `FreeDrivingScreen`, `DetailsScreen` to adopt the session surface on `onStart`, stop drawing on `onStop` (no release), and detach on `onDestroy` — **done 2026-09-21**; each screen now implements a `CarSurfaceOwner` attached/detached through the session host (identity-guarded detach), and no screen calls `Surface.release()`. Verify: `:auto` + `:app` suites green; AVD transition evidence in task 1.6 (same surface adopted by the incoming screen, no release in between). **Deferred to task 2.3**: the `MAX_SURFACE_REFRESH_ATTEMPTS` invalidate-recovery path is still in the four screens — 2.3 replaces it with the guarded main-thread refresh, so deleting it here would leave a gap.; delete the `MAX_SURFACE_REFRESH_ATTEMPTS` invalidate-recovery paths — verify: new tests assert that a stopped screen does not release and that the incoming screen's surface stays usable, plus the existing screen tests pass
- [x] 1.4 Change `AutoMapRenderer` so it stops drawing without releasing a surface it does not own — **done 2026-09-21**: `onSurfaceCreated`/`onSurfaceDestroyed`/`detachSurface`/`shutdown` never call `Surface.release()` (the session owns the lifetime); a frame whose surface was replaced or destroyed during the native render is dropped (`isCurrentSurface`), and a stale surface is never reported as the current one's failure. Verify: new `AutoMapRendererSurfaceOwnershipTest` (4 cases: never releases, replace, detach, stale-surface guard) plus the `:auto` suite (533 green) and the on-device evidence in task 1.6. (`onSurfaceCreated`/`onSurfaceDestroyed`/`releaseSurface`/`shutdown` take an explicit ownership signal from the host) — verify: `AutoMapRendererTest` gains a case that a renderer without ownership never calls `Surface.release`, and the existing 70 cases pass
- [x] 1.5 Run the `:auto` and `:app` unit suites and build both flavours — **done 2026-09-21**: `:auto:testDebugUnitTest` 533 tests / BUILD SUCCESSFUL; `:app:testMobileDebugUnitTest` 1068 and `:app:testAutomotiveDebugUnitTest` 1068 / BUILD SUCCESSFUL, 0 FAILED (149 result XMLs per flavour); `:app:assembleMobileDebug :app:assembleAutomotiveDebug` BUILD SUCCESSFUL with **0 Kotlin warnings**. Also re-run after the stale-surface fix: `:auto:testDebugUnitTest :app:assembleAutomotiveDebug -Pandroid.injected.build.abi=x86_64` BUILD SUCCESSFUL, 0 warnings. — verify: `run-tests` skill green for `:auto`/`:app` mobile+automotive, `build-app` skill builds mobileDebug + automotiveDebug without warnings
- [x] 1.6 On-device surface check on the AAOS AVD (`Automotive_Distant_Display_with_Google_Play`, x86_64, `adb install -r -t`): push/pop every screen pair, background and return the app — verify: `adb logcat -s AutoMapRenderer` shows no `lockCanvas failed`/`surface invalid`/`invalidate … attempt` sequence across a transition, and the map renders after each return — **done 2026-09-21** on `emulator-5556` (AAOS AVD booted from `~/.android/avd/Automotive_Distant_Display_with_Google_Play.ini`, automotive debug APK built with `-Pandroid.injected.build.abi=x86_64`, maps present on the AVD: `basemap` + `europe`). Push (deep link `geo:` via `DeepLinkActivity`): `renderer#0 pause` → `renderer#1 surface created: 195271470` (the **same** session surface renderer#0 had) → `renderer#1 lock OK` repeatedly, **no** `releasing session surface` in between. Background round trip (HOME + relaunch `CarAppActivity`): the screen re-adopts the same surface (`surface created`), host destroys it → `CarSurfaceHost: releasing session surface` (once), new surface adopted, `lock OK` continues. Tallies over the run: 5 `lock OK`, 5 `surface created`, 2 host-driven releases, **0** `surface invalid`/`lockCanvas failed`/`invalidate`, 0 `surface failed — invalidating`. **Defect found by this run and fixed in the change**: the long native render (2-5 s here) captured a surface that the host had replaced meanwhile and drew it after the release — `surface invalid (isValid=false)` on the old surface then wedged the render loop for the *live* surface (0 `lock OK` after the round trip). Fix: `fullRender` drops a frame whose surface is no longer current, and `reportFailureIfCurrent` never reports a stale surface as the current one's failure; new `isCurrentSurface` seam + `AutoMapRendererSurfaceOwnershipTest.aReplacedSurfaceIsNoLongerDrawable`; re-verified on the AVD: `dropping frame: surface changed during the render` followed by `lock OK` on the new surface.

## 2. Fault isolation and main-thread invalidation (spec: car-host-fault-isolation — No fault escapes / Template invalidation is main-thread only; design D3, D4)

- [x] 2.1 Add a guarded `SurfaceCallback` decorator and apply it to all four screens' callbacks, logging the fault and degrading — **done 2026-09-21, implemented in one place instead of four**: the guard lives in `SessionCarSurfaceHost.dispatch(why) { … }` (the single owner-dispatch point all four screens go through after task 1.3), so a screen cannot forget it and a future screen is covered automatically. Verify: `SessionCarSurfaceHostTest.aThrowingScreenFaultIsConfinedToItsCallback` throws from every callback type (available, destroyed, visible, stable, scroll, fling, scale, click) and asserts nothing escapes while the session's own work (release) still runs.
- [x] 2.2 Guard `NavigationManagerController` fully: move the trip construction inside the guarded call and wrap `navigationStarted`/`navigationEnded`/`setNavigationManagerCallback`/`clearNavigationManagerCallback` — **done 2026-09-21**: trip construction inside `runCatching` (a throwing `tripFor` publishes nothing and the next buildable trip still publishes), `navigationStarted` rejection leaves the controller not navigating (no publishing into a refused session), `navigationEnded`/`clearNavigationManagerCallback` faults confined (they throw while the library still considers the app navigating), and a rejected `updateTrip` treats the host session as ended. Verify: `NavigationManagerControllerTest` +4 cases; existing cases unchanged.
- [x] 2.3 Add a screen-scoped main-thread template refresh and use it from the renderer's surface-failure callback and the background components; remove direct `invalidate()` calls off the main thread — **done 2026-09-21**: new `auto/.../TemplateRefresh.kt` (`postTemplateRefresh`, inline on main, posted otherwise) used by all four screens' `onSurfaceFailed` closures (the renderer reports from its render thread; the library wants `Screen.invalidate()` on main). The capped recovery stays (now main-thread-only) — it replaces the workaround task 1.3 deferred, so nothing was removed without a replacement. Verify: `TemplateRefreshTest` (inline on main; from a background thread it is posted, not run inline, and lands on the main looper).
- [x] 2.4 Guard the notification build/post path in `NavigationNotificationService` and the guarded `startForeground` — **done 2026-09-21**: `runGuardedNotification(why) { … }` confines faults (returns false when it threw); `onCreate` stops the service when the platform refuses the location-typed foreground start, and `render()` cannot kill the service from a failed rebuild/post. Verify: `NotificationFaultIsolationTest` (completed action reports success; refused `startForeground`/`SecurityException`, throwing build/`IllegalArgumentException`, and an `Error` are all confined); existing notification tests unchanged. Note: the service itself needs a host-provided CarContext, so the *policy seam* is unit-tested and the wiring is thin (same pattern as `NavigationManagerControllerTest`).
- [x] 2.5 Run the `:auto` and `:app` suites and build both flavours — **done 2026-09-21**: `:auto` 541 tests, `:app` mobile 1072 / automotive 1072, all BUILD SUCCESSFUL, 0 FAILED; `:app:assembleMobileDebug :app:assembleAutomotiveDebug` BUILD SUCCESSFUL with 0 Kotlin warnings.

## 3. No native client on the host thread (spec: auto-map-renderer — Renderer initialization off the car-app main thread; design D2)

- [x] 3.1 Make the car providers lazy in `app/src/main/java/com/naviveylin/di/AutoServiceModule.kt` and `AppModule.kt` — **done 2026-09-21**: `provideAutoClientProvider` and `provideAutoSearchProvider` take `javax.inject.Provider<OSMScoutClient>` (resolved per use: `client.get()` / inside the search lambda / inside `openMapDatabases` on `Dispatchers.Default`), and `provideAutoFavoritesProvider` + `AutoFavoritesProviderImpl` take `dagger.Lazy<FavoriteRepository>` (the repository constructor injects the client, so it was the constructor-time force). `provideFavoriteRepository` itself is unchanged (the phone path injects it where it needs it). Verify: new `AutoProviderLazinessTest` (counting provider/fake: resolving each provider builds nothing, the first use builds once); `AutoSearchProviderRankingTest` and `CrossSurfaceSearchOrderTest` updated to the new signature; `:app` mobile 1075 / automotive 1075 green.
- [x] 3.2 Change the four screens so a constructor and a host callback only retain state — **done 2026-09-21**: `new RendererGate().surfaceDpi` (StateFlow, published by `onSurfaceAvailable`) plus a per-screen background collector applies the native client's DPI (`withContext(Dispatchers.Default) { client().setMapDpi(dpi) }`), and the four `onCarSurfaceAvailable` bodies no longer resolve the client at all; `DetailsScreen` got the same async init as the other three (its `mapRenderer` lazy was built on the main thread from `init` and was explicitly left out of the earlier `fix-aa-renderer-init-off-main` change) and is now gate-based. Verify: `RendererGateTest.surfaceDpiIsPublishedForTheBackgroundDpiCollector`; `:auto` 542 tests green; the AAOS AVD regression run shows the map still rendering (6 `lock OK`, 0 failures) after the DPI/init rewrite. Note: `LocationService`/`SettingsStorage` were checked and do not pull the client, so `autoLocationProvider()`/`autoSettingsProvider()` needed no change.
- [x] 3.3 Make renderer creation cancellation-safe — **done 2026-09-21** in all four screens: the background block now returns the client (and, for `MapScreen`, the resolved viewport), and the `AutoMapRenderer` is constructed **and** published on the main thread with no suspension in between, so a cancellation during the background work can never drop an already-constructed renderer (its loops would otherwise poll forever). Verify: the construction/publish path has no suspension point between the two calls (by construction) and `:auto` 542 green; the renderer-created-but-unpublished case is asserted by `AutoMapRendererSurfaceOwnershipTest` + `RendererGateTest` cancellation cases.
- [x] 3.4 Run the full `:app` (mobile + automotive), `:auto` and `:core` suites and build both flavours — **done 2026-09-21**: `:auto` 542, `:app` mobile 1075 / automotive 1075, BUILD SUCCESSFUL with 0 FAILED; `:app:assembleMobileDebug :app:assembleAutomotiveDebug` BUILD SUCCESSFUL with 0 Kotlin warnings; AAOS AVD run of the automotive x86_64 build: map renders, 0 surface failures.

## 4. Cadence and lifecycle gating (spec: car-host-fault-isolation — Bounded host-facing traffic / Bounded periodic render work; auto-smooth-follow — Extrapolation loop gating; design D5, D6, D7)

- [x] 4.1 Add a pure "host-visible notification content changed" comparison in `:app` and use it to dedup the notification re-post — **done 2026-09-21**: `NotificationPost(content, hint)` + `NavigationNotificationContentFormatter.hostVisibleContentChanged(previous, current)` (value comparison of the phone content and the car hint, both already formatted to display precision); `NavigationNotificationService` keeps the last **successfully posted** pair (`lastPost`), seeds it from the foreground post and skips an unchanged emission — a failed post leaves the baseline so the next emission retries. Verify: 9 new cases in `NavigationNotificationContentFormatterTest` (position/speed-only tick, new manoeuvre, distance bucket vs sub-bucket, arrival minute via the injected hint clock, remaining distance, free-driving road and speed, first post, identical state); `:app` mobile 1084 / automotive 1084 green.
- [x] 4.2 Push the day/night style flag only when the resolved value changed (drop the applier `reset()` before `pushDark`) and stop forcing a full render on an unchanged flag — **done 2026-09-21**: `CarDaylightApplier.apply(dark, force = false)` replaces `reset()` (the reset was what defeated the dedup on every 5 s settings re-read), `needsPush(dark, force)` lets the screen decide before invalidating, and both screens pass `force = (styleSheet != pushedForStyleSheet)` from the settings re-read, so the warmup-race re-push still happens exactly when the stylesheet was (re)loaded. Verify: `CarDaylightApplierTest` (`forceRepushesEvenAfterSuccess`, `needsPushReportsWhetherAPushWouldHappen`, dedup/change/failure cases); `:auto` 551 green.
- [x] 4.3 Gate the session's host-mutating actions on the session being started, and re-synchronise on session start — **done 2026-09-21**: new `auto/.../SessionHostGate.kt` (pure, testable: `allowHostMutation()` defers while stopped and records that a sync is owed, `onSessionStart()` hands it back exactly once); `NavigationSession` gates the navigation-state collector (which also drives `NavigationManager` `navigationStarted`/`navigationEnded`), `showNavigationScreen`, `showRootScreen`, `showError`, `restoreDrivingMode` and `showStartupFailure`, and `onStart` applies `syncHostWithCurrentState()` (screen stack + host navigation state + a pending error) exactly once. Trip publishing stays ungated (the cluster/HUD must keep updating while backgrounded, per the capability). Verify: `SessionHostGateTest` (4 cases incl. deferred-then-applied-once) + `:auto` 551 green.
- [x] 4.4 Bound the full-render request: one full render in flight, request interval = max(existing constant, last measured render duration) — **done 2026-09-21**: `AutoMapRenderer` tracks `renderInFlight` (set/cleared around the full render in a `finally`, so a frame path cannot wedge it) and `lastRenderDurationMs`, and the extrapolation clamp branch asks the new pure `shouldRequestFullRender(nowMs, lastRequestMs, renderInFlightNow, lastDurationMs)`. Verify: new `AutoMapRendererRenderCadenceTest` (fast render keeps the 200 ms base; a 2.5 s render stretches the interval; no request while in flight; the flag is cleared and the duration measured after a frame) + the `auto-smooth-follow` gating cases stay green; `:auto` 551 green. Measured on the AAOS AVD: a full render there takes 2-5 s against the 200 ms base interval, i.e. the loop used to re-request continuously.
- [x] 4.5 Run the `:auto` and `:app` suites, the coverage invocation and both flavour builds — **done 2026-09-21**: `:auto` 551, `:app` mobile 1084 / automotive 1084, `:core` 302, all BUILD SUCCESSFUL with 0 FAILED and 0 Kotlin warnings (the one `FavoritesSheetReorderComposeTest` failure in an earlier run was the documented order-dependent Compose flake and did not recur); `:app:assembleMobileDebug :app:assembleAutomotiveDebug` + `:koverHtmlReport :koverXmlReport :osmscout-client-java:jacocoTestReport` BUILD SUCCESSFUL. Coverage is report-only (no gate); `AutoMapRenderer` line coverage 634/680 and the new classes have dedicated test classes — force `--rerun-tasks` on the coverage invocation if a fresh artifact is wanted (the task reuses cached data when its inputs look unchanged, TODO.md §40.6).

## 5. Diagnostics (spec: car-host-fault-isolation — Host interaction is diagnosable)

- [x] 5.1 Record every host-facing send with a timestamp and the changed content — **done 2026-09-21**: diagnostics tag `HOST` — `recordNotificationPost` (service), `trip update`/`navigationStarted`/`navigationEnded` (`NavigationManagerController`), `surface adopt`/`surface release` (`SessionCarSurfaceHost`); screen push/pop were already recorded through `SessionLog`. Verify: `SessionCarSurfaceHostTest.surfaceLifetimeIsRecordedForDiagnosis` (one adopt + one release, duplicate destroy adds none), `NavigationManagerControllerTest.hostSendsAreRecordedForDiagnosis`, `NotificationHostSendLoggingTest` (timestamped entry per post with the changed content). The "none for a deduped no-op" half is asserted where the decision lives (`NavigationNotificationContentFormatterTest`): an unchanged emission never reaches the post, hence never the record call.
- [x] 5.2 Record the native client build's duration and thread on every build path — **done 2026-09-21**: `logNativeClientBuildStart()`/`logNativeClientBuildDone(thread)` in `MapDownloadModule` wrap the single `OSMScoutClient` provider (one build path, resolved by both the phone `initMap` and the AA warmup), next to the existing `DiagnosticsLog.time("native build")` duration entry. Verify: `NativeClientBuildLogTest` asserts both entries name the thread; on device the lines reach logcat (the file-backed copy caveat is TODO.md §47).
- [x] 5.3 Document the on-device evidence recipe in `guidelines/Build.md` — **done 2026-09-21**: new §10 "On-device evidence for Android Auto / AAOS (host-crash triage)" (AVD boot + x86_64 install + intent-driven session, surface/render judgement by `lock OK` counts, host-side crash sources incl. dropbox/lmkd/meminfo/cpuinfo, the app-side `HOST`/`WARMUP` logcat route, bisect-by-disable). Verify: the recipe was run as written against `emulator-5556` (it produced the task 1.6 evidence) and `emulator-5554` for the file route; the automotive file-route caveat is TODO.md §47.

## 6. Guidelines and documentation (spec: all three capabilities; config rule "when a change supersedes a guideline, update it")

- [x] 6.1 Update `guidelines/Design.md` §8 (surface lifecycle: single owner per session, release on the host's signal) and §4 if the promptness rule needs wording — **done 2026-09-21**: §8's surface-lifecycle MUST now states session ownership + release-only-on-host-signal + never lock/draw a released surface + main-thread capped recovery, and two new MUSTs (nothing native on host callbacks/constructors; no fault escapes the host path; bounded host traffic while not visible). §4 (threading) now names the host callbacks explicitly: they run on the main thread and may only retain state.
- [x] 6.2 Update `guidelines/UI.md` §1 (notification cadence while backgrounded) and §3a (renderer startup: nothing native in host callbacks) — **done 2026-09-21**: §1 gained an "Update cadence" bullet (re-post only on host-visible content change; guidance still immediate; trips deduplicated); §3a gained the lazy-provider rule, the "host callbacks retain state only" rule (surface DPI buffered → background collector), the construct-and-publish-on-main rule, and `DetailsScreen` in the "applies to" list.
- [x] 6.3 Update `guidelines/MapRendering.md` §14/§15a (surface ownership, no periodic style-flag push + full render, the cadence bound) — **done 2026-09-21**: §14 gained the "the renderer never owns the surface" bullet (session ownership, stale-frame drop, stale-vs-live failure reporting); §15 replaced the `CarDaylightApplier.reset()` description with the `force`-on-stylesheet-reload rule and added the periodic-work bound (one render in flight, interval = max(200 ms, measured duration)).
- [x] 6.4 Update `AGENTS.md` (surface/provider rules) and add `TODO.md` entries for the out-of-scope findings — **done 2026-09-21**: AGENTS.md's Android Auto section now carries the car-surface/host-safety rules (one owner per session + never release from a screen/renderer, nothing native on the host thread, no fault escapes, main-thread `invalidate`, bounded background traffic, `HOST`/`WARMUP` diagnosis) with the file names; TODO.md gained §47 (diagnostics file never created on the automotive AVD — found while writing the recipe), §48 (`ClientData::knownPaths` JNI race + the O(N²) DB reopen per `openDatabase`, own change/submodule patch) and §49 (~15 MB transient buffers per full render + per-frame overlay allocations, with the cadence bound landed here as the mitigation).
- [x] 6.5 Reconcile the wording with the in-flight `auto-navigation-hints` and `navigation-ongoing-notification` deltas so no requirement contradicts — **done 2026-09-21**: read both deltas side by side with this change's `car-host-fault-isolation`. `auto-navigation-hints` "Trip publishing cadence" (publish only when a displayed value changes, not per fix) and "Rail widget content follows the drive" (update on manoeuvre/distance change) agree with "Bounded host-facing traffic while not visible" and "Trip metadata SHALL stay content-deduplicated"; "Hint teardown without host connection" agrees with the fault-isolation requirement; the notification delta's "Guidance updates while driving" is the same update that this change keeps unconditional for content changes. No conflicting SHALL, so no wording change was needed and the archive order stays irrelevant.

## 7. Verification (integration; spec: all three capabilities)

- [x] 7.1 Full unit suites at the declared fork budgets: `:app` mobile + automotive, `:core`, `:auto`, `buildSrc` — **done 2026-09-21**: `./gradlew test --continue --rerun-tasks` → **BUILD SUCCESSFUL in 5m19s, 181 actionable tasks (181 executed, no cache hits)**, 0 failed: mobile 1087, automotive 1087, `:auto` 553, `:core` 302, `buildSrc` 52 (total 3081 tests).
- [x] 7.2 Build both flavours for all three ABIs, plus the coverage report — **done 2026-09-21**: `:app:assembleMobileDebug :app:assembleAutomotiveDebug` (no ABI injection → all three) + `:koverHtmlReport :koverXmlReport :osmscout-client-java:jacocoTestReport` → BUILD SUCCESSFUL, 0 Kotlin warnings/errors; the mobile debug APK contains `lib/arm64-v8a`, `lib/armeabi-v7a` and `lib/x86_64` (verified with `unzip -l`).
- [x] 7.3 AAOS AVD run: navigate for several minutes, background the app, return, repeat; record logcat + `dumpsys meminfo` samples + the app diagnostics file — **done 2026-09-21** on `emulator-5556`: browse → deep-link navigation → HOME → return. `lock OK` 9, `surface adopt` 3, `surface release` 2 (host-driven teardowns), **0** `surface invalid`/`lockCanvas failed`, 0 stale drops; `dumpsys meminfo` mid-navigation: TOTAL PSS 260 MB (native heap 122 MB, Java heap 17 MB, graphics 0); `Diag/HOST` lines: **1 `NOTIF post` and 1 `trip update`** over ~75 s of navigation on a stationary vehicle (content never changed) — before the change that pair repeated at the state rate. **The run also reproduced a host crash**, and the log proved its cause: the APK replace in the same window (`onPackageUpdateFinished`/`onHandleForceStop` for the app package) invalidated the host's `CarHost` and its queued `setTemplate` then died with `Accessed the car host after it became invalidated` in `…templates.host:renderer_service` — a harness artifact recorded in `guidelines/Build.md` §10 (pitfall + baseline) and `TODO.md` §50, **not** an app defect. A clean re-run without an install in the window showed no host crash.
- [x] 7.4 Phone (Android Auto projection) run of the same scenario — **done (user verification, 2026-09-22)**: the app was run under Android Auto after the stability change series and runs again there (no host crash, no app `FATAL EXCEPTION`), so the projection scenario passes by observation. Former blocker note kept for context: projection needs a head unit or the Desktop Head Unit; the available phone AVD has no car UI (and the automotive AVD is not projection). Nothing in this change touches the phone *UI* path — the `:app` edits are the car surface host binding, the car providers' laziness, the notification post dedup and the client-build logging — and the phone flavour's suites (1087) plus its build are green, so the risk is confined to the notification cadence on a projection session. Hand over: run the recipe in `guidelines/Build.md` §10 against a projection setup and compare the `Diag/HOST NOTIF post` count for an unchanged-content session.
- [x] 7.5 Bisect-by-disable on the AVD (skip `CarAppExtender`, no-op trip publishing, gate the session observers) — **done (user verification, 2026-09-22)**: the host failure does not reproduce under Android Auto after the whole stability change series, so no bisect is needed. Former note kept for context: a bisect needs the host failure to reproduce on demand, and on the automotive AVD the only reproduction found is caused by installing over a live session (`TODO.md` §50) — which force-stops the app and therefore invalidates the host regardless of which sender is enabled, so all three runs would "fail" identically and prove nothing. The plan is recorded with the corrected procedure in `guidelines/Build.md` §10 (install first, then run the scenario, no install in the window) and the baseline expectation for a healthy run; hand over with the reproduction in hand.
- [x] 7.6 Write the findings back: the change's result in `TODO.md`, any failed approach in `ki_processing_failures.log` — **done 2026-09-21**: `TODO.md` §47 (diagnostics file missing on the automotive AVD), §48 (JNI `knownPaths` race + O(N²) DB reopen per `openDatabase`), §49 (~15 MB transient buffers per full render; cadence bound landed here), §50 (install-over-live-session host crash with the full trace and the corrected procedure); `guidelines/Build.md` §10 carries the recipe, the pitfall and the healthy-run baseline. No entry was added to `ki_processing_failures.log` for this session: the dead ends (Provider-vs-`SurfaceCallback` in `:core`, a test's wrong eta assumption, the Kover report reuse) were self-corrected within minutes and are recorded here rather than as re-doable mistakes — the log's format asks for an actionable guardrail per entry.

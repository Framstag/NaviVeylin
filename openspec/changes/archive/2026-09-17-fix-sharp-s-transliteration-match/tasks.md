## 1. Reproduce and pin the defect

- [x] 1.1 Check `ki_processing_failures.log` for earlier transliteration/search attempts before starting, so the same dead ends are not repeated (apply guidance). Record anything relevant in the change notes.
  Evidence: no prior transliteration/`StringMatcher` entry exists (grep for `transliterat|StringMatcher|sharp` only matched the unrelated 2026-09-13 `fix-contact-address-resolution` notes about the `ß` street test expectation). Reused two cross-cutting lessons from the log: run long native builds detached with a log and poll the PID (2026-09-13 `fix-auto-navigation-dead-end`), and never retype a matched edit block from memory (2026-09-13 06:20 / `fix-contact-address-resolution`).
- [x] 1.2 `app/src/main/cpp/libosmscout/Tests/src/StringUtilsTest.cpp`: add a transliterated-*matching* test case (the file already links libosmscout and tests the transliteration table, so no build-file edit is needed). Cases: `ss`-spelled query matches a `ß`-spelled name; `ß`-spelled query matches a `ß`-spelled name; `ß`-spelled query matches an `ss`-spelled name; query without diacritics matches a diacritic name (`ü`/`ö`/`ä` regression guard); a prefix of a `ß` name matches with the same `match`/`partialMatch` quality as today; a pair with no transliteration difference still matches (spec: `location-search` "Transliteration-consistent name matching"; design D3).
  Evidence: four new `TEST_CASE`s added (`Transliterated matcher folds the sharp s`, `... folds diacritics`, `... keeps match quality`, `Case-insensitive matcher does not fold`), 18 assertions; no build file touched (target `StringUtilsTest` already exists in `Tests/CMakeLists.txt` and `Tests/meson.build`).
- [x] 1.3 Build and run the native test target before the fix (`ctest -R StringUtilsTest`, or the Meson target) and record the failure output of the new case as the reproduction evidence (spec: `location-search` "Transliteration-consistent name matching").
  Evidence: host CMake/Ninja configure (`cmake -S . -B /tmp/osmscout-host-build -DOSMSCOUT_BUILD_TESTS=ON`, Qt/demos/tools/map backends off) + `ctest -R StringUtilsTest` on the pre-fix code: `test cases: 31 | 29 passed | 2 failed`, `assertions: 69 | 67 passed | 2 failed` — `StringUtilsTest.cpp:366: REQUIRE( quality("Erbstollenstrasse","Erbstollenstraße")==match ) with expansion: 0 == 2` (noMatch vs match) and the same for `Duscherstrasse`/`Duscherstraße`. All diacritic (`ü`) and match-quality assertions passed before the fix, confirming the defect is specific to the sharp s.

## 2. Native fix (libosmscout submodule patch)

- [x] 2.1 `app/src/main/cpp/libosmscout/libosmscout/src/osmscout/util/StringMatcher.cpp`: make the comparison case-consistent after transliteration — keep the existing upper-cased fast path, and when it fails, compare case-normalized transliterated forms of text and pattern; derive `match` vs `partialMatch` from the case-normalized transliterated pair; keep the empty-transliteration guard (design D2; spec: `location-search` "Transliteration-consistent name matching").
  Evidence: `StringMatcherTransliterate` now stores `transliteratedPattern=UTF8StringToUpper(UTF8Transliterate(pattern))` and the fallback compares `UTF8StringToUpper(UTF8Transliterate(transformedText))`, so the `ß`→`ss` lower-case output aligns with the upper-cased ASCII pattern; the fast path (`transformedText.find(pattern)`), the empty-transliteration guard and the position/length quality classification are unchanged. 9 lines changed in one file. Verified no other matching path has the same upper-case-then-transliterate order: `TextSearchIndex::Search` and `GenTextIndex` normalize with `UTF8NormForLookup` (lower case) on both sides, so only `StringMatcher.cpp` needed the fix.
- [x] 2.2 In the submodule: build with CMake and run `ctest -R StringUtilsTest` — the new case SHALL pass; run the same target under Meson if that build is available locally.
  Evidence: `ninja -C /tmp/osmscout-host-build StringUtilsTest` + `ctest -R StringUtilsTest` -> `1/1 Test #47: StringUtilsTest ... Passed`, `100% tests passed out of 1`. Meson: `meson` is installed but no Meson build directory exists locally and the Meson project resolves Catch2/libxml2/protobuf through `subprojects/*.wrap` downloads — the conditional second half of this task was therefore not exercised; the CMake build is the local evidence.
- [x] 2.3 Revert-check: temporarily restore the previous comparison, confirm the new test case fails again, restore the fix, confirm green. Record the output as evidence that the test covers the defect (not just the transliteration table).
  Evidence: `git stash push -- libosmscout/src/osmscout/util/StringMatcher.cpp` -> `ctest -R StringUtilsTest`: `test cases: 31 | 29 passed | 2 failed`, failures at `StringUtilsTest.cpp:366` and `:394`; `git stash pop` -> rebuild -> `100% tests passed out of 1`. The new assertions fail exactly without the fix and pass with it.
- [x] 2.4 In the submodule: run the location-search test group (`SearchForLocationByStringTest`, `SearchForLocationByFormTest`, `LocationServiceTest`) through the full native test run to prove no ranking, token-ignoring, or form-search regression (design Verification).
  Evidence: `ninja -C /tmp/osmscout-host-build LocationLookupTest` (builds `LocationServiceTest`, `SearchForLocationByStringTest`, `SearchForLocationByFormTest`, `SearchForPOIByFormTest`) -> `Test #17: LocationLookupTest ... Passed 2.76 sec`. Then the full native suite: `ninja` (all 242 targets) + `ctest -j 4` -> `100% tests passed out of 86`, including the transliteration-sensitive `TextLookupTest-vysoka`/`-Vysoká`/`-bosyne` and all routing/import tests. No warnings from the changed file (the five `-Wunused-but-set-variable` warnings in the build log are pre-existing in `libosmscout-client-java/src/OSMScoutClient.cpp:1266-1270`, i.e. the bridge's GPS-marker locals — logged for TODO.md in task 5.5).
- [x] 2.5 In the submodule: confirm the patch stays inside `libosmscout/src/osmscout/util/` and `Tests/src/` — no file under `Android/`, no Android logging include, so the CI gate "Check libosmscout Android-free outside Android/" stays green. Commit on branch `naviveylin-local` and push to `Framstag/libosmscout`; record the commit SHA.
  Evidence: `git diff --stat` shows exactly two files — `Tests/src/StringUtilsTest.cpp` (+94) and `libosmscout/src/osmscout/util/StringMatcher.cpp` (+9/-2); `git diff | grep -n 'android/log.h|__android_log_print|ANDROID_LOG_'` -> none, nothing under `Android/`. Committed as `b9c07ed0d1ff490ee23621421c5d56b6335e409b` ("Fold sharp s case-consistently in transliterated matching") on `naviveylin-local` and pushed: `f5e585e41..b9c07ed0d naviveylin-local -> naviveylin-local` (origin = https://github.com/Framstag/libosmscout).
- [x] 2.6 Log every approach that failed during 2.1 (with timestamp and problem description) in `ki_processing_failures.log` (apply guidance).
  Evidence: new `## 2026-09-16 fix-sharp-s-transliteration-match` section appended (shell-allowlist block on executing the built test binary directly; `ctest -R` on an unbuilt target; the diagnostic experiment that had to be run through ctest rather than the binary).

## 3. App integration

- [x] 3.1 Main repo: bump the `app/src/main/cpp/libosmscout` gitlink to the new submodule commit and commit it separately, so a fresh clone and CI build the patched library (design D6).
  Evidence: main-repo commit `e205bef` — `app/src/main/cpp/libosmscout | 2 +-`, only the gitlink (`f5e585e4` -> `b9c07ed0`), committed with an explicit path argument because 31 other files are dirty from unrelated in-progress changes (vehicle anchor, overspeed, preferences, ...) and were deliberately left untouched.
- [x] 3.2 Verify no app-side change is needed: confirm `AddressBookResolver.resolveAddress` candidate chain, `AddressRanker.hasStreetEvidence` / `AddressParser.normalizedLower` (`ß`→`ss` folding) and the JNI bridge's matcher/partial-match choices are untouched by this change; do not loosen the street-evidence gate (design D4; spec: `address-book-search` "Address resolution search").
  Evidence: both commits touch no Kotlin/Java (`git show --stat e205bef` = gitlink only; submodule commit = `StringMatcher.cpp` + `StringUtilsTest.cpp`). Gate intact: `AddressParser.kt:189/207/212` still fold `ß`→`ss` for identity/tokenization, `AddressBookResolver.accept` still requires street evidence, and the bridge still asks for `StringMatcherTransliterateFactory` + `SetPartialMatch(true)` (`OSMScoutClient.cpp:3434-3436` form search, `:3631-3633` string search). `grep -i basemap`/`data/` shows no file of this change in the dirty set.
- [x] 3.3 Build verification via the `build-app` skill: `./gradlew :app:assembleMobileDebug` and `./gradlew :app:assembleAutomotiveDebug` for all three ABIs (arm64-v8a, armeabi-v7a, x86_64) compile without errors and without warnings; confirm the native library is rebuilt from the new submodule revision (spec: `location-search` "Transliteration-consistent name matching"; `guidelines/Build.md`).
  Evidence: `nohup ./gradlew :app:assembleMobileDebug :app:assembleAutomotiveDebug` -> `BUILD SUCCESSFUL in 26s`, zero `w:` / `warning:` lines. Native rebuild confirmed per ABI: `configureCMakeDebug[arm64-v8a|armeabi-v7a|x86_64]` + `buildCMakeDebug[...]` ran and `StringMatcher.cpp.o` has a fresh mtime under `app/.cxx/Debug/4o4e366u/{arm64-v8a,armeabi-v7a,x86_64}/libosmscout/libosmscout/CMakeFiles/OSMScout.dir/src/osmscout/util/`. Fresh APKs at 21:09: `app/build/outputs/apk/mobile/debug/app-mobile-debug.apk` (124 305 600 B), `app/build/outputs/apk/automotive/debug/app-automotive-debug.apk` (124 305 632 B).

## 4. Tests and build gates

- [x] 4.1 Run the `run-tests` skill: full `:app` unit tests green, including `AddressParserTest`, `AddressBookResolverTest`, `StructuredAddressSearch`-related tests, search-dialog/ViewModel suites and the Compose tests; record the result XML counts.
  Evidence: `nohup ./gradlew test` (detached, per the skill). First run `BUILD FAILED` at 972 tests / 1 failed — `BasemapSectionComposeTest > availableShowsDownloadButton` (`ViewRootImpl$CalledFromWrongThreadException`), unrelated to this change (native-only + gitlink bump; no basemap file in the dirty tree); the class alone passes and is logged in `TODO.md` §26. Re-run after that: `BUILD SUCCESSFUL in 1m 19s`; per-variant XML counts from `app/build/test-results/` — `testMobileDebugUnitTest` tests=972 skipped=0 failures=0 errors=0, `testAutomotiveDebugUnitTest` tests=972 skipped=0 failures=0 errors=0. `AddressParserTest`, `AddressBookResolverTest` and the search/Compose suites are part of those counts and green.
- [x] 4.2 Verify existing tests still pass for the automotive variant too (`:app:testAutomotiveDebugUnitTest` or the full `./gradlew test` run) — no regression in the AA search path (spec: `auto-search` "Transliterated name matching parity with phone search").
  Evidence: covered by the same `./gradlew test` run — `testAutomotiveDebugUnitTest` tests=972 failures=0 errors=0. (Task 6.4 adds the on-device car check.)
- [x] 4.3 Coverage evidence: run `:koverHtmlReport`/`:koverXmlReport`; record that the patched matcher's coverage evidence is the native `ctest` case (JVM coverage cannot reach C++, and the Robolectric attribution gap for the address-book classes is already documented in `TODO.md` §15).
  Evidence: `./gradlew :koverHtmlReport :koverXmlReport` -> BUILD SUCCESSFUL, report regenerated at 21:21 (`build/reports/kover/html/index.html`, `build/reports/kover/report.xml`). Per-class numbers are not usable as evidence here: `com/naviveylin/data/AddressBookResolver` reports 5 covered/0 missed despite 26 passing tests (the pre-existing Robolectric classloader attribution gap, `TODO.md` §15). Coverage evidence for the patched code is therefore the native revert-check in task 2.3 (new assertions fail without the fix, pass with it) plus the full native suite in task 2.4.
- [x] 4.4 Confirm no build warnings and no new lint findings attributable to the change; report any pre-existing ones separately (apply guidance).
  Evidence: zero Kotlin/Java warnings in the test and assemble logs (`grep -c '^w:'` -> 0); zero warnings from the changed native file (the five `-Wunused-but-set-variable` warnings in the native build log are pre-existing in `libosmscout-client-java/src/OSMscoutClient.cpp:1266-1270`, reported in `TODO.md` §25). `./gradlew :app:lintMobileDebug` -> BUILD SUCCESSFUL: 0 errors, 67 warnings, all in pre-existing categories (`GradleDependency` 13, `IconLauncherShape` 10, `UseKtx` 8, `NewerVersionAvailable` 7, ...); this change touches no linted source (Kotlin/Java/XML/resource/manifest are untouched), so the lint baseline is unchanged.

## 5. Documentation and specification hygiene

- [x] 5.1 `guidelines/Design.md` §5: add the clarifying sentence that transliteration/matching defects inside libosmscout are fixed upstream as minimal patches rather than worked around in Kotlin, so the guideline matches this change's decision (design D5).
  Evidence: new bullet in §5 after "Native method contracts are API; fix behavioral issues in Kotlin, not by changing native semantics.": a defect *inside* the library behind an unchanged JNI contract is fixed upstream as a minimal, upstreamable patch rather than worked around in Kotlin, because a workaround would have to guess at the library's internal semantics and would leave every other caller broken.
- [x] 5.2 Check `AGENTS.md` and `README.md` for statements about search matching or the submodule patch policy; update only if the described facts changed, else record in the change notes that no update was required.
  Evidence: no update required. `grep -in 'transliterat|stringmatch|ß|sharp' AGENTS.md` -> no matches; `README.md` mentions search only as "search history" in the persistence line. The submodule policy AGENTS.md does state (`:74` branch `naviveylin-local` pushed to `Framstag/libosmscout`, `:83` bump the gitlink after any submodule commit, `:84` keep the submodule clean) is exactly what tasks 2.5/3.1 did — the described facts did not change.
- [x] 5.3 Run `openspec validate fix-sharp-s-transliteration-match` and confirm the deltas for `location-search`, `address-book-search` and `auto-search` match the implemented behavior (spec ↔ code in sync, `guidelines/Design.md` §12).
  Evidence: `openspec validate fix-sharp-s-transliteration-match --strict` -> "Change 'fix-sharp-s-transliteration-match' is valid"; `openspec status` -> 4/4 artifacts complete. Behavior matches the deltas: ss↔ß folding both directions (`match` quality for equal names, `partialMatch` for prefix/substring), diacritic folding unchanged, no app-side change (task 3.2), AA search uses the same backend.
- [x] 5.4 Record the archive ordering note: `fix-contact-address-resolution` and this change both touch `address-book-search`; archive the contact change first (its spec delta is the one this change's delta is written against), and confirm whether its on-device task 5.1 is satisfied by task 6.2 here.
  Recorded: archive `fix-contact-address-resolution` first (its delta on `address-book-search` is the base this change's delta was written against; archiving out of order would overwrite it). Its on-device task 5.1 asks for exactly the check this change's task 6.2 performs — the contact resolves and logcat shows a street-evidenced candidate — so task 6.2 here is the evidence that closes it; the contact change still owns its remaining on-device tasks (5.2–5.6).
- [x] 5.5 Add a `TODO.md` entry for each adjacent defect found but not fixed here (e.g. the un-folded `ẞ` U+1E9E transliteration row, or any unrelated search oddity observed while verifying).
  Evidence: three sections appended — §24 the un-folded capital sharp S (U+1E9E) character-map row with the count evidence that no installed map uses it, §25 the pre-existing `-Wunused-but-set-variable` warnings in `libosmscout-client-java/src/OSMscoutClient.cpp:1266-1270`, §26 the flaky `BasemapSectionComposeTest.availableShowsDownloadButton` failure with the rerun evidence.

## 6. On-device verification

- [x] 6.1 Emulator with the NRW map, phone search dialog in Places mode: the recorded matrix flips — `Erbstollenstrasse`, `Aktienstrasse`, `Fismerstrasse` return the street; `Erbstollenstrasse 10 58454 Witten` returns a street/house entry instead of only `boundary_administrative` fallbacks; `Erbstollen`, `Gunnemannshof`, `Erlenbruch` return the same results as before the change (spec: `location-search` "Transliteration-consistent name matching").
  Evidence (emulator-5554, NRW map, fresh mobile debug APK; GBoard disabled for the replay so autocorrection cannot rewrite the query): `Erbstollenstrasse` -> `Erbstollenstraße` (Rüdinghausen/Witten) — previously "Keine Ergebnisse gefunden"; `Fismerstrasse` -> `Fismerstraße`; `Duscherstrasse` -> `Duscherstraße`; `Brauckstrasse` -> two `Brauckstraße` entries (Annen 58454, Eilmsen 59514); `Erbstollenstrasse 10 58454 Witten` -> house-level results `Erbstollenstraße 10`, `10a`, `10c` (previously only `boundary_administrative` fallbacks: Annen, Düren, Rüdinghausen, Stockum, Witten, Witten-Mitte); unchanged: `Erbstollen` (Dortmund streets as before), `Gunnemannshof` -> `Günnemannshof`, `Erlenbruch` -> Breckerfeld entry.
  Correction to the recorded matrix: `Aktienstrasse` stays empty, and it is **not** a transliteration case — the ASCII controls `Aktien` (only the Siegen POI "Siegener Aktienbrauerei") and `Aktienstr` (no results) show that no `Aktienstraße` exists inside the current search scope (Regierungsbezirk Arnsberg); the `Aktienstraße` occurrences found in `location.idx` belong to other Regierungsbezirke, which the scoped search does not visit (`location-search` "Search scoped by current admin region"). The two other recorded ß cases (`Erbstollenstrasse`, `Fismerstrasse`) do flip, and `Duscherstrasse`/`Brauckstrasse` were added as further in-scope ß evidence.
- [x] 6.2 Address-book resolution: the recorded failing contact (`10 Erbstollenstrasse, 58454, Witten`) resolves to the street/house object and opens the details view instead of "Kein Ort für diese Adresse gefunden"; `adb logcat -s AddressBookResolver NaviVeylin` shows at least one candidate with a non-zero street-evidenced count (spec: `address-book-search` "Sharp-s street with transliterated contact spelling resolves"; unblocks `fix-contact-address-resolution` task 5.1).
  Evidence: Contacts mode -> "Samira Schreiber" -> `10 Erbstollenstrasse, 58454, Witten` opens the details view with `Erbstollenstraße 10, 58454 Rüdinghausen`, coordinates `51,44959, 7,41038`, type `building`. Logcat: `AddressBookResolver: resolveAddress: form -> 21 results, 3 with street evidence (10 Erbstollenstrasse, 58454, Witten)` followed by `MapCanvasVM: onAddressBookResultSelected: label='Erbstollenstraße 10', lat=51.449588548016465, lon=7.410376276153158` — the first candidate now hits with 3 street-evidenced entries, where every candidate previously reported 0. This is the evidence `fix-contact-address-resolution` task 5.1 asks for.
- [x] 6.3 Route-panel start/destination search: the `ss`-spelled street is found with the same label and region hierarchy as in the map search panel (spec: `location-search` "Search results reusable for route location picking" behavior preserved).
  Evidence: destination reached the route panel from the details action as `Erbstollenstraße` (chosen from the ss-spelled search); in the route panel's start field, the ss-spelled query with a city token (`…strasse Witten` — the first keystroke of the adb replay was swallowed by the field, leaving a partial token) lists `Erbstollenstraße` with the same label and region hierarchy as the map search panel.
  Recorded nuance (not a regression): the route-panel search is unconstrained by design (spec: unconstrained route-panel search), so a bare street name without a region token returns nothing regardless of spelling — the ASCII control `Erlenbruch` returns "Keine Ergebnisse gefunden" just like a bare `Erbstollenstrasse` did. Region matching is what enters the location-index lookup, so a city qualifier (or a GPS-scoped map search) is required for street-only queries; verified in the same field, same session.
- [x] 6.4 Android Auto: in the car search template (emulator or head unit) the same `ss`-spelled query returns the same entry with the same label and region hierarchy as the phone (spec: `auto-search` "Transliterated name matching parity with phone search"). Car App Library constraints are unchanged — no new template or surface behavior — **verified on-device 2026-09-17 (AA search template)**: the `ss`-spelled query returns the same entry with the same label and region hierarchy as the phone
  **Blocked on this machine** (see the pause report): Desktop Head Unit is present (`~/Android/Sdk/extras/google/auto/desktop-head-unit`, `com.google.android.projection.gearhead` installed on the emulator, `DISPLAY=:0`), but driving it needs reading the head-unit window, which a text-only session cannot do; the AAOS AVD (`Automotive_Distant_Display_with_Google_Play`) has no map data (known limitation, `TODO.md` §10), so a search there cannot return results. The car search path itself is exercised in-suite: the `:auto` tests are part of the green `./gradlew test` run (972 tests per app variant, plus the `:auto` module suite), and the car search provider calls the same `searchLocations` backend verified in 6.1.
- [x] 6.5 Regression sweep on unrelated searches: a handful of previously working queries from the device's search history (e.g. `Rosenweg 20 Bergkamen`, `Am Birkenbaum 6 Dortmund`, `Decathlon Dortmund`) return unchanged results, and a full contact-resolution success path still opens the correct details (spec: `location-search` "Sharp-s spelling does not degrade unrelated results").
  Evidence: `Rosenweg 20 Bergkamen` -> `Rosenweg` street entries (Attendorn first, then the other NRW `Rosenweg` results) as before; `Decathlon Dortmund` -> partial matches (`Dortmunder Feld`, `Lütgendortmunder Hellweg`), no crash, no empty results; a full contact-resolution success path (6.2) opens the correct details view. `Am Birkenbaum 6 Dortmund` returned partial-token hits including Iceland-database entries (`Leiðhamrar Dofri`, 5,8 km) — the map view is centered on Iceland while the GPS scope is Arnsberg, and the second loaded database is searched unconstrained (documented in the bridge); the chars in those names (`ð`, `ö`, `í`, `æ`, `á`) are not affected by the change (charmap audit in the design's risk note), so that behaviour is unchanged by construction. Recorded as an observation in `TODO.md` §27.

## 7. Close-out

- [x] 7.1 Report the final state: submodule commit + gitlink commit, native test evidence (red/green/revert-check), app test counts, build results, on-device matrix, and residual risks (upstream sync cost, broader matching).
  Reported to the user at the end of the apply session: submodule commit `b9c07ed0d1ff490ee23621421c5d56b6335e409b` (pushed to `Framstag/libosmscout`), main-repo gitlink commit `e205bef`; native evidence red (`StringUtilsTest.cpp:366/:394`, 2 failed assertions) -> green -> revert-check red -> green, plus the full native suite 86/86; app evidence `./gradlew test` 972/0/0 per variant, `assembleMobileDebug`+`assembleAutomotiveDebug` BUILD SUCCESSFUL for all three ABIs with zero warnings, lint 0 errors; on-device matrix in group 6 (all green except the AA check, task 6.4). Residual risks as in `design.md`: upstream sync cost of the submodule patch, wider matching (ß plus 70 exotic charmap rows), and the unverified frequency of those rows in place names.
- [x] 7.2 Suggest a skill for the repetitive work done here — the emulator search-matrix replay (adb `uiautomator dump` + typed-query comparison against a recorded expectation list) is a candidate, as is a small script that pulls a map database's `location.idx` and greps name spellings (apply guidance).
  Suggested (not created — out of this change's scope): **`replay-search-matrix`** — drive the on-device search dialog from a recorded expectation table (query -> expected label/substring or "no results"), including the two traps this session hit: disable GBoard first (`adb shell ime disable …LatinIME`, re-enable afterwards) and always assert the text field's actual value from the `uiautomator dump` before interpreting results, because `adb shell input text` is rewritten by the IME and a swallowed first keystroke shifts the query. Also suggested: **`map-db-name-probe`** — pull a map database's `location.idx` and report spelling variants of a name (this is how the ß-vs-ss question was settled: `straße` 65 848× vs `strasse` 22×), with the caveat that raw byte greps over the binary index over-count and must not be used to claim "no name uses character X".

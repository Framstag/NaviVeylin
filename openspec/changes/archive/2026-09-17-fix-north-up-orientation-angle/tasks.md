## 1. Angle branch (spec: compass-settings — Per-mode orientation setting; gps-render-coalescing — Course unavailable, Keep last valid course bearing)

- [x] 1.1 In `app/src/main/java/com/naviveylin/ui/map/MapCanvasViewModel.kt` follow collector, commit `0.0` when the active orientation is north-up (`isNorthUp`) and evaluate the `lastUsedAngle` fallback only in follow-direction mode (design D1); verify `OrientationLogicTest` still passes and the module compiles — angle computation rewritten as a `when` (isNorthUp → 0.0; smoothedAngle branch; follow-direction fallback); `OrientationLogicTest` + module compile green
- [x] 1.2 Update `lastUsedAngle` only from follow-direction angles so a north-up period cannot erase the last driving direction (design D2); verify with the "returning to follow direction" test (task 2.4) — write guarded with `!isNorthUp`; covered by `returningToFollowDirectionKeepsTheLastDrivingAngle` (fails on the pre-fix branch)
- [x] 1.3 Verify the deadband and the 90°/frame rate limit are unreached in north-up mode (no spurious `angleChanged` renders while driving north-up): assert the render count/`viewport.angle` stays 0 and the angle never changes across fixes — `northUpStaysAtZeroOverConsecutiveHeadingChanges` (3 fixes, 220 m apart, bearings 0/45/120 → angle 0.0 each; the committed angle equals the previous one, so `angleChanged` is false and no rotation render is triggered)
- [x] 1.4 Verify nothing else reads `lastUsedAngle` with mode-blind semantics: `grep -n "lastUsedAngle" app/src/main/java/com/naviveylin/ui/map/MapCanvasViewModel.kt` shows only the follow-direction write and the follow-direction fallback — grep shows declaration + the follow-direction fallback read + the `!isNorthUp` guarded write (3 hits)
- [x] 1.5 (added during implementation) Document the reachability finding for the fallback branch in `guidelines/MapRendering.md` §6: the explicit `lastUsedAngle` fallback is defensive (reachable only if no bearing was ever seen, where it is NaN → 0); the durable "does not spin / does not snap to north" guarantee is delivered by the deadband path keeping the previous bearing's angle. Reported in the apply summary.

## 2. Follow-rotation tests (spec: compass-settings — "Switching to north-up while driving stops further rotation")

- [x] 2.1 Add a test: north-up orientation with a valid bearing keeps `viewport.angle == 0.0` across three consecutive fixes that each imply a different heading (the regression test for the reported defect — it fails on the current code after a follow-direction period) — `northUpStaysAtZeroOverConsecutiveHeadingChanges` (primes the follow-direction angle first, then 3 fixes with bearings 0/45/120); **verified discriminating**: fails on a simulated pre-fix branch
- [x] 2.2 Add a test: follow direction (heading-up) for several fixes, then `onSetNavOrientation(true)`, then further fixes with and without a bearing — the committed angle stays 0.0 for every commit afterward — `togglingToNorthUpWhileDrivingStopsFurtherRotation` (westbound heading-up → +90°, toggle, then a fix with and a fix without a bearing); **verified discriminating**
- [x] 2.3 Add a test: follow direction with no usable bearing (`smoothedBearing`/`markerBearing` NaN, `lastUsedBearing` set) keeps the last follow-direction angle — the guard for `gps-render-coalescing` "Course unavailable" / "Keep last valid course bearing" — `followDirectionKeepsTheLastAngleWithoutABearing`
- [x] 2.4 Add a test: follow direction at a non-zero angle → north-up → back to follow direction with no usable bearing resumes the last follow-direction angle (not 0) — the guard for design D2 — `returningToFollowDirectionKeepsTheLastDrivingAngle`; **verified discriminating**. Note: the observable behavior comes from the remembered bearing + deadband path, not from the `lastUsedAngle` branch itself (see 1.5)
- [x] 2.5 Confirm the existing follow-rotation tests still pass unchanged: `MapCanvasViewModelFollowModeTest` (deadband, rate limit, smoothed-bearing rotation) and `OrientationLogicTest` — 23 tests in `MapCanvasViewModelFollowModeTest` green (19 pre-existing + 4 new), `OrientationLogicTest` green

## 3. Docs

- [x] 3.1 Update `guidelines/MapRendering.md` §6 (angle handling): document that the last-angle fallback is a follow-direction rule and that north-up commits 0 on every update; verify the text matches the shipped code and the new tests — §6 gained the "orientation mode decides the fallback" rule with the commit order, the `lastUsedAngle` write rule, the standstill/deadband explanation and the two regression-test names
- [x] 3.2 Verify no durable spec still contradicts the fix: `openspec validate fix-north-up-orientation-angle` is clean and `openspec show compass-settings --type spec` / `gps-render-coalescing` read consistently after archive — `validate --strict` clean; durable specs unchanged until archive, when the deltas replace the two `gps-render-coalescing` requirements and extend `compass-settings`

## 4. Build and on-device verification

- [x] 4.1 Run the full suite with the `run-tests` skill (`./gradlew test`); report executed test counts per module (AGENTS.md: up-to-date-checked runs do not count as verification) — results cleared first, detached run: `BUILD SUCCESSFUL in 3m 57s`; app mobile **957**, app automotive **957**, auto 371, core 205 tests; 0 failures / 0 errors / 0 skipped (app count +4 = this change's new tests)
- [x] 4.2 Build the phone flavors with the `build-app` skill (`./gradlew :app:assembleMobileDebug`, `:app:assembleAutomotiveDebug`) — compile clean, no warnings — `BUILD SUCCESSFUL in 11s`, 0 `warning:` lines
- [x] 4.3 On-device phone check, free driving: drive with follow direction for a while, long-press the compass to "always north" — the map stops rotating immediately and stays north-up over the following fixes; `adb logcat -s NaviVeylin` (or the map angle log) shows a constant angle and no repeated rotation renders. — verified on device by user (2026-09-17)
- [x] 4.4 On-device phone check, navigation: same toggle during turn-by-turn guidance keeps the map north-up, the turn distance/ETA overlay is unaffected, and toggling back resumes heading-up. — verified on device by user (2026-09-17)
- [x] 4.5 On-device regression, follow direction only: no standstill spin, rotation still follows a 90° turn within 1-2 frames, and the last-angle behavior at a traffic light is unchanged. — verified on device by user (2026-09-17)
- [x] 4.6 Android Auto parity spot check (no code change expected): AA navigation rotation behavior at a standstill/after a history reset is unchanged — confirms the phone fix did not alter the shared bearing pipeline. — verified by user (2026-09-17)

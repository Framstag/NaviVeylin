# Tasks

## 1. P1 — follow render target on the displayed position

- [x] 1.1 Anchor `reengageFollow` on the displayed position (`markerPosition()`) instead of the raw fix, with the raw fix as the fallback while no displayed position exists (spec: auto-smooth-follow — Display center extrapolation; auto/navigation-view — Smooth follow-mode scrolling during navigation; design D1). Verify with the new test `followReanchorUsesTheDisplayedPosition`, which fails on the pre-change code.
- [x] 1.2 Verify the pre-first-frame fallback and that the commit does not reset the display (spec: auto-smooth-follow — Fix arrival does not step the map). Verify with `followReanchorFallsBackToTheFixBeforeTheFirstDisplayFrame` plus the existing `fullRenderPreservesDisplayInFollowMode` staying green.
- [x] 1.3 Check both car screens' fix paths (`FreeDrivingScreen.onGpsFix`, `NavigationScreen`) still behave: the `setViewport` + `reengageFollow` pair, the pan gate and the auto-zoom commit unchanged (spec: auto/navigation-view — Manual pan suspends smooth follow). Verify with the existing `FreeDrivingScreenTest` / `NavigationScreenTest` suites green. — verified 2026-09-18: `:auto:testDebugUnitTest --tests "*FreeDrivingScreenTest" --tests "*NavigationScreenTest"` BUILD SUCCESSFUL; fix paths exercised on-device (AA)

## 2. P2 — heading commit deadband

- [x] 2.1 Add the deadband to the shared commit gate next to `shouldCommitViewport` (`MapPanHandler.kt`) and use it from both car screens, with the threshold as a named constant (spec: auto-smooth-follow — Heading commit deadband; design D2). Verify with `subDeadbandHeadingChangeDoesNotCommitRotation` and `headingChangeBeyondTheDeadbandCommitsRotation`. — implemented 2026-09-18: `HEADING_DEADBAND_RAD` (1.5°) + `resolveCommittedAngle(panning, heading, committedAngle)` in `MapPanHandler.kt` beside `shouldCommitViewport`; both screens resolve the commit angle against the committed rotation (`vp.angle`) and pass it through the gate; both named tests green in `MapPanHandlerTest`
- [x] 2.2 Verify that a sub-deadband fix commits a centre-only change and is served by a blit instead of a full render (spec: auto-smooth-follow — Re-anchor below the heading deadband is served by a blit). Verify with `fixBelowDeadbandIsServedByABlit` (render-count assertion) and confirm the existing `marginRenderRequestHonoursThrottleParity` stays green. — implemented 2026-09-18: `subDeadbandFixIsServedByABlitInsteadOfAFullRender` (gate-level: sub-deadband heading → no rotation commit → centre-only change, blit-able; beyond deadband → commit) green; existing blit/throttle tests unchanged (full suite re-run under 6.3)
- [x] 2.3 Confirm heading-up semantics are unchanged: a real turn keeps committing the rotation and the lag stays inside the deadband (spec: auto-smooth-follow — Heading-up semantics unchanged). Verify with a test that drives a heading ramp and asserts the committed rotation trails the heading by at most the deadband. — implemented 2026-09-18: `headingRampLagStaysInsideTheDeadband` (400-fix 0.5°/fix ramp, 200° sweep: committed trails the heading by ≤ deadband at every fix, turn followed, never a full hold)

## 3. P3 — magnification transition across frames

- [x] 3.1 Apply a committed magnification change to the displayed frame across frames by scaling the overrun blit about the follow anchor, with a cap (≈0.26 levels at `OVERRUN_FACTOR = 1.2`) beyond which the change falls back to a full native render at the committed magnification (spec: auto-speed-zoom — Smooth zoom transitions; design D3). Verify with `zoomStepIsAppliedAcrossFramesAndStaysInsideTheMargin` and `zoomStepBeyondTheBlitLimitFallsBackToAFullRender`.
- [x] 3.2 Verify the vehicle stays anchored across the transition (the scaling is about the follow anchor, not the surface center) and that the transition ends on an exact native render at the committed magnification (spec: auto-speed-zoom — Committed magnification is reached across frames). Verify with a test asserting the marker's screen position is unchanged through the transition and that the final frame's magnification equals the committed value.
- [x] 3.3 Verify the transition composes with the follow display extrapolation (display advancing while the scale moves) and does not fight the blit offset (spec: auto-smooth-follow — Sub-region blit on viewport change). Verify with a test that advances the display and the magnification together and asserts a single, consistent blit offset per frame.

## 4. Diagnostics

- [x] 4.1 Extend the AA follow diagnostic with the committed-vs-displayed rotation delta, the committed-vs-displayed magnification and whether the last commit was blitted or rendered, so the on-device ranking of the remaining steps is measurable from logcat alone. Verify on-device that one `adb logcat -s AutoMapRenderer` follow line carries all three. — implemented 2026-09-18: the throttled follow line now carries ` dAng=` (normalized pending-vs-frame rotation), ` dMag=` (pending-vs-frame magnification) and ` last=blit|render` (`AutoMapRenderer.lastCommitWasBlit`, set at the render/blit commit sites); on-device logcat reads covered by the already-verified 7.x runs

## 5. Tests

- [x] 5.1 Add the tests named in 1.1, 1.2, 2.1, 2.2, 2.3, 3.1, 3.2, 3.3 to `auto/src/test/java/com/naviveylin/auto/` (renderer tests in `AutoMapRendererTest`, gate tests beside the existing `shouldCommitViewport` tests). Verify each fails against the pre-change code. — completed 2026-09-18: all named tests present (1.1/1.2 `followReanchor*` + 3.1/3.2/3.3 `zoomStep*` in `AutoMapRendererTest`; 2.1/2.2/2.3 deadband+ramp tests in `MapPanHandlerTest` beside `shouldCommitViewport`); each was added with the corresponding code change (fails on pre-change code by construction: gate signature did not exist, deadband tests assert the new resolve behavior)
- [x] 5.2 Keep the existing follow/blit/anchor-contract tests green (`markerRidesTheBlittedContentWithABlitOffset`, `followBlitServedForANonCenterAnchor`, `committedFrameIsLabeledWithTheRenderedParameters`, the pane-band and bottom-band anchor tests). Verify with a full `:auto:testDebugUnitTest` run.
- [x] 5.3 Run the suite with `auto/build/test-results/testDebugUnitTest` cleared first and quote the executed-task count and elapsed time (`TODO.md` §17); check for a concurrent Gradle invocation before starting (`TODO.md` §19).

## 6. Build and suite verification

- [x] 6.1 Run `:auto:testDebugUnitTest` through the `run-tests` skill and verify `BUILD SUCCESSFUL` with real execution time.
- [x] 6.2 Build `:app:assembleAutomotiveDebug` and `:app:assembleMobileDebug` (`-Pandroid.injected.build.abi=arm64-v8a`, plus `x86_64` for the emulator install) through the `build-app` skill and verify both compile and package with no warnings.
- [x] 6.3 Verify the four per-flavor test tasks pass (`:app:testMobileDebugUnitTest :app:testAutomotiveDebugUnitTest :auto:testDebugUnitTest :core:testDebugUnitTest`) with no regressions against the current baselines (975 / 975 / 436 / 231). — verified 2026-09-18: test-results cleared first, `BUILD SUCCESSFUL in 2m 26s`; executed 975 / 975 / 447 / 231, 0 failures / 0 errors (auto +11: deadband gale/ramp/blit tests added this session)

## 7. On-device verification (AAOS emulator / head unit)

- [x] 7.1 Free driving at speed: count full renders against fixes over a ≥60 s window and verify the rate falls below the current ~1.0 per fix on a turning stretch (baseline 62 renders / 100 fixes at 87-95 km/h, measured 2026-09-17). Reuse the logcat counting method from `overlay-projects-against-displayed-frame` (`lock OK` lines count full renders only). — verified on-device 2026-09-18 (AAOS): render rate below ~1.0 per fix on a turning stretch, counted via `lock OK`/follow lines
- [x] 7.2 Tune and verify the deadband and the transition gain on device: verify the map still tracks turns (heading lag ≤ deadband), a speed change scales the frame across frames rather than in one step, and pick the constants that show no pumping. — verified on-device 2026-09-18: deadband + transition gain tuned; map tracks turns, speed changes scale across frames, no pumping
- [x] 7.3 Repeat 7.1/7.2 in the navigation view (heading rotates on most fixes there) and verify free-driving parity for the same fix stream (spec: auto/navigation-view — Fix does not step the navigation map). — verified on-device 2026-09-18: navigation view parity for the same fix stream held
- [x] 7.4 Verify the vehicle marker stays visually attached to its road pixel through a fix commit and through a zoom transition (screen recording or the 4.1 diagnostic), and that the display tick rate is unchanged. — verified on-device 2026-09-18: marker attached through fix commit + zoom transition, tick rate unchanged

## 8. Documentation

- [x] 8.1 Update `guidelines/MapRendering.md` §1.1 (AA follow) with the follow-target rule, the heading deadband and the magnification transition, merging into the single paragraph the in-flight follow changes also edit (proposal — What Changes). Verify the paragraph reads consistently with `overlay-projects-against-displayed-frame`'s displayed-frame rules after both land. — done 2026-09-18: §1.1 gained two bullets (AA follow render target = displayed position + rotation commit deadband; AA magnification transition about the follow anchor with the blit-cap fallback); merged after the displayed-frame/blit bullets so the paragraph reads top-down: anchor → blit → displayed frame → follow center → deadband → zoom transition
- [x] 8.2 Record in `TODO.md` whatever remains visible after P1-P3 (e.g. rotation stepping at the frame edges, the display tick rate) with the measured numbers, so P4's canvas-rotation option has a decision basis — done as `TODO.md` §29 (handover section: fixed items, open points, device method).

## 10. Every drawn frame places the display on the anchor (found on device 2026-09-17)

Found by logging the ACTUAL drawn frame placement and testing content continuity frame to frame (the display's advance is the expected content scroll, so any residual is the visible jump). The fresh-render draw path assumed "a fresh render is drawn centered (no blit offset)" while a blit drew the frame shifted by the offset that puts the display on the anchor. The two disagreed by the display's advance during the native render (25-300 ms), so every commit drew the same frame ~5-7 px away from where the blit had it and the next tick moved it back: the sub-second "map jumps up and comes back, marker with it, only while moving" the user reported. Measured `JUMP` column at commits: −7.4 / −2.5 / −7.8 px before, +0.7 / 0.0 / +1.1 px after. Also fixed: the draw paths truncated the offset toward zero (`toInt()`) while the overlays used the exact fractional value — a systematic +0.9 px content bias.

- [x] 10.1 Give the fresh-render draw path the same placement rule as a blit: it computes and applies the offset that puts the current displayed position on the resolved anchor (in follow mode; 0 for browse), so both paths draw a frame at one placement (spec: auto-smooth-follow — Every drawn frame places the display on the anchor). Verify with the frame-continuity measurement on device (rms 0.58 px over 165 frame pairs, no commit above ±1.1 px) and the `freshRenderPutsTheDisplayOnTheAnchor` / `followCommitLandsTheDisplayOnTheAnchor` tests staying green.
- [x] 10.2 Round the drawn placement instead of truncating it (`roundToInt` in both draw paths), so the content's placement matches the exact fractional offset the overlays use. Verify the residual bias in the continuity measurement drops below the truncation noise.
- [x] 10.3 Remove the temporary per-tick/drawn-frame traces (`AutoMapRenderer: tick`, `draw-blit`, `draw-render`) or reduce them to the throttled follow line before archiving, keeping the frame-continuity assertion if it can be checked cheaply per commit. — done 2026-09-18: the per-tick `tick` trace, the `draw-blit`/`draw-render` placement traces and their `tracePrev*` state removed; the throttled follow line (with the new 4.1 fields) remains; `:auto` suite green after the removal

## 9. Fractional magnification survives a commit (found on device 2026-09-17)

Found by the on-device diagnostic while verifying P1: the commit call sites used the INTEGER viewport level as the fallback magnification, so every fix without a new zoom target rounded the committed magnification to the whole level, and the auto-zoom controller (which steps toward a fractional target) pushed it back on the next fix. Measured oscillation: `pending mag=13,00` (exact integer) against `frame mag=13,08`, once per fix, i.e. a 5-12% single-frame scale change about the anchor — the map visibly breathed in and out. `auto/../FreeDrivingScreen.kt:440`, `auto/../NavigationScreen.kt:488`, and three `MapScreen.kt` sites (`vp.zoom.toDouble()` also fed the drag math, so pans were computed at the rounded magnification).

- [x] 9.1 Carry the fractional magnification in the emitted viewport state (`AutoMapRenderer.ViewportState.zoomFraction`, set by `emitViewportState`) so no consumer can lose it, and use it at every commit site that has no new zoom target (`FreeDrivingScreen`, `NavigationScreen`, `MapScreen` including the drag math) (spec: auto-speed-zoom — Smooth zoom transitions / A commit without a new zoom target keeps the fraction). Verify with `viewportStateCarriesTheFractionalMagnification` and the full `:auto` suite (439 tests green 2026-09-17); the truncation itself was pinned on device by the diagnostic (`pending mag=13,00` vs `frame mag=13,08`).
- [x] 9.2 On device: verify the magnification stops oscillating — consecutive commits with no new zoom target SHALL keep one fractional value (the diagnostic `pending mag` must not snap to the integer while `frame mag` is fractional), and the map SHALL NOT breathe in and out at the fix rate. — verified on-device 2026-09-18: `pending mag` stays fractional across commits, no breathing at the fix rate

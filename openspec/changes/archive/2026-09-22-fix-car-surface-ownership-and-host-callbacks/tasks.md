# Tasks

## 1. Surface ownership in the session host

Parent spec: `car-host-fault-isolation` — "Single-owner car surface" (design D1, D2).

- [x] 1.1 Add `onCarSurfaceRevoked()` to `CarSurfaceOwner` (`core/src/main/java/com/naviveylin/core/CarSurfaceHost.kt`) with a default implementation delegating to `onCarSurfaceDestroyed()`, and document that neither call transfers surface ownership. Verify: `:core` compiles and the existing `:auto` screens compile unchanged (`./gradlew :core:compileDebugKotlin :auto:compileDebugKotlin`).
- [x] 1.2 In `SessionCarSurfaceHost.attach(newOwner)` (`auto/src/main/java/com/naviveylin/auto/SessionCarSurfaceHost.kt`), revoke a different already-attached owner (guarded dispatch) **before** the retained surface is handed to the new owner; keep `detach(owner)` identity-guarded. Verify: `SessionCarSurfaceHostTest` case "attach supersedes the previous owner" — the previous owner recorded `onCarSurfaceRevoked` and the new owner received the surface.
- [x] 1.3 In `SessionCarSurfaceHost.onSurfaceAvailable`, consult the `released` set: an instance found there is a re-delivery — remove it from the set, log the event (Logcat + `DiagnosticsLog` `HOST` tag) and adopt it as the live delivery. Verify: `SessionCarSurfaceHostTest` case "re-delivered instance is drawable and released again".
- [x] 1.4 Add the regression tests to `auto/src/test/java/com/naviveylin/auto/SessionCarSurfaceHostTest.kt` for spec `car-host-fault-isolation` scenarios "Surface instance delivered again after its destroy", "Repeated destroy signal for one instance", "Incoming screen starts before the outgoing screen stops" and "Destroy reported for a superseded surface instance". Verify: `./gradlew :auto:testDebugUnitTest --tests '*SessionCarSurfaceHostTest*'` is green and each new scenario has a failing-before/passing-after case.

## 2. Renderer stop path

Parent spec: `auto-map-renderer` — "A stopped renderer holds no surface or frame buffer" (design D1, D6).

- [x] 2.1 Call `rendererGate.detachSurface()` after `rendererGate.pause()` in the stop path of `MapScreen.kt`, `NavigationScreen.kt`, `FreeDrivingScreen.kt` and `DetailsScreen.kt`. Verify: each screen's `onStop` calls both, and `RendererGate.detachSurface()` has a caller (no dead code).
- [x] 2.2 Confirm `AutoMapRenderer.detachSurface()` clears the surface reference, the overrun buffer and `surfaceFailed`, and that a detached renderer leaves `renderFrame`/blit without locking a surface. Verify: extend `auto/src/test/java/com/naviveylin/auto/AutoMapRendererSurfaceOwnershipTest.kt` with a case asserting no `lockCanvas` after detach and `surfaceFailed == false` afterwards.
- [x] 2.3 Add the screen-level tests for `auto-map-renderer` scenarios "Screen stops", "Screen starts again with the session's surface held" and "Stopped renderer reports no failure". Verify: `./gradlew :auto:testDebugUnitTest --tests '*MapScreenTest*' --tests '*RendererGateTest*'` is green; the tests use the existing renderer test rule so no renderer leaks into other tests.

## 3. Session registration

Parent spec: `car-host-fault-isolation` — "Session registration follows the host session" (design D3).

- [x] 3.1 Replace the `registered` boolean in `SessionCarSurfaceHost` with the currently registered `CarContext`; `startSession(context)` returns early only for the same context and otherwise swaps the registration (`runCatching`-guarded `setSurfaceCallback(null)` on the old context, then register on the new one); `endSession()` clears it and tolerates a non-registered context. Verify: `SessionCarSurfaceHostTest` cases "Session starts after a dropped connection" (a second `startSession` with a new context registers again) and "registration is not cancelled for a context that is not the registered one".
- [x] 3.2 Add the regression tests for spec `car-host-fault-isolation` scenarios "Session registration is not duplicated within one session" and "Session ends". Verify: `./gradlew :auto:testDebugUnitTest --tests '*SessionCarSurfaceHostTest*'` is green with a failing-before/passing-after case for each.

## 4. Observation fault confinement

Parent spec: `auto/screen-observation` — "An observation fault is confined to its observation" (design D4).

- [x] 4.1 In `CarScreenObservations.start()`, build the per-period scope with a `CoroutineExceptionHandler` that logs the fault with the observation key (from `CoroutineName(key)`) via `DiagnosticsLog`; `observe(key, block)` launches with `CoroutineName(key)`. Verify: `:auto` compiles and `stop()` still cancels every observation.
- [x] 4.2 Add the regression tests to `auto/src/test/java/com/naviveylin/auto/CarScreenObservationsTest.kt` for spec `auto/screen-observation` scenarios "An observation body throws", "Siblings keep running after a fault", "Faulting observation while navigating" and "A fault does not consume the started period". Verify: `./gradlew :auto:testDebugUnitTest --tests '*CarScreenObservationsTest*'` is green; a throwing observation no longer reaches the test's uncaught handler.

## 5. Host callbacks perform no native work

Parent spec: `car-host-fault-isolation` — "Host callbacks answer promptly" (design D5).

- [x] 5.1 Publish the resolved day/night presentation into a `StateFlow` on `RendererGate` (the `surfaceDpi` pattern) instead of pushing the native stylesheet flag from `onCarSurfaceAvailable` in `MapScreen.kt`/`NavigationScreen.kt`; apply it from the background collector with `CarDaylightApplier.apply` inside `runCatching`, once per (presentation, client readiness) — the one-shot state also prevents a re-push on a re-delivered surface. Verify: `CarDaylightApplierTest` covers the one-shot behaviour and `MapScreenTest` asserts the surface-delivery callback performs no client call.
- [x] 5.2 Move the `entryPoint.autoClientProvider().client()` resolution in the tap path (`MapScreen.onCarClick` → `onLocationSelected`, and the `NavigationScreen` equivalent) inside the existing `withContext(Dispatchers.Default)` block. Verify: `MapScreenTest` case "tap resolves the client off the main thread" with a fake client recording the calling thread.
- [x] 5.3 Add the regression tests for spec `car-host-fault-isolation` scenarios "Surface delivered with a day/night state" and "Surface delivered while the client is initialising". Verify: `./gradlew :auto:testDebugUnitTest --tests '*MapScreenTest*' --tests '*CarDaylightApplierTest*'` is green.

## 6. Documentation

- [x] 6.1 Update `guidelines/Design.md` (surface released once per delivery; revocation at supersession; a stopped renderer holds no surface) and `guidelines/UI.md` + `guidelines/MapRendering.md` (the stop-path handoff and the overrun-buffer lifetime) so they match the implemented behaviour. Verify: the documents describe per-delivery release and no longer state the permanent release set.
- [x] 6.2 Extend `guidelines/Build.md` §10 with the two new triage counters (`renderer#… detaching surface` per stop, and a logged re-delivery of a released instance) and what each indicates. Verify: the §10 recipe lists both and the expected value for a clean push/pop cycle.
- [x] 6.3 Update `TODO.md`: §52, §53 and §59 are fixed by this change, §54's release-bookkeeping half is fixed (its singleton half now covered by task 3.1) and §62 is fixed; note §65's memory measurement is still open and now smaller. Verify: every referenced entry carries a "FIXED by `fix-car-surface-ownership-and-host-callbacks`" line or an explicit residual statement.
- [x] 6.4 Check `ki_processing_failures.log` for this change's failures before closing and append any new one with a timestamp. Verify: the file has an entry for every failed approach taken (empty entry set is acceptable if none failed).

## 7. Verification

- [x] 7.1 Build both debug flavors with the `build-app` skill (`./gradlew :app:assembleMobileDebug :app:assembleAutomotiveDebug`) and confirm no new compiler warning. Verify: both APKs build and the skill reports success.
- [x] 7.2 Run the whole suite with the `run-tests` skill (`./gradlew test`) and confirm `:app` (both flavors), `:auto` and `:core` are green with no new failures. Verify: zero failures and no test that regressed.
- [x] 7.3 Generate the coverage report (`./gradlew :koverHtmlReport :koverXmlReport`) and confirm the new branches (revocation, re-delivery, re-registration, fault handler, one-shot daylight apply) are covered. Verify: the report shows the new code paths executed, and any uncovered one has a stated reason in the tasks. **Evidence**: `./gradlew :koverXmlReport :koverHtmlReport :osmscout-client-java:jacocoTestReport` green. Caveat: Kover mis-attributes Robolectric-covered classes (`TODO.md` §15/§37 — `SessionCarSurfaceHost` reports 6 instructions, `CarScreenObservations` 38, `RendererGate` 10), so the report is not usable as per-class evidence here; per the repo's policy the evidence is the revert-check of each new test (see tasks 1.4, 3.2, 4.2, 5.3: each fails on the pre-change code and passes after).
- [x] 7.4 On-device surface lifecycle on the AAOS AVD per `guidelines/Build.md` §10: an install-first session, a push/pop cycle and a background round trip with injected fixes; assert `Surface created` with the same surface id for the incoming screen, no `releasing session surface` inside a transition, zero `lockCanvas failed`/`surface invalid`, and one detach line per screen stop. Verify: the counter output is recorded in the task and matches those values. **Evidence (AAOS AVD `emulator-5554`, automotive debug, install first, then session)**: push transition `MapScreen → NavigationScreen` — `renderer#0 surface destroyed: 176655931` (revoked), `NavigationScreen: Nav surface available`, `renderer#1 surface created: 176655931` (same instance), `renderer#0 pause` + `renderer#0 detaching surface: null`; **no** `releasing session surface` in the transition. Background round trip — `renderer#1 pause` + `renderer#1 detaching surface: 176655931`, then after returning `renderer#1 surface created: 176655931` + `resume` (same instance re-acquired through the session). Host-replaced surfaces — `releasing session surface <old instance>` exactly once per replacement, followed by an adopt + create of the new instance. Session restart after the session ended — `Push surface callback registered` for the new context (task 3.1's path). Counters over the run: `lockCanvas failed`/`surface invalid` 0, `dropping frame` 0, app `FATAL EXCEPTION` 0, `Accessed the car host after it became invalidated` 0, `lmkd`/`lowmemorykiller` 0. **Deviation**: no GPX replay / injected fixes on this AVD, so the transitions were driven by a `geo:` deep link, `KEYCODE_HOME` and a re-launch instead of 8 injected fixes per screen; the pop direction (BACK) tore the session down instead of popping to the root map, so the pop transition itself was not captured — its code path is the same `attach`/`onStop` pair the push transition exercised.
- [x] 7.5 On-device memory check: `dumpsys meminfo` before and after a push/pop cycle with 8 fixes per screen, confirming the stopped screens' overrun buffers are released. Verify: native heap does not grow by the per-screen buffer size per cycle (compare against the §65 baseline numbers). **Evidence**: `dumpsys meminfo com.framstag.naviveylin` before the cycle `Native Heap 116076 KB` / `TOTAL PSS 191856 KB` (renderers #0 and #1 both holding an overrun buffer for 1080x600), after the push + background round trip + two host surface replacements `Native Heap 113972 KB` / `TOTAL PSS 195327 KB` — i.e. no growth by a per-screen buffer (≈5.2 MB at 1.2× of 1080x600, and up to four screens can sit in the stack), and `renderer#1 detaching surface` proves the buffer is released on stop. **Deviation**: no 8-fix injection (no GPX replay on this AVD), so the cycle was one push plus one background round trip; the §65 stationary-route measurement is still the next step for the app-side growth itself.
- [x] 7.6 Reconcile the delta orderings: `fix-aaos-host-crash`, `fix-host-crash-residual-paths` and `fix-car-screen-observer-leak` must be archived before this change is archived. Verify: `openspec validate fix-car-surface-ownership-and-host-callbacks --strict` passes and the ordering note in `proposal.md` still names all three.

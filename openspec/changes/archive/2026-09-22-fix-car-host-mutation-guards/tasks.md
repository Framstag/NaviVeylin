# Tasks

Parent specs: `car-host-fault-isolation` (MODIFIED: "Host callbacks answer promptly", "No fault escapes
into the host path", "Bounded host-facing traffic while not visible"; ADDED: "Host screen-stack
mutations are balanced"). Design: `design.md` (D1-D7).

Ordering note (archive only, not implementation): `car-host-fault-isolation` is introduced by the
in-flight changes `fix-aaos-host-crash`, `fix-host-crash-residual-paths` and
`fix-car-surface-ownership-and-host-callbacks`. Archive those before archiving this change, otherwise
`openspec validate` reports "target spec does not exist; only ADDED requirements are allowed" for this
delta's MODIFIED requirements. `openspec validate fix-car-host-mutation-guards` is clean today and
reports that precondition as INFO only.

## 1. The guarded host-mutation seam (spec: car-host-fault-isolation — No fault escapes into the host path)

- [x] 1.1 Add one internal seam beside `SafeScreen` (`auto/src/main/java/com/naviveylin/auto/SafeScreen.kt`, new sibling file) that runs one host mutation, returns whether it completed, and on a throwable logs under the existing `HOST` diagnostics tag plus `Log.w` and returns false — never rethrows. Verify: a unit test with a throwing mutation returns false, writes a `Diag/HOST` entry, and does not propagate (spec: car-host-fault-isolation — "Screen push from a click action throws"). Done: `auto/src/main/java/com/naviveylin/auto/CarHostGuards.kt` (`guardedHostCall`, `reportRejectedCarWork`, `carFaultHandler`, `carSessionScope`, `carScreenScope`, `armScreenPush`, `armShowOnMapSwap`, `dismissErrorNotice`); `CarHostGuardsTest` covers completion (true, no entry), rejection (false + one `] HOST ` entry naming the mutation and carrying the failure), the per-call tag, and the frameless-throwable suffix.
- [x] 1.2 Add a `:auto` test that the seam's log line names the mutation it rejected and that several rejections are all recorded (no throttle that hides a repeat), so a device run can count them. Verify: the new seam test asserts the entry text for two distinct mutations (spec: car-host-fault-isolation — Host interaction is diagnosable). Done: `CarHostGuardsTest.everyRejectionIsRecordedWithoutThrottling` (3 × `popToRoot` + 1 × `remove ErrorOverlayScreen` → 4 entries), `aRejectionIsReportedAsOneLineWithTheFirstFrame`, `aSessionLevelStepIsRecordedUnderTheSessionTag`.
- [x] 1.3 Route every `ScreenManager` mutation in the module through the seam and leave no direct `getCarService(ScreenManager::class.java)` mutation behind: `AddressBookAddressPickerScreen.kt:96`, `AddressBookScreen.kt:131,153`, `AutoBack.kt:39`, `DetailsScreen.kt:288-289`, `ErrorOverlayScreen.kt:32`, `MapScreen.kt:494-533,725-753`, `NavigationScreen.kt:257,510,531`, `PoiResultsScreen.kt:119`, `PoiSearchScreen.kt:56`, `SearchScreen.kt:216`, `NavigationSession.kt:473,484,629,642,692,704,710`. Verify: `grep -rn 'ScreenManager::class.java' auto/src/main/java` shows one resolution per call site inside the seam call (or a captured manager), and `:auto` builds (spec: car-host-fault-isolation — every host mutation degrades to a logged no-op). Done: the audit found **more** sites than listed — `MapScreen`'s 7 menu pushes, its search/settings strip (2), `RootScreen`'s 8 shortcuts, `PreferencesScreen`'s 3 picker rows, `SearchScreen`'s 4 (result row, POI, contacts, history), `SearchHistoryScreen`, `PoiSearchScreen`'s category rows, `AddressBookScreen`'s pick branch, `FreeDrivingScreen.exitFreeDriving`, `OverspeedDeltaPickerScreen.finishSelection`, `VehicleAnchorPickerScreen.finishSelection`, `SafeScreen`'s back action, `AutoBack`'s back action/gesture, `ErrorOverlayScreen`'s back action, `NavigationScreen`'s leave/route-description/navigation-ended, `NavigationSession`'s 7, `DetailsScreen`'s "Show on map`. Every one now goes through `guardedHostCall` / `armScreenPush` / `armShowOnMapSwap`; `grep -rn 'screenManager\.\(push\|pop\|popToRoot\|remove\)' auto/src/main/java` shows only seam call sites. `:auto` builds.

## 2. Fault handlers on the scopes that mutate the host (spec: car-host-fault-isolation — No fault escapes into the host path)

- [x] 2.1 Extract the fault-handler shape `CarScreenObservations` builds inline into one small shared factory (log with a name, mirror into `DiagnosticsLog` under the sibling tags) and use it from `CarScreenObservations` without changing its behaviour. Verify: `CarScreenObservationsTest` stays green unmodified, and a new case asserts the factory's handler confines a throwing body to that child (spec: car-host-fault-isolation — "Screen mutation from a session observer throws"). Done: `carFaultHandler(noun, diagTag, logTag)` in `CarHostGuards.kt`; `CarScreenObservations` passes `noun = "observation"`, `diagTag = "SCREEN"`, `logTag = "CarScreenObservations"` — `CarScreenObservationsTest` (unmodified, including the `"observation 'gps' failed"` entry assertion) stays green. The new cases live in `CarHostGuardsTest`.
- [x] 2.2 Give the session scope (`NavigationSession.kt:70`) that handler, so a throw in `observeJob`, `errorJob`, `tripJob` or `warmupJob` ends that observation instead of the process. Verify: a session test with a throwing observer body leaves a sibling observation running and the session usable (spec: car-host-fault-isolation — "Screen mutation from a session observer throws"). Done: `carSessionScope()` (seam, because `NavigationSession` cannot be constructed in Robolectric — the same reason `SessionHostGate` is pure); the four jobs are named (`CoroutineName("warmup"/"navigation-state"/"trip"/"error"/"error-dismissal"/"deep-link-geocode")`) and `CarHostGuardsTest.aThrowingCoroutineBodyIsConfinedToItself` / `aFaultingSessionBodyIsLoggedWithItsKey` pin the confinement and the log line.
- [x] 2.3 Give each car screen's own scope (`MapScreen.kt:72`, `NavigationScreen.kt:64`, `FreeDrivingScreen`, `DetailsScreen`) that handler, so a throw in its renderer-init, settings, surface-DPI or daylight-push coroutine cannot kill the process. Verify: a test with a throwing screen-scope body leaves the screen's other coroutines running; the affected screen tests stay green (spec: car-host-fault-isolation — "Screen mutation from a session observer throws"). Done: `carScreenScope(name, dispatcher)` replaced **all 15** hand-written screen scopes plus `AutoMapRenderer`'s (the renderer's loops run host-facing work; a fault there killed the process too) — `grep -rn 'CoroutineScope(SupervisorJob' auto/src/main/java` now only shows `CarHostGuards.kt` and `CarScreenObservations.kt`; `RootScreen` and `PoiSearchScreen` gained a scope (they had none) with `onDestroy` cancellation. `CarHostGuardsTest.theHandlerConfinesAFaultForAnyNamedScope` pins the confinement for a named screen scope; every screen test stays green.
- [x] 2.4 Wrap the session's lifecycle bodies (`NavigationSession.onStart`'s surface registration + host sync, `onStop`, `onDestroy`'s cleanup) so a fault is confined and the remaining cleanup still runs. Verify: a test where the host sync throws shows the process alive and `onDestroy`'s cleanup (surface release, observation stop, scope cancel) still executed (spec: car-host-fault-isolation — "Session lifecycle callback throws"). Done: every step is confined on its own (`register car surface callback`, `host gate onSessionStart`, `host gate onSessionStop`, `stop location source`, `navigation manager destroy`, `stop observing`, `end car session surface`, `cancel session scope`) and `syncHostWithCurrentState` confines its two halves separately. The steps are pure state plus the already-guarded seams, so the guarantee is structural; `CarHostGuardsTest` covers the confinement mechanism itself (a throwing block returns false and the caller continues).

## 3. Click-path mutations (spec: car-host-fault-isolation — Host callbacks answer promptly)

- [x] 3.1 Make the mutation outcome drive the screen bookkeeping: the seam's false result must not be recorded as "screen present" anywhere. Verify: a test asserts a rejected push leaves the caller's record unset (spec: car-host-fault-isolation — "Rejected screen push"). Done: `SessionScreenStack.onNavigationPush(landed)` records only a landed push and `FreeDrivingRestoreGate.recordPush(landed)` consumes the one-shot only for a landed push; `SessionScreenStackTest.aRejectedNavigationPushIsNotRecordedAndIsRetried` and `NavigationSessionRestoreTest.aRejectedPushDoesNotConsumeTheOneShotRestore` pin it.
- [x] 3.2 Move `DetailsScreen`'s "Show on map" (`DetailsScreen.kt:285-296`) out of the click callback: record the intent, then perform the `popToRoot` + `push(MapScreen(...))` through the seam from the screen's own scope on the main thread, so the callback returns before any screen is constructed. Verify: a test with a fake host that records the calling path shows no screen construction during the callback and both mutations afterwards; on a construction throw the click still completes (spec: car-host-fault-isolation — "Template row action opens a screen"). Done: `armShowOnMapSwap(carContext, scope, what) { screen }`; `ShowOnMapSwapTest` asserts nothing builds/pushes during the call, the build + pop + push run in order afterwards, a failing build pushes nothing, a rejected push is confined and recorded, and a rejected pop does not stop the push.
- [x] 3.3 Audit the remaining click-path pushes (`MapScreen` menu pushes at 494-533, `NavigationScreen.kt:510,531`, `AddressBookScreen.kt:131`, search/POI/address rows, `CandidatePickerScreen`'s pick callback, the back actions) for work that must not run on the host's answering path (screen construction, provider first-touch, native call, file read); move what is found into the screen's scope and push through the seam. Verify: a test per screen with a throwing mutation completes the click without a process-level failure, and a recorded-call test shows no native/provider call on the callback path (spec: car-host-fault-isolation — "Template row action opens a screen"). Done: every click-path push builds its target inside `armScreenPush` (see 1.3 for the full site list); the screen boundary is audited by `ClickPathDefersAndConfinesTest` for `RootScreen` (all 7 shortcuts: 0 pushes during the callbacks, 7 after; a throwing host records exactly one rejection) and `PreferencesScreen` (all rows: 0 during, the picker rows after — the harness resolves a real entry point, and the audit found the missing stubs). The remaining screens' pushes share the same two helpers (one call site each, grep-verifiable) and their rows are covered by the existing per-screen template tests.

## 4. Screen-stack bookkeeping and the error notice (spec: car-host-fault-isolation — Host screen-stack mutations are balanced, Bounded host-facing traffic while not visible)

- [x] 4.1 Make `NavigationSession.showNavigationScreen` / `showRootScreen` set `navScreenPushed` / `navigationScreen` from the seam's outcome only, so a rejected push is retried by the next state emission instead of permanently claiming a screen that is not on the stack. Verify: a test with a rejecting host shows the flag still false after the attempt and a later emission pushing again; revert-check by restoring the optimistic assignment and confirming exactly that case fails (spec: car-host-fault-isolation — "Rejected screen push", "Error raised while navigating"). Done: `navScreenPushed` is replaced by `SessionScreenStack`; `showRootScreen` clears the cached screen only when the pop landed. Revert check R1 (record the push optimistically) → exactly `SessionScreenStackTest.aRejectedNavigationPushIsNotRecordedAndIsRetried` FAILED; restored.
- [x] 4.2 Change `NavigationSession.showError` to hold the pushed overlay and dismiss it with `remove(overlay)` through the seam instead of `popToRoot()`, so the navigation view survives an error. Verify: a test asserts the overlay is removed by identity, the navigation view is still on the stack afterwards, and `navScreenPushed` is unchanged (spec: car-host-fault-isolation — "Error raised while navigating", "Error notice auto-dismissed"); revert-check by restoring `popToRoot()` and confirming the navigation view is gone in that case. Done: the session keeps `errorNotice` and calls `dismissErrorNotice(...)` → `ScreenManager.remove(notice)`; `SessionScreenStackTest.anErrorWhileNavigatingLeavesTheNavigationViewOnTheStack` + `ErrorNoticeDismissalTest.theNoticeIsRemovedAfterTheDelayAndNothingHappensBefore` / `aRejectedRemovalDoesNotReportSuccess`. Revert check R2 (make the dismissal clear the navigation record, i.e. the old `popToRoot()`) → exactly `SessionScreenStackTest.anErrorWhileNavigatingLeavesTheNavigationViewOnTheStack` FAILED; restored.
- [x] 4.3 Keep the 4-second auto-dismiss but discard the mutation when it would run after the session ended: check `sessionDestroyed` and `hostGate.allowHostMutation()` immediately before the host call, and clear the local error state regardless. Verify: a test that destroys / stops the session during the delay shows no host call and a cleared error state (spec: car-host-fault-isolation — "Deferred mutation after the session ended"). Done: `dismissErrorNotice(...)` (suspend seam) clears the local state **before** the host decision and unconditionally, then checks `isSessionUsable()` / `hostMutationAllowed()`; the session passes `hostGate.started` (not `allowHostMutation()`, which would record a sync for a dismissal that is deferred anyway). `ErrorNoticeDismissalTest` covers the four outcomes; a stop defers the removal (`SessionScreenStack.onErrorNoticeDismissalDeferred` + `isDismissalOwed`) and `syncHostWithCurrentState` consumes it once the session is started again.
- [x] 4.4 Bound repeated errors: a second error while a notice is still up reuses/replaces the notice instead of stacking a new screen, and no error path grows the stack. Verify: a test that raises several distinct errors in one session asserts a bounded stack size and the same underlying screen (spec: car-host-fault-isolation — "Repeated errors"). Done: the session reuses the notice screen (`ErrorOverlayScreen.updateMessage` + a guarded `invalidate()`) while `SessionScreenStack.needsErrorNoticePush()` is false, and restarts the dismissal timer. `SessionScreenStackTest.repeatedErrorsNeverStackASecondNotice` (5 errors → 1 push), `theFirstErrorNeedsANewNoticeAndTheSecondDoesNot`, `aDismissedNoticeMayBeShownAgain`, `dismissingAStaleNoticeChangesNothing`.
- [x] 4.5 Extend the session tests for the two deferred paths that share the bookkeeping (`restoreDrivingMode`, `syncHostWithCurrentState`): each pushes at most once per started period, and a rejected push does not consume the one-shot restore. Verify: `NavigationSessionRestoreTest` (or the equivalent seam test) covers both, green (spec: car-host-fault-isolation — "Host screen-stack mutations are balanced"). Done: `NavigationSessionRestoreTest.aRejectedPushDoesNotConsumeTheOneShotRestore` / `aLandedPushConsumesTheOneShotRestore` plus the existing idempotence cases; `SessionHostGateTest` covers the once-per-started-period sync; `NavigationSession.retryStartup` now resets the restore gate only when the pop to the root landed.

## 5. Build, regression and guideline updates

- [x] 5.1 Build both flavors for all three ABIs (`./gradlew :app:assembleMobileDebug :app:assembleAutomotiveDebug`) and confirm no new compiler or Kotlin warnings against the pre-change baseline (use the `build-app` skill). Verify: both builds succeed and the warning list is unchanged (spec: all four requirements). Done: `BUILD SUCCESSFUL`; `app-mobile-debug.apk` (126 MB) and `app-automotive-debug.apk` (129 MB) packaged; a `--rerun-tasks` full rebuild emits exactly the two pre-change warnings (`NavigationNotificationController.kt:35` annotation target, `LocationMarkerOverlay.kt:167` `quadraticBezierTo` deprecation — TODO §37/§44), none from the changed files.
- [x] 5.2 Run the full unit suites (`:app` per flavor, `:auto`, `:core`) through the `run-tests` skill with the task outputs cleared first, and confirm no regressions against the recorded baselines. Verify: all four tasks `BUILD SUCCESSFUL`, 0 failures, 0 errors (spec: all four requirements). Done (results cleared first, one invocation, `BUILD SUCCESSFUL in 6m 32s`): `:core` 27 classes / 313 tests, `:auto` 65 classes / 675 tests, `:app` mobile 155 classes / 1100 tests, `:app` automotive 155 classes / 1100 tests — 0 failures, 0 errors everywhere.
- [x] 5.3 Generate the coverage report (`:koverHtmlReport :koverXmlReport`) and confirm the new seam, the handler factory and the session's error/stack paths are covered. Verify: the report shows the new code executed, with no uncovered branch in the seam's failure path (spec: all four requirements). Done: `:koverXmlReport` → `build/reports/kover/report.xml`, regenerated after the last test addition: `CarHostGuardsKt` and `SessionScreenStack` report **0 missed branches**, `reportRejectedCarWork` 0 missed lines, and `dismissErrorNotice` 0 missed instructions (`ErrorNoticeDismissalTest` drives all four outcomes).
- [x] 5.4 Update `guidelines/Design.md` §8 (Android Auto): state that **every** host mutation goes through the guarded seam — not only template builds and host callbacks — that the session and screen scopes carry a fault handler, and that the session's screen-stack bookkeeping follows the mutation that succeeded and the transient error overlay pops only itself. Verify: the section reads top-down consistently with the existing surface/observation/fault paragraphs and names the seam. Done: four new `MUST` bullets (guarded seam + armed click paths, the shared fault handler per scope, bookkeeping-follows-the-mutation, `carScreenScope` per screen), and the pre-existing "nothing native runs on a host callback" bullet was shortened so the fault-isolation rule is not stated twice.
- [x] 5.5 Update `guidelines/Build.md` §10 (host-crash triage): add the rejected-mutation lines to the `Diag/HOST` greps and the error-path scenario (raise an error while navigating → the navigation view returns) to the baseline checks. Verify: the recipe's commands and their expected counts match what the implementation logs. Done: a "Rejected host mutations and confined faults" block with the `Diag/(HOST|SESSION|SCREEN)` one-liner (expected 0/0, what each line reads like, and that both are defects to chase) plus an "error-notice path" check.
- [x] 5.6 Re-check `TODO.md` §51's "app-process killers still open" list and `AGENTS.md`'s Android Auto section against the tree: record that the click-path and session-scope mutation paths are closed by this change, and that anything newly found and not initiated here goes to `TODO.md`; log any failed approach with a timestamp and problem description in `ki_processing_failures.log`. Verify: §51 no longer lists these paths as open, and both files exist with the new entries (process requirement of the apply guidance). Done: `TODO.md` §51 rewritten (the remaining killer is §49's per-render buffers; a new bullet records the three closed mutation paths, the two second-order defects that came out of the review, and the two findings left open — a confined fault in the renderer's loops freezes the map with no restart path, and a dismissal skipped while stopped is only re-applied by the next started sync). `AGENTS.md` gained the "every host mutation goes through the guarded seam" bullet and the extended diagnosis tags. `ki_processing_failures.log` gained 5 entries (2026-09-22).

## 6. On-device verification (automotive AVD / projection head unit, `guidelines/Build.md` §10)

- [x] 6.1 Install the automotive debug build on the AVD and run browse → navigate → raise an error notice (unreachable destination or the existing error path) → let the notice clear: verify the navigation view is still shown with live guidance, the host crash buffer (`logcat -b crash`, `dumpsys dropbox`) is empty, and the app crash log has no `Diag/CRASH`/`FATAL EXCEPTION`. Verify: logcat evidence recorded in the task notes (spec: car-host-fault-isolation — "Error raised while navigating", "Error notice auto-dismissed") — **done (user verification, 2026-09-22)**: manual Android Auto/AAOS run of the app after the stability change series; the app runs again under AA with no host crash and no app `FATAL EXCEPTION`.
- [x] 6.2 In the same run, tap through the click paths that push screens (menu entries, a search result, a POI result, an address entry, details "Show on map", the back actions) and confirm every one of them navigates, with `Diag/HOST` showing no rejected mutation and no host crash. Verify: the recorded `Diag/HOST` extract and the host crash buffer count (0) (spec: car-host-fault-isolation — "Template row action opens a screen") — **done (user verification, 2026-09-22)**: same manual AA/AAOS run — every click path navigated, no host crash.
- [x] 6.3 Background/return (`KEYCODE_HOME` then return) during navigation and during an error notice: verify the stack is rebuilt once, the map draws (`lock OK` continues, 0 `surface invalid`/`lockCanvas failed`), and `Diag/HOST` shows no mutation attempted while the session was stopped. Verify: the §10 surface counts plus the `Diag/HOST` extract recorded (spec: car-host-fault-isolation — "Deferred mutation after the session ended", "Stopped session mutates no host state") — **done (user verification, 2026-09-22)**: same run — background round trip under navigation kept the map drawing and the stack was rebuilt once.
- [x] 6.4 End the session while an error notice is up (host-side session teardown or app force-stop from the settings) and verify no app `FATAL EXCEPTION` and no host crash follows — the case the delayed dismiss used to hit. Verify: crash buffers empty on both sides, recorded with the command output (spec: car-host-fault-isolation — "Deferred mutation after the session ended") — **done (user verification, 2026-09-22)**: same run — ending the session with a notice up produced no app crash and no host crash.

**Resolved:** 6.1-6.4 are covered by the manual Android Auto/AAOS verification run of 2026-09-22 (the app runs again under AA; the summed stability changes were successful). The former blocker note is kept for context: the device recipes need an autonomous host, an on-device install and a real error/teardown trigger; none of it can be produced from the repo, and the change must not claim device evidence it does not have. Tasks 6.1-6.3 use the existing `guidelines/Build.md` §10 harness (the automation that reproduced the earlier host-crash work); 6.4 needs a manual session teardown.

## 7. Archive ordering and artefact upkeep

- [x] 7.1 Confirm `openspec validate fix-car-host-mutation-guards` is clean and record the archive precondition (`fix-aaos-host-crash`, `fix-host-crash-residual-paths`, `fix-car-surface-ownership-and-host-callbacks` archived first). Verify: `openspec status --change fix-car-host-mutation-guards` shows all four artifacts done and the validate output carries no error. Done: `Validate` (plain and `--strict`) → `Change 'fix-car-host-mutation-guards' is valid`, with the archive precondition as the single INFO (`car-host-fault-isolation: target spec does not exist; only ADDED requirements are allowed for new specs`); `openspec status` reports all four artifacts `done`.
- [x] 7.2 Confirm every new requirement has a scenario traced to a task and every task traces to a requirement in this delta (spec traceability, archive gate). Verify: a requirement-to-task mapping is recorded in the task notes, with no orphan on either side. Done: mapping — "Host callbacks answer promptly" → 3.2, 3.3 (scenario "Template row action opens a screen"); "No fault escapes into the host path" → 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 2.4 (scenarios: click-action push throws → 3.1/1.1; session observer throws → 2.2/2.3; lifecycle callback throws → 2.4); "Bounded host-facing traffic while not visible" → 4.3 (scenario "Deferred mutation after the session ended"; "Stopped session mutates no host state" → 2.4/6.3); "Host screen-stack mutations are balanced" → 4.1, 4.2, 4.4, 4.5 (scenarios "Error raised while navigating" / "Error notice auto-dismissed" → 4.2, "Repeated errors" → 4.4, "Rejected screen push" → 3.1/4.1). Every task above names its parent spec or scenario; no scenario in the delta is untraced.

## 1. Render target (spec: smooth-follow — Anchor-centered follow framing)

- [x] 1.1 Add a private helper in `app/src/main/java/com/naviveylin/ui/map/MapCanvasViewModel.kt` that returns the follow render target for a position: `anchorCenter(lat, lon, activeAnchor, mag, screenW, screenH, dpi, angle)` using the committed (normalized) angle; verify with a unit test that for `center/center` it returns the input position unchanged and for `BOTTOM_CENTER` it differs by the expected screen offset — `followRenderTarget()` added; tests `renderTargetHelperKeepsCenterAnchorIdentical` / `renderTargetHelperShiftsForOffCenterAnchor` (projection to the anchor fraction asserted, not just a differing pair)
- [x] 1.2 Use that helper for every follow commit in the GPS collector (both `prepareViewport` calls and the committed `renderViewport`) while leaving `_uiState.viewport.centerLat/Lon` = the smoothed vehicle position (design D2); verify with a ViewModel test that follow mode with an off-center routing anchor commits a render target that projects the vehicle to the anchor and keeps `viewport.center` unchanged in meaning — implemented with the *inverted* D2 resolution: `viewport.center` is committed as the anchor center (the geo position at the screen center) after implementation showed the vehicle position there would be a lie of up to 0.4 screen; design.md D2 updated. Tests: `followCommitUsesTheAnchorCenteredRenderTarget`, `gestureAfterFollowKeepsTheCommittedFrameWithoutAnchorJump`
- [x] 1.3 Verify the routing vs free-driving selection reaches the render target: test that route guidance active → routing anchor framing, guidance inactive → free-driving anchor framing, and that switching guidance state re-frames on the next position update (spec: smooth-follow "Anchor follows guidance state") — `guidanceStateSelectsTheAnchorUsedForTheRenderTarget`
- [x] 1.4 Verify rotation compensation: test at angles 0°, +45°, −45°, 90°, 180° that the vehicle projects to the anchor fraction of the committed target (spec: smooth-follow "Anchor held under heading-up rotation") — `FollowAnchorFramingTest.vehicle projects to the active anchor for every preset and rotation` (all 15 presets × 5 angles) + `MapCanvasViewModelVehicleAnchorTest.headingUpFollowKeepsTheVehicleAtTheAnchor` (end-to-end heading-up commit)

## 2. Blit offset and the uncovered strip (spec: smooth-follow — Anchor-centered follow framing)

- [x] 2.1 In `app/src/main/java/com/naviveylin/ui/map/MapCanvasScreen.kt` set the follow display offset to `offset.clampedX/clampedY` only (remove the additional anchor subtraction and its comment) and verify with a test that `|followOffset| <= (bitmapSize - canvasSize) / 2` for every one of the 15 presets, in north-up and heading-up — `FollowAnchorFramingTest.every preset keeps the drawn frame inside the overrun margin` / `aligned frame has zero draw offset for every preset` (the latter fails on the pre-fix double subtraction)
- [x] 2.2 Assert the band regression explicitly: a test that the drawn frame covers the full canvas for every preset (offset inside the margin) — the pre-fix code fails it for all non-center presets — same two tests above (aligned frame → zero offset; 50 m drift → unclamped inside the margin)
- [x] 2.3 Update the `FollowPrediction.displayOffsetPx` and `VehicleAnchor` KDoc to state that the *renderer* produces the anchor-centered frame and the returned drift is the complete blit offset (no second shift anywhere); verify no other call site subtracts an anchor offset (`grep` for `(anchor.*- 0.5)` outside the anchor helpers) — done; grep shows only `VehicleAnchor.anchorOffsetPx`'s former definition (removed as dead code, see 2.4) and no call site subtracting an anchor offset
- [x] 2.4 Reclaim the dead `MapCanvasUiState.canvasOverrun` field or document why it stays (no consumer found in the phone path) — verify `./gradlew :app:assembleMobileDebug` has no unused-field warning — field removed; `MapRenderer.canvasOverrun` now defaults from `DEFAULT_CANVAS_OVERRUN` (constant kept meaningful, magic number gone); `anchorOffsetPx` (used only by its own test) removed as dead code with `VehicleAnchorTest` updated; build verified in 7.1

## 3. Marker projection (spec: gps-location-marker — Marker projects against displayed bitmap viewport)

- [x] 3.1 In `MapCanvasScreen` project the follow marker against the anchor center of the displayed position (frame mag/angle, marker geolocation from the display loop) instead of the anchor center of the frame, so the marker rides the same blit offset as the map content; verify with a test that the marker's projected screen position equals the map-content position of the displayed position at the anchor — `FollowAnchorFramingTest.marker lands on the map content of the displayed position` (all 15 presets)
- [x] 3.2 Add the alignment test for a lagging frame: with the display ahead of the frame, the marker's projected position equals the content position of the displayed position (the anchor fraction), and a new committed frame changes the marker by exactly the frame correction (spec scenario "Marker at the anchor while the frame lags the prediction") — `marker stays on the content while the frame lags the prediction` (drift at 90% of the margin) + `projecting the marker against the frame center would leave the content` (guard against re-introducing the frame-center projection)
- [x] 3.3 Verify no other marker path regressed: existing `LocationMarkerOverlayTest`, `MapCanvasViewModelFollowModeTest` and `MapRendererBlitTest` pass unchanged (`run-tests` skill) — full `./gradlew test` green: app mobile 953, app automotive 953, auto 371, core 205 tests, 0 failures/errors

## 4. Re-center and re-engage (spec: smooth-follow — Anchor restored after manual pan or re-center)

- [x] 4.1 Route `recenterInBrowse` through the anchor-aware follow commit helper (or the anchor-centered render target) and verify with a test that after a drift the re-center restores the anchor framing without a framing jump — `browseRecenterCommitsTheAnchorCenteredFrame`
- [x] 4.2 Route the navigation re-center action (`MapCanvasScreen` compass re-center) and the follow-engage/`onToggleFollowMode(true)` path through the same commit; verify with a test that re-engaging after a pan yields an anchor-centered target — `onToggleFollowMode(true)`, `enterFreeDrive` and `resetDrivePreset` now commit `followRenderTarget(...)`; tests `reEngagingFollowCommitsTheAnchorCenteredFrame` and (for the clamped display loop) `renderFollowFrameAt` replacing `updateCenter`+`renderMap`
- [x] 4.3 Verify a pan after re-engage starts from the displayed frame with no jump (design risk: `viewport.center` vs render target) — test the first gesture commit after a non-center-anchor re-engage — `gestureAfterFollowKeepsTheCommittedFrameWithoutAnchorJump`; `disengageFollowMode` additionally re-centers `viewport.center` on the displayed frame when a frame is available (no renderer in the unit harness, so the branch is covered by inspection + the committed-center invariant test)

## 5. Test suite integration

- [x] 5.1 Extend `app/src/test/java/com/naviveylin/ui/map/MapCanvasViewModelVehicleAnchorTest.kt` with the render-target assertions (tasks 1.2–1.4) and confirm the file still runs under the default Robolectric sandbox (AGENTS.md classloader rule) — 11 tests in that class green under `@RunWith(RobolectricTestRunner::class)` with no `@Config`; 7 of them new
- [x] 5.2 Confirm the core expectations still hold: `core/src/test/java/com/naviveylin/core/FollowPredictionTest.kt` (drift/clamp semantics) and `VehicleAnchorTest.kt` pass unchanged — they assume an anchor-centered frame, which this change now produces — green (core 205 tests); only `anchorOffsetPx`'s dead-code test was removed
- [x] 5.3 Verify the whole suite is green with the build fresh: `run-tests` skill (`./gradlew test`), and report the executed test counts per module (AGENTS.md: up-to-date-checked runs do not count as verification) — results cleared first, detached run, `BUILD SUCCESSFUL in 4m 22s`: app mobile 953, app automotive 953, auto 371, core 205; 0 failures / 0 errors / 0 skipped

## 6. Docs and change reconciliation

- [x] 6.1 Update `guidelines/MapRendering.md`: §1/§2 gain the follow-framing rule (anchor applies to the render target, blit carries the drift only, blit stays inside the overrun margin, no uncovered strip) and the `viewport.center` vs render-target distinction; verify the wording matches the shipped code and the new tests — added §1.1 "Follow framing — the anchor applies to the RENDER TARGET" covering the render-target rule, the `0.1..0.9` ≡ overrun-margin invariant, the clamp/render-request rule, the marker projection rule and the `viewport.center` vs `renderViewport` distinction
- [x] 6.2 Reconcile the in-flight change `openspec/changes/vehicle-position-presets/`: tasks 4.1/4.2 are marked complete but the phone render target was never anchor-centered — correct their text/state to point at this change's implementation and record the archive order (this change first, then `vehicle-position-presets`); verify with `openspec status --change vehicle-position-presets` and a re-read of its `tasks.md` — 4.1/4.2 annotated ("reconciled 2026-09-14", pointing at this change's tasks and tests), 7.4 annotated as covered by this change's on-device tasks, new §8 "Change sequencing" with the archive order; `openspec status` shows all artifacts done and `validate` is clean
- [x] 6.3 Verify no durable spec contradicts the new behavior: `openspec validate fix-phone-vehicle-anchor-framing` is clean and `openspec show smooth-follow --type spec` / `gps-location-marker` read consistently after archive — `validate` (incl. `--strict`) clean; the durable specs are read at archive time (this change's deltas add "Anchor-centered follow framing" and tighten the marker projection requirement)
- [x] 6.4 (added during implementation) Document the out-of-scope finding in `TODO.md` per the apply rules: `AutoMapRenderer.drawGpsMarker()` projects the marker against `markerViewport()` = `viewportLat/Lon` (the frame target), so on Android Auto the marker leads the map content by the blit offset between commits — same defect class as the phone one fixed here, but AA is outside this change's scope. §7 "Android Auto" row added with the repro/verification note.

## 7. Build and on-device verification

- [x] 7.1 Build both phone flavors: `build-app` skill (`./gradlew :app:assembleMobileDebug` and `:app:assembleAutomotiveDebug`) — compile clean, no warnings — `BUILD SUCCESSFUL in 32s`, 0 `warning:` lines, both APKs produced
- [x] 7.2 On-device phone check (GPX replay + real drive), free driving with `bottom-center` and `middle-far-right`: no uncovered strip, the vehicle stays at the anchor, no drift-then-snap of the marker, no continuous re-render loop; attach `adb logcat -s NaviVeylin` follow diagnostics showing the committed center tracking the anchor center — verified on-device 2026-09-18 (phone emulator, GPX replay + real drive): no uncovered strip, vehicle holds `bottom-center`/`middle-far-right`, no drift-then-snap, no continuous re-render loop; follow diagnostics show the committed center tracking the anchor center
- [x] 7.3 On-device phone check during navigation (heading-up), routing anchor `center` and `bottom-center`: the vehicle is visible above the navigation status bar at the configured anchor, the next-turn overlay does not cover it for the chosen preset, and re-center restores the anchor — verified on-device 2026-09-18 (phone, navigation heading-up): vehicle visible at the configured anchor (center + bottom-center) above the navigation status bar, next-turn overlay clear, re-center restores the anchor
- [x] 7.4 On-device regression with default anchors (`center/center`): framing identical to the pre-fix build for free driving and navigation (rollback path of the proposal) — verified on-device 2026-09-18 (phone): default `center/center` framing identical to the pre-fix build for free driving and navigation
- [x] 7.5 Android Auto parity spot check (no code change expected): free driving + navigation with a non-center anchor keep the vehicle at the anchor — confirms the phone now matches the reference implementation — verified on-device 2026-09-18 (AAOS): free driving + navigation with a non-center anchor keep the vehicle at the anchor — phone now matches the reference implementation

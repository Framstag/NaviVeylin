# Tasks — Fix AA Follow-Mode Vehicle Jumps

References: spec `auto-smooth-follow` (delta: Correction easing forward-only "Fix arrival behind the display holds" / "Forward-only across consecutive fixes"; Extrapolation loop gating freeze "Vehicle stops" / "Vehicle resumes after a stop"). Design decisions D1-D3 in `design.md`.

## 1. Shared core — reuse FollowDisplayState

- [x] 1.1 Confirm `FollowDisplayState` (core, phone port of the forward-only rule) is unchanged and its tests pass: `./gradlew :core:testDebugUnitTest` — DONE (class untouched by this change; 11 tests green)
- [x] 1.2 Update the "Phone-only: the Android Auto renderer keeps its own easing" doc comment in `core/src/main/java/com/naviveylin/core/FollowDisplayState.kt` to state phone + AA adoption; verify no API/signature change and `:core:testDebugUnitTest` still green — DONE (comment corrected, class byte-identical)

## 2. Renderer — forward-only display (D1)

- [x] 2.1 Add `private val followDisplayState = FollowDisplayState()` to `AutoMapRenderer`; in `extrapolationTick`, replace the inline ease (L~700-707: `displayLat += (predicted.first - displayLat) * alpha` / `displayLon += ...`) with `state.advance(predicted.first, predicted.second, dtSec, headingDeg)` where `headingDeg` = `gpsMarkerBearing` (NaN skips the rule, same as phone), then copy `state.lat/lon` back into the `@Volatile displayLat/displayLon`; verify existing follow tests still pass (`off`/`blit` unchanged for straight-line constant speed) — DONE (ease replaced; `displayLat/Lon` remain the single source for `markerPosition`/`anchorCenterFor`; `FollowDisplayState(tauSec = EASE_TAU_SEC)`)
- [x] 2.2 Re-sync the state after a frozen gap: on the first tick after a gate-off period (volatile display differs from the state position), `state.reset()` so the next `advance` seeds from the held position (resume-from-frozen, spec "Vehicle resumes after a stop"); also `state.reset()` in `clearOverrunBuffer`; verify resume test covers a held position + a moved fix — DONE (reset paths; resume-from-frozen asserted in `gateFreezeKeepsDisplayAndMarkerAtStop`)

## 3. Renderer — frozen gate + seed-once (D2)

- [x] 3.1 Extract the gate-off handling in `startExtrapolationLoop` (L~653-663) into an internal `onGateInactive()`; REMOVE the `displayLat = Double.NaN; displayLon = Double.NaN` reset — the display and marker stay frozen while the gate is closed (spec "Vehicle stops": marker SHALL remain at the displayed position, no raw-fix fallback, no re-frame); verify the gate-off path keeps `markerPosition()` == the last displayed position — DONE (implementation note: the freeze lives as a gate guard at the top of `extrapolationTick` (`if (!extrapolationGateActive(nowMs)) return`) instead of a separate empty `onGateInactive()` — the loop's gate-off branch keeps the display fields untouched; the guard makes the freeze structural AND testable without the async loop)
- [x] 3.2 Replace the every-fix stationary commit in `setGpsMarker` (L~338-348) with a seed-once latch: re-seed (`display = fix`, `viewport = anchorCenterFor(fix)`, `blitEligible = true`, `requestRender()`) only on the FIRST stationary fix after a moving episode (or when no display exists); subsequent jittery stationary fixes (`fixMoved > 1e-6` deg) update nothing; the latch resets when the vehicle crosses the movement threshold again and on `clearOverrunBuffer`; verify no viewport/display mutation on repeated stationary fixes — DONE (`stationarySeedApplied` latch; `stationaryJitterFixesDoNotReAnchorViewport` asserts the once-only seed + frozen jitter)
- [x] 3.3 Verify `markerPosition()` fallback stays correct: raw fix used only when no display exists yet (pre-first-display); while stopped the marker rides the frozen display; verify markerScreenPosition tests (`markerRidesTheBlittedContentWithABlitOffset`) stay green — DONE (fallback path only; existing AA marker-glue tests unchanged and green)

## 4. Renderer — margin throttle parity (D3)

- [x] 4.1 `RENDER_REQUEST_INTERVAL_MS` 500 → 200 ms (phone's `GPS_FOLLOW_RENDER_INTERVAL_MS`); verify a clamped tick requests the margin render sooner (behavioral test: `marginRenderRequestHonoursThrottleParity` — first clamp fires; a clamped tick 140 ms later does NOT re-request; one 100 ms later does) and no extra native renders are queued (requests coalesce via `pendingRender`) — DONE (constant 200L; behavioral test pins the 200 ms window; note in the test: a ~64 m jump at zoom 12 is only ~2 px (within the 10 px margin), so the test uses a ~1.4 km jump ≈ 36 px to reach the clamp)

## 5. Regression tests (AutoMapRendererTest)

- [x] 5.1 Forward-only hold: feed a moving fix (speed 14 m/s, bearing 90°), run `extrapolationTick` with a fresh `nowMs`; feed a fix that overshot (curve: new fix behind the displayed position along travel); tick again; assert the display does not move backward along travel (progress preserved) and resumes advancing on the next tick once the prediction passes — DONE (`forwardOnlyHoldStopsBackwardSlideAtFixArrival`: hold asserted by exact equality, resume asserted)
- [x] 5.2 Gate freeze: with `asyncLoopsEnabled = false`, drive the gate-off branch after a moving episode; assert display unchanged, `markerPosition()` == display (no raw-fix fallback); then a moving fix + ticks resume from the frozen position without a snap — DONE (`gateFreezeKeepsDisplayAndMarkerAtStop`; also `staleFixHoldsDisplayInsteadOfEasingBack` — NOTE: this existing test pinned the PRE-delta backward ease-back on a stale fix and was updated to pin the new forward-only hold, which the spec now mandates: "displayed position SHALL never move backward along the direction of travel")
- [x] 5.3 Seed-once at stop: `setGpsMarker` at speed 0 with a jittered fix three times; assert the viewport/display update ONCE (first stationary fix) and stay put on the following two — DONE (`stationaryJitterFixesDoNotReAnchorViewport`)
- [x] 5.4 Confirm spec-scenario coverage: straight-line constant speed (no backward `disp` delta), curve overshoot hold, stop/resume seamless — covered by 5.1-5.3; run `./gradlew :auto:testDebugUnitTest` — DONE (AutoMapRendererTest 41/41 green in every run, incl. existing resolved-anchor + marker-glue tests)

## 6. Build, docs, on-device verification

- [x] 6.1 Build both flavors for arm64-v8a: `./gradlew :app:assembleMobileDebug -Pandroid.injected.build.abi=arm64-v8a` and `./gradlew :app:assembleAutomotiveDebug -Pandroid.injected.build.abi=arm64-v8a` — DONE (BUILD SUCCESSFUL both)
- [x] 6.2 Full unit suite: `./gradlew test` (mind the classloader rules from AGENTS.md for tests touching `OSMScoutClient`) — DONE with caveat: `:core:testDebugUnitTest` green, `:auto:testDebugUnitTest` flaky by test order in this working tree (pre-existing in-flight interference: `OverspeedDeltaPickerScreenTest`/`SettingsLoadGuardTest`/`NavigationTemplateFactoryTest` rotate in and out of failures between runs — pass alone and in the trio with AutoMapRendererTest; all 41 AutoMapRendererTest green every run), and `:app:testAutomotiveDebugUnitTest` fails with `java.io.EOFException` on the test worker EVEN WITH the auto changes stashed to the committed state (isolated run) → pre-existing tree state, unrelated to this change's code (all 136 app-automotive test XMLs green before the worker EOF; AGENTS.md documents the stub/sandbox hazard). If `EOFException` persists after a rerun, delete `app/build/test-results/testAutomotiveDebugUnitTest/binary/` (a stale `in-progress-results-generic.bin` from an interrupted run also produces this).
- [x] 6.3 Update `guidelines/MapRendering.md` §1.1 AA follow paragraph: extend the "single follow center / frozen stop" rule to the AA loop (merge with the blit-anchor change's edit in the same paragraph); verify the guideline text matches the implemented behavior — DONE ("AA follow loop shares the same contract" bullet added after "Frozen stop/go (same delta)")
- [x] 6.4 On-device AA GPX replay (head unit / car-connected phone): drive curves + a stop; `adb logcat -s NaviVeylin` — confirm no per-fix backward `disp` delta (no sawtooth), `clamped` stays rare (no render churn), no snap at stop/resume; record the logcat excerpt in the change — **verified 2026-09-17 (AAOS emulator + GPX replay, free driving)** — excerpt: `follow fix=51,576138,7,463120 spd=50,6 … pred=51,576217 … disp=51,576184 … off=-0,3,-16,3 clamped=false` + 15 more; session totals: 16 `stp=true` (stops), 13 `dec=true`, `clamped` **1× total** (rare), max `off` ≈ 17 px (inside the margin, comma = decimal separator) — no per-fix backward `disp` delta, no render churn, no snap at stop/resume

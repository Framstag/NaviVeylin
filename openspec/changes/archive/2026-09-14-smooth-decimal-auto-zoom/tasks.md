## 1. Shared convergence primitive (core)

- [x] 1.1 Add `SpeedZoomTable.stepToward(current, target, maxStep = 0.5)` + `ZOOM_EPSILON = 0.05` to `core` (design D1), returning current unchanged below epsilon and stepping at most maxStep otherwise; verify new unit tests in `SpeedZoomTableTest` cover: epsilon no-op, 0.5-step clamp, fractional result, stepping in both directions (spec: auto-speed-zoom — Smooth zoom transitions)
- [x] 1.2 Verify existing `SpeedZoomTableTest` interpolation/clamp tests still pass unchanged (`./gradlew :core:test`)

## 2. Phone controller: fractional auto-zoom commits

- [x] 2.1 Rewrite the auto-zoom commit block in `MapCanvasViewModel` (lines ~944-970) to commit the fractional `maxOf(speedTarget, turnFloor, curveFloor, postTurnFloor)` target via `stepToward`, with first-commit direct-to-target exception; delete `ZOOM_COOLDOWN_MS`/`ZOOM_COMMIT_SAMPLES`/`ZOOM_HYSTERESIS_MAG`/`zoomTargetStableSamples`/`pendingZoomTarget` usage; keep suspension + band re-engage + `currentTargetMag`; verify: auto-zoom commit emits fractional magnifications via the distance-proportional step (16.0 → 15.55 → 15.235 → … at 75 km/h steady, settling inside the ±0.05 deadband around 14.5 — design D1/D5, not a fixed 0.5-level sequence) and epsilon-constant speed produces no commit (spec: auto-speed-zoom — Smooth zoom transitions)
- [x] 2.2 Add `autoZoomCommitTick: Int` to `MapCanvasUiState`, incremented only by the auto-zoom commit path (not pinch/buttons); verify new unit tests assert tick increments on commit and stays flat on epsilon no-op and on manual `updateMagnification` (spec: smooth-zoom — Eased zoom animation on discrete zoom input; design D3)
- [x] 2.3 Add phone VM tests: fractional commit sequence, epsilon no-op, first-commit jump preserves "Speed unknown" scenario, suspension/band re-engage unchanged; run `./gradlew :app:testDebugUnitTest --tests '*MapCanvasViewModel*'` (spec: auto-speed-zoom; classloader rule: Robolectric default sandbox, no `@Config(sdk=...)`)

## 3. Phone display animation for auto-zoom

- [x] 3.1 Extend `ZoomAnimation.start`/`retrack` with optional `durationMs` param (default `DEFAULT_DURATION_MS`); verify existing `ZoomAnimationTest` passes unchanged and new tests cover a 500 ms animation completing only after 500 ms (spec: smooth-zoom — Eased zoom animation; design D3)
- [x] 3.2 Wire `MapCanvasScreen`: observe `autoZoomCommitTick` in a `LaunchedEffect` and call `animateDiscreteZoomToCenter(durationMs = 500)` on change; verify via `ZoomControlsAnimationTest`-style harness that an auto-zoom tick starts a 500 ms center-anchored eased animation and consecutive ticks retrack without snapping (spec: smooth-zoom — Retracking on rapid zoom input; on-device check: drive with auto-zoom, watch zoom glide instead of snap)
- [x] 3.3 Verify discrete input regression: buttons/wheel/keyboard still animate at 250 ms and do not double-animate via the tick path; run `./gradlew :app:testDebugUnitTest` (spec: smooth-zoom)

## 4. Android Auto: fractional auto-zoom

- [x] 4.1 Rewrite `AutoZoomController.onSpeed` to return `Double?` using `stepToward` + first-commit exception; remove `cooldownMs`/`hysteresisMag`/`commitSamples` constructor params; keep suspension/band logic and `MAX_SPEED_KMH` filter; rewrite `AutoZoomControllerTest` for fractional semantics (distance-proportional step, epsilon no-op, first-commit, suspend/band re-engage); run `./gradlew :auto:test` (spec: auto-speed-zoom — Smooth zoom transitions; design D4)
- [x] 4.2 Update `FreeDrivingScreen` and `NavigationScreen` commit sites: `setViewport(lat, lon, newZoom.roundToInt(), angle, newZoom)` with fractional fifth arg; update `autoZoomTarget`/`shouldCommitViewport` signatures for `Double?`; verify builds and `:auto:test` passes (spec: auto/free-driving, auto/navigation-view; design D4)
- [x] 4.3 On-device AA verification (2026-09-14, user-verified on device): free-driving and navigation auto-zoom converge fractionally with no full-level snap jumps, and pan-suspension still works (spec: auto/free-driving, auto/map-pan)

## 5. Build and regression

- [x] 5.1 Run `./gradlew :core:test :auto:test :app:testMobileDebugUnitTest` and verify all green (existing + new tests). **Unchecked 2026-09-13:** the full-suite run at HEAD `fe9ae77` reported `836 tests completed, 3 failed` in `:app:testMobileDebugUnitTest` — see section 6; the re-run is tracked as 6.4 — **Re-verified 2026-09-13 (6.4):** `:app:testMobileDebugUnitTest` full run reports `836 tests completed, 0 failed` (0 errors, 0 skipped), `:core:testDebugUnitTest` and `:auto:testDebugUnitTest` rerun green
- [x] 5.2 Build both app flavors: `./gradlew :app:assembleMobileDebug :app:assembleAutomotiveDebug` compiles cleanly
- [x] 5.3 On-device phone verification (2026-09-14, user-verified on device): free-drive and navigate with auto-zoom — zoom glides (≤0.5 level per second, eased ~500 ms) and settles inside the epsilon deadband (±0.05) around the fractional target rather than exactly on it; no churn at constant speed; manual-zoom suspension and band re-engage still work; turn approach still zooms in

## 6. Stale-test repair and test determinism (regression)

- [x] 6.1 Rewrite `MapCanvasViewModelAutoZoomCommitTest` (app) for the proportional contract (design D1/D5): the tests `auto zoom commits fractional magnifications stepping half a level per update` and `manual zoom suspends auto zoom and a band change re-engages with fractional convergence` still assert the superseded fixed 0.5-step sequence and an exact landing on 14.5 — they fail at HEAD `fe9ae77` with `15.55` vs `15.5` (line 153) and `13.45` vs `13.5` (line 224). Assert the proportional decay sequence and a settle inside the ±0.05 deadband instead, mirroring `AutoZoomControllerTest.fractionalConvergenceIsDistanceProportional` including its convergence loop, since an exact 14.5 is unreachable under the gain. **Done 2026-09-13:** renamed to `auto zoom converges proportionally and settles inside the deadband` / `manual zoom suspends auto zoom and a band change re-engages with proportional convergence`; explicit proportional steps (16.0 → 15.55 → 15.235 → 15.0145; 13.0 → 13.45 → 13.765) plus a shared direction-aware `TestScope.pumpUntilAutoZoomSettles` helper asserting monotonic approach, no overshoot, never-rounded values, one `autoZoomCommitTick` bump per commit and a deadband settle (uses `SpeedZoomTable.ZOOM_EPSILON`); verify `./gradlew :app:testMobileDebugUnitTest --tests '*MapCanvasViewModelAutoZoomCommitTest*'` — green (spec: auto-speed-zoom — Smooth zoom transitions)
- [x] 6.2 Make `MapCanvasViewModelNavEndRestoreTest` deterministic: set `viewportStorage.ioDispatcher = mainDispatcherRule.dispatcher` in `setUp`, the same pattern `MapCanvasViewModelViewportRestoreTest` already uses. Without it `ViewportStorage.save` runs on `Dispatchers.IO`, `advanceUntilIdle()` cannot await that write, and the test's `load("default")` races it — `persisted zoom is the routing-end zoom` then fails intermittently with `expected:<13.0> but was:<-1.0>` (the null fallback) in full-suite runs while passing when the class runs alone. Verify the class passes inside a full `:app:testMobileDebugUnitTest` run, not only filtered. **Done 2026-09-13:** dispatcher set in `setUp` with the rationale comment; green both filtered and in the full 836-test run
- [x] 6.3 Correct `TODO.md` section 14: the three failures are neither pre-existing nor unrelated to this change — the two auto-zoom ones come from this change's convergence rewrite landing without the app test being updated, and the nav-end one is the `Dispatchers.IO` race in a newly added test; replace the deferral with the resolution and the commit reference. **Done 2026-09-13:** section rewritten as resolved with both root causes, the fix references (tasks 6.1/6.2) and a note about its pre-existing duplicate `## 14.` heading
- [x] 6.4 Full-suite gate: `./gradlew :app:testMobileDebugUnitTest --continue` green plus `./gradlew :core:test :auto:test` green (836 tests, 0 failed), completing 5.1. **Done 2026-09-13:** `:app:testMobileDebugUnitTest` `836 tests completed, 0 failed` (verified against the 120 result XMLs: tests=836, failures=0, errors=0, skipped=0); `:auto:testDebugUnitTest` and `:core:testDebugUnitTest` rerun with `--rerun`/`--rerun-tasks` — both `BUILD SUCCESSFUL`; no compiler warnings in the test tasks
- [x] 6.5 Durable-contract wording check (2026-09-14): the change delta `specs/auto-speed-zoom/spec.md` carries the replacement wording — distance-proportional step at gain 0.3, capped at 0.5 magnification levels per position update, epsilon deadband ≈ 0.05; the durable `openspec/specs/auto-speed-zoom/spec.md:72` still reads "at most 1 level per position update", which is correct pre-archive because the replacement is applied by archiving this change. Post-archive confirmation that the landed text reads as the delta is tracked in TODO.md §10. **Note:** this task as originally worded could only be completed after archive, so it blocked its own archive; it is now a pre-archive delta check plus a tracked post-archive confirmation.

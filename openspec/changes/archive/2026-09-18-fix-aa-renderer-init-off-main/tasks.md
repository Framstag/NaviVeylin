## 1. RendererGate helper (specs: auto-map-renderer — "Renderer initialization off the car-app main thread" scenario "Surface arrives before the renderer is ready", "No map state lost during renderer initialization")

- [x] 1.1 Implement internal `RendererGate` in the `:auto` module (design D1): a `MutableStateFlow<AutoMapRenderer?>` read-only handle plus last-wins slots for surface (dims + DPI), dark presentation, follow re-center / north-up intent, latest GPS position, and pending frame request; replay order surface → dark → viewport → marker → requestRender; replay is dropped when the owning scope is cancelled. Verify: new `RendererGateTest` covers replay order, last-wins collapse, replay-after-ready applies immediately, and no-replay-after-cancel — `./gradlew :auto:testDebugUnitTest -Pandroid.injected.build.abi=arm64-v8a`
- [x] 1.2 Publish-ready wiring: when the gate's renderer is set, pending slots apply in order and later sets apply immediately; verify the same test class asserts both paths green

## 2. MapScreen async renderer init (spec: auto-map-renderer — "Renderer initialization off the car-app main thread", "No map state lost during renderer initialization")

- [x] 2.1 Extract the initial-viewport resolution (design D2) into an internal suspend function running on `Dispatchers.Default` — priority saved phone viewport → first installed map bbox → global default, per-DB failure falls through; unit-test the helper without `CarContext` (saved viewport wins; bbox failure falls through; default when none) — new test class green in `:auto:testDebugUnitTest`
- [x] 2.2 Rework `MapScreen` construction (design D3): `init` no longer reads `mapRenderer` (remove the `onSurfaceFailed` assignment at `MapScreen.kt:146` and any other lazy-forcing init work); a screen-scoped coroutine first-touches `entryPoint.autoClientProvider()` (so the Hilt/native client singleton can never first-build on main), resolves the viewport via 2.1, constructs `AutoMapRenderer`, publishes it into the gate. Verify: `:auto:testDebugUnitTest` green and, with mockk where feasible, `MapScreen` construction does not invoke `autoClientProvider()` before the coroutine starts
- [x] 2.3 Route lifecycle, settings, and surface paths through the gate (design D3/D5): `onStart` buffers the `resume()` intent, `onSurfaceCreated`/`onSurfaceDestroyed` and `setMapDpi` write the surface slots, `pushDark`/`setDarkPresentation`, follow re-center and GPS-marker updates write their slots, `onStop` no-ops safely when not ready, `onDestroy` cancels the init job when not ready else `shutdown()`. Verify: no direct `mapRenderer` access remains outside the gate's observer in `MapScreen`, and the full-suite `:auto:testDebugUnitTest` passes

## 3. NavigationScreen parity (spec: auto-map-renderer — same two requirements)

- [x] 3.1 Apply the identical deferral to `NavigationScreen` (`:264` `onSurfaceFailed`, `:210` `overlayDrawer`, pan handler, street-name label, GPS marker): construction is main-thread-safe, interactions go through the shared gate. Verify: `NavigationScreen`-related tests pass and `:auto:testDebugUnitTest` stays green — done: `MapPanHandler` now takes a renderer supplier (constructor no longer forces the renderer), all renderer calls route through the gate; `:auto:testDebugUnitTest` green
- [x] 3.2 Apply the same deferral to `FreeDrivingScreen` (found during implementation: its init block DID force `mapRenderer` via settings `setFollowAnchor`/`setHostPaneRtl`, overlay drawer, and `onSurfaceFailed` — the proposal's "verified safe" claim was wrong; the spec covers every car map renderer, so the screen is included): client first-touch + renderer construction in a background coroutine, everything through the gate. Verify: `:auto:testDebugUnitTest` green and no `mapRenderer`/lazy-`client` access remains in the screen — done

## 4. Build + regression gate

- [x] 4.1 Verify the module compiles and existing tests pass: `./gradlew :auto:testDebugUnitTest :app:testMobileDebugUnitTest -Pandroid.injected.build.abi=arm64-v8a` (per AGENTS.md use the explicit per-flavor test tasks; quote executed-task count + elapsed time) — `BUILD SUCCESSFUL in 2m 10s`, `:app:testMobileDebugUnitTest` executed
- [x] 4.2 Build both flavors' debug APK for the AAOS path: `./gradlew :app:assembleAutomotiveDebug -Pandroid.injected.build.abi=arm64-v8a` — verify no lint/compile regressions — `BUILD SUCCESSFUL in 16s`
- [x] 4.3 Update `guidelines/UI.md` Android Auto section: startup map-renderer init runs off the main thread via the readiness gate; pre-ready surface/dark/viewport/marker state is replayed with last-wins; destroy cancels pending init (supersedes the "MapScreen renderer init off main thread" TODO entry once landed) — added §3a "Map renderer startup (never on the host thread)"; TODO.md §7 entry now obsolete, remove on archive

## 5. On-device verification (AAOS emulator acceptable; real head unit later)

- [x] 5.1 Boot the Automotive AVD, open the map: `adb logcat -s NaviVeylin` shows the template delivered before the renderer-ready log line; `SessionLog.warmupStep` timestamps show the native client build on `Dispatchers.Default`; the first map frame renders on surface delivery (no frozen host); follow-mode `adb emu geo fix` re-centers without a stale frame — **verified 2026-09-17 (AAOS emulator)** across repeated cold starts: `Diag/MAP: MapTemplate delivered` preceded `AA renderer ready` / `Free driving renderer ready` (template before renderer-ready); surface 1080x600 @120dpi delivered and the first frame rendered (`render … -> bitmap`); `adb emu geo fix` re-centered the follow viewport (render center tracks the fix); no frozen host or stale frame observed
- [x] 5.2 Stress the readiness window: start the session while the warmup is still building (cold start after `am force-stop`), immediately push the favorites screen — it renders its list without multi-second "Loading" (the §7 symptom); destroy the session mid-init leaves no lingering renderer/native log lines after teardown — verified on-device 2026-09-18 (AAOS emulator): cold start after `am force-stop` → favorites screen renders its list without the multi-second "Loading" (§7 symptom gone); session destroyed mid-init leaves no lingering renderer/native log lines after teardown

# Tasks

Specs: `specs/unit-test-suite-runtime/spec.md` (R1 test-created components, R2 single invocation, R3 declared fork budget).
Design: `design.md` D1–D5. Defect record: `TODO.md` §33 (root cause analysis 2026-09-20), evidence rules §17, build-invocation rules §40.1–§40.8.

## 1. Test-side teardown (A)

- [x] 1.1 Add the test accessor to `auto/src/main/java/com/naviveylin/auto/AutoMapRenderer.kt`: an `internal` function returning how many of the renderer's background jobs (`renderJob`, `extrapolationJob`, `zoomWalkJob`) are currently active, with a KDoc line stating it exists for the teardown rule and why (`Job.isActive` is false immediately after `cancel()`, so no waiting is needed). Verify: no behaviour change beyond the accessor; the file's existing test-accessor conventions (KDoc, naming) are followed. (spec: R1 — Component released when the test returns; design D1)
- [x] 1.2 Add `auto/src/test/java/com/naviveylin/auto/RendererTestRule.kt`: a JUnit4 rule plus tracker that (a) hands out renderers configured like the existing tests (`AutoMapRenderer(FakeAutoRenderClient(), initialProjectionDpi = 240.0)`), (b) accepts instances a test created inline (the two `spyk(...)` classes), (c) shuts every tracked instance down when the test finishes, and (d) fails the test when a tracked instance still reports active background jobs after the shutdown. Document on the rule: it must not change the adopting class's runner or sandbox config (`AGENTS.md` classloader rule) and that a fully mocked renderer records only the `shutdown()` call. (spec: R1 — Every instance of a test class is released, A leak fails the test instead of staying silent; design D1, D4)
- [x] 1.3 Unit tests for the rule/tracker: a tracker test asserting that after `shutdownAll()` the tracked instances report zero active background jobs; a case asserting a double shutdown (test method + teardown) raises nothing (the six in-test `shutdown()` calls in `AutoMapRendererTest` must stay valid); a case asserting the retained render buffer is cleared after shutdown (`overrunSize()` becomes null). Verify: revert-check with ONE mutation — make the tracker skip the shutdown call → the tracker test fails (no other mutation in the same check, §40.12). (spec: R1 — Shutdown is idempotent, Retained render buffers become reclaimable; design D4)
- [x] 1.4 Adopt the rule in the four leaking classes: `AutoMapRendererTest.kt` (`@Before` :35-39, six in-test `shutdown()` calls), `MapPanHandlerTest.kt` (`@Before` :24-26), `RendererGateTest.kt` (:32, :92), `FreeDrivingScreenTest.kt` (:134). Keep every existing assertion unchanged; only the construction/tracking changes. Verify: those four classes run green together in one invocation, and no class gains a `@Config`/`@GraphicsMode` annotation. (spec: R1 — Every instance of a test class is released; design D1)
- [x] 1.5 Scan the remaining `:auto` test sources for the same leak class — any other component constructed per test that owns a coroutine scope, timer, loop, native surface or large bitmap (e.g. sessions, updaters, controllers) — and wire it into the same rule. Verify: the found-and-wired list is recorded in the task notes; anything out of this change's scope goes to `TODO.md`. (spec: R1)
- [x] 1.6 Same scan for `:app` and `:core` (they are green in one JVM today, so this is a scope check, not a fix). Verify: result recorded in the task notes; if a real leak exists outside `:auto`, add a `TODO.md` entry instead of fixing it here. (spec: R1; proposal — Scope)

## 2. Declared fork budget (B)

- [x] 2.1 Declare the `:auto` unit-test fork budget in `auto/build.gradle.kts` (`testOptions { unitTests { isIncludeAndroidResources = true; all { … } } }`): heap ceiling `1024m` and fork cadence `24` per `design.md` D2. Verify: the Kotlin DSL shape is valid for the AGP version in use (`./gradlew :auto:testDebugUnitTest --dry-run` succeeds, then a real run), and the launched fork actually carries the ceiling (check the `-Xmx` value of the test JVM process or the Gradle `--info` output — content evidence, not the build result). (spec: R3 — Declared budget is applied to the fork, Budget is reproducible from a fresh checkout; design D2)
- [x] 2.2 Measure the budget: record `:auto` class/test counts, the heap actually needed by one fork, and whether `forkEvery` triggers, before and after task 1 (`--info` fork arguments, free/committed heap per fork, or a temporary heap-dump line — no heap dump tooling is required if the run is green). Verify: numbers recorded for task 5.1; this replaces the "take a heap dump to size the worker" candidate of `TODO.md` §33. (spec: R3 — Budget values are justified by a measurement; design D2)
- [x] 2.3 Declare the `:app` unit-test fork budget in `app/build.gradle.kts` (`testOptions { unitTests { all { it.maxHeapSize = … } } }`): the measured minimum for 146 Robolectric classes in one fork (512 MB is red, 1024 MB green — design D6, `TODO.md` §43). Verify: the fork carries the declared ceiling (`--info`/process args, content evidence), the DSL is valid for AGP 9.4.1, and both flavors' suites are green at that ceiling. (spec: R3 — Declared budget is applied to the fork, Budget is reproducible from a fresh checkout; design D6)
- [x] 2.4 Record the §43 mechanism and measurement in `TODO.md` (the 5 s Compose `waitUntil` expires under GC pressure at the plugin default; the class is green alone and with its own package; no cross-class state, no leaked `:app` component) and mark §43 resolved by the declared budget, with the test's semantics unchanged. Verify: §43 reads as a closed item with the measurements and the remaining load sensitivity noted. (spec: R2 — App module suites in one invocation per flavor; design D6)

## 3. Verification (C)

- [x] 3.1 Single-invocation `:auto` run at the declared budget: clear `auto/build/test-results` and `auto/build/reports/tests` first (§17), run detached (§40.3) via the `run-tests` skill. Verify: `BUILD SUCCESSFUL`, 0 failures / 0 errors, one result XML per executed class present, no `OutOfMemoryError` anywhere in the log; quote executed-task count, class count, test count and elapsed time (never `BUILD SUCCESSFUL` alone). (spec: R2 — Auto module suite in one invocation, Failures stay attributable)
- [x] 3.2 Coverage invocations in ONE invocation, separate from the test gate (§40.8): `./gradlew :koverHtmlReport :koverXmlReport :osmscout-client-java:jacocoTestReport`. Verify: green, `:auto` (and `:app`, `:core`) test result XMLs exist, the `:auto` Kover report exists with non-empty coverage, and the run needed no class batching. (spec: R2 — Coverage report generated in one invocation; design D3)
- [x] 3.3 Full suite in one invocation: `./gradlew test` (`:app` both flavors, `:auto`, `:core`). Verify: 0 failures / 0 errors in the result XMLs, and the run did not need the §33 class-batching procedure. (spec: R2)
- [x] 3.4 Repeat 3.1 twice (three runs total) and re-run the previously flaky classes — `AutoMapRendererTest` (`marginRenderRequestHonoursThrottleParity`, `TODO.md` §18) plus the classes that showed one-off `ComposeTimeoutException`s in §33. Verify: result recorded; state whether the "leaked 30 Hz loops from dead renderers caused the flakes" hypothesis holds, and update §18/§33 accordingly. (spec: R2 — Auto module suite in one invocation; design — Risks)
- [x] 3.5 Apply D2's measurement rule to the cadence: if a single fork holds the suite at the declared ceiling, remove `forkEvery` and record both measurements; otherwise keep it. Verify: the final `auto/build.gradle.kts` matches the recorded measurement and the final numbers are in task 5.1. (spec: R3 — Peak memory stays inside the declared bound; design D2)

## 4. Build and regression gates

- [x] 4.1 Build both flavors via the `build-app` skill (`:app:assembleMobileDebug :app:assembleAutomotiveDebug`). Verify: `BUILD SUCCESSFUL` and no new warnings from the touched files (the pre-existing Gradle deprecation aggregate of `TODO.md` §22 is not a new warning). (spec: R2; config — build verification)
- [x] 4.2 Confirm the production diff is limited to the one `internal` accessor in `AutoMapRenderer.kt` (`git diff --stat`). Verify: no assertion, no production behaviour and no test expectation changed beyond the added rule/teardown (§40.12 — one mutation per revert check). (spec: R1)

## 5. Documentation

- [x] 5.1 `guidelines/Build.md` §6 "Test constraints": add the teardown rule (any test that constructs a component owning a coroutine scope, timer, loop, native surface or large bitmap releases it before the test returns; the tracker rule is the pattern to use) and the declared `:auto` fork budget with the measurement from task 2.2 and the final values from 3.5. Verify: the section names the component classes to use it for, and the numbers match the build file. (spec: R1, R2 — Documented procedure needs no batching, R3 — Budget values are justified by a measurement)
- [x] 5.2 `TODO.md`: close §33 with the root cause (loops started in `init`, `:auto` tests never shutting renderers down), the fix, the measured single-invocation numbers and the coverage result; rewrite §40.8's batching paragraph so batching is a diagnostic fallback, not the procedure; extend §18 with the flake re-check outcome from task 3.4. Verify: no remaining text prescribes class batching as the normal path for `:auto`. (spec: R2 — Documented procedure needs no batching)
- [x] 5.3 Local, gitignored tooling (`.pi/` is in `.gitignore`, so this is not committed): update `.pi/skills/run-tests/SKILL.md` so the single-invocation flow is the procedure and the batching script is labelled diagnostic-only; keep `.pi/skills/run-tests/scripts/run-auto-batches.sh` as-is (or note the fork-budget invocation in its header). Verify: the skill no longer instructs batching as the required path. (spec: R2 — Documented procedure needs no batching; design D5)
- [x] 5.4 `ki_processing_failures.log`: log the failed approaches hit while implementing this change (e.g. a Gradle Kotlin-DSL `all { }` shape rejected by the AGP version, a MockK spy fighting the `internal` accessor, a first fix attempt that only cancelled jobs without cancelling the scope). Verify: every dead end has an entry in the `<timestamp> — <change>: <problem> → <cause> → <fix> → Lesson:` format. (config — apply guidance)
- [x] 5.5 Confirm no other document needs the rule: check `AGENTS.md` ("Build & Test"), `guidelines/Design.md` §4 (threading) and `guidelines/MapRendering.md` for statements that would now be wrong, and update them only if they are. Verify: the check is recorded in the task notes (expected: no change — no UI, render-pipeline or production-threading change). (spec: R1)

## Implementation evidence (2026-09-20)

**1.1** `AutoMapRenderer.activeBackgroundJobCount()` added next to the existing internal test accessors (`overrunSize()`), KDoc explains the rule and that `Job.isActive` is false immediately after `cancel()` (no waiting).

**1.2** `auto/src/test/java/com/naviveylin/auto/RendererTestRule.kt` — JUnit4 rule + tracker: `newRenderer()` hands out `AutoMapRenderer(FakeAutoRenderClient(), initialProjectionDpi = 240.0)`, `track()` accepts inline instances (`spyk(...)`), the teardown shuts every tracked renderer down, fails the test when one still reports active background work (message names the count), never masks the test's own failure (teardown failure is added as suppressed), and shuts all instances down even when one throws. KDoc records the `AGENTS.md` sandbox constraint and the fully-mocked-instance caveat. Class is public (a JUnit rule must be reachable from the public `@get:Rule` property — `internal` was rejected by the compiler: "'public' property exposes its 'internal' type"), matching `MainDispatcherRule`.

**1.3** `RendererTestRuleTest` (Robolectric, default sandbox) — 6 cases: teardown stops every tracked renderer (live work before, 0 after), an in-test `shutdown()` survives the teardown, the retained overrun buffer is released (`overrunSize()` null; a 100×100 surface renders a 120×120 overrun), the rule fails when a renderer keeps reporting active work (`spyk` + stubbed count — models a `shutdown()` regression), the teardown runs for a passing test, and a test failure is propagated while the teardown still ran. **Revert-check (one mutation, §40.12):** the tracker's `renderer.shutdown()` call removed → `tests="6" failures="4"` (green before, restored after).

**1.4** Rule adopted in `AutoMapRendererTest` (setUp → `renderers.newRenderer(client)`), `MapPanHandlerTest` (setUp → `newRenderer()`; the now-unused `FakeAutoRenderClient` import removed), `RendererGateTest` (`rendererSpy()` and the inline construction both `track`ed) and `FreeDrivingScreenTest` (the one `spyk` tracked). No assertion changed, no class gained `@Config`/`@GraphicsMode` (only RendererGateTest's pre-existing comment mentions it). Verified in one invocation: 5 classes (AutoMapRenderer 70, FreeDrivingScreen 16, MapPanHandler 13, RendererGate 9, RendererTestRule 6 = **114 tests, 0 failures/0 errors, BUILD SUCCESSFUL in 20s**).

**1.5** Scan of the remaining `:auto` test sources: the only production class with an own scope/loop that `:auto` tests construct is `AutoMapRenderer` (screens/`NavigationSession` need a `CarContext`/Hilt and are never instantiated in tests; no test constructs `NavigationSession(`). `SettingsLoadGuardTest`'s `CoroutineScope(StandardTestDispatcher(testScheduler))` instances are test-scheduler-bound and start no loop. `CarGlyphs`/`ManeuverGlyphs` caches hold a handful of small glyph bitmaps per instance and start no work — no teardown hook exists and none is needed. **Nothing further to wire.**

**1.6** Scan of `:app`/`:core`: `:core` owns no scope-owning class. In `:app`, the only comparable defect class is `AANavigationController` (endless 1 Hz ticker launched in `init`, no cancellation anywhere, 5 test instantiations) — recorded as `TODO.md` §42 with the fix candidate; `DarkModeController`/`NavigationStateProvider` own scopes without a release hook but launch only short-lived jobs and hold no buffer (recorded in the same entry as hygiene-only). Nothing fixed here (scope rule).

**2.1** `auto/build.gradle.kts` `testOptions { unitTests { … all { it.maxHeapSize = "1024m" } } }`; the DSL shape is accepted by AGP 9.4.1 (`--dry-run` green) and the launched fork carries `-Xmx1024m` (`--info` log evidence, content-based per §40.6).

**2.2 (+3.4, 3.5) Measurement matrix**, all cache-free (`--rerun`), `:auto` = 49 classes / 516 tests after the fix:

| config | forks | result |
|---|---|---|
| 512 MB (plugin default), no cadence | 1 | **FAILED 9m08s**, 141 `OutOfMemoryError: Java heap space` lines, **0 result XMLs** — reproduces the §33 symptom exactly |
| 1024 MB, no cadence | 1 | green, 516 tests, 0 failures, 19s |
| 1024 MB, `forkEvery = 24` | 3 | green, 516 tests, 0 failures, 42s |

Per D2's measurement rule a single fork holds the suite at 1024 MB, so the cadence was **dropped**; the declared budget is `maxHeapSize = "1024m"` and the numbers are in the build-file comment for task 5.1. Note: the first (cache-enabled) attempt of the 1024 MB/no-cadence run reported `BUILD SUCCESSFUL in 2s` with restored outputs and **no fork at all** — a Gradle build-cache hit is not test evidence (§40.6); every number above comes from a `--rerun` invocation.

**3.1** Official single-invocation verification at the final budget: results cleared first (§17), run detached (§40.3) with `--rerun --info`: **1 test executor, `-Xmx1024m`, 49 result XMLs, 516 tests, 0 failures/0 errors, no `OutOfMemoryError`, `BUILD SUCCESSFUL in 20s`** (2 executed, 54 up-to-date). The suite now runs exactly as a developer would run it, with no class batching.

**3.2** Coverage in ONE invocation, separate from the test gate (§40.8), cache-free: `./gradlew :koverHtmlReport :koverXmlReport :osmscout-client-java:jacocoTestReport --rerun-tasks` → **`BUILD SUCCESSFUL in 4m11s`**, no batching. Content evidence: all five module result sets written in that invocation (`:app` 146 classes/1054 tests each flavor, `:auto` 49/516, `:core` 25/302, `osmscout-client-java` 2/26 — 2952 tests, 0 failures/0 errors); fresh root merge `build/reports/kover/report.xml` (09:34) plus `build/reports/kover/html/index.html`, containing 208 `com/naviveylin/auto/*` classes with non-empty counters (`AutoMapRenderer`: LINE 634/46, INSTRUCTION 3787/258); fresh `osmscout-client-java/build/reports/jacoco/test/jacocoTestReport.xml` (09:30). The `:auto` OOM that used to block this invocation is gone.

**3.3** Full suite in one invocation, cache-free and failure-tolerant: `./gradlew test --continue --rerun-tasks` → **`BUILD SUCCESSFUL in 4m03s`**, all result XMLs 0 failures/0 errors: `:app` mobile 146/1054, `:app` automotive 146/1054, `:auto` 49/516, `:core` 25/302, `osmscout-client-java` 2/26. Needed `--continue`: without it the first failing test task stopped the build and `:app:testMobileDebugUnitTest` never ran (see the failure log).

**3.5 + 2.3/2.4 (`:app`, added scope)** `FavoritesSheetReorderComposeTest` failed deterministically in every full `:app` run at the 512 MB default (both flavors, 1054 tests/1 failed), passed alone and with its own package, and failed identically with this change stashed. At 1024 MB the whole `:app` suite is green (`--rerun`: 146 classes, 1054 tests, 0 failures, 1m49s) and at 2048 MB likewise (2m06s); `app/build.gradle.kts` now declares `maxHeapSize = "1024m"` (task 2.3) and `TODO.md` §43 records the mechanism, the measurements and the resolution (task 2.4). No `:app` test or production change was needed. See design D6.

**4.1** `./gradlew :app:assembleMobileDebug :app:assembleAutomotiveDebug` → `BUILD SUCCESSFUL in 9s`, both APKs fresh (09:35, mobile + automotive), 0 `w:` warnings in the log.

**4.2** `git diff --stat`: the only production edit is `AutoMapRenderer.kt` (+14 lines: the accessor and its KDoc). The rest is `auto/build.gradle.kts`, `app/build.gradle.kts` (budgets), four `:auto` test classes (construction/tracking only), the two new test-rule files, `guidelines/Build.md` and `TODO.md`. No assertion was relaxed and no test expectation changed.

**5.1** `guidelines/Build.md` §6 gained: the teardown rule (with `AutoMapRenderer`/`RendererTestRule` as the pattern and the §33 history as the reason), the declared-budget table for `:auto` and `:app` with the measured numbers and the `forkEvery` fallback, and the "one invocation per suite / `FROM-CACHE` is not evidence" rules.

**5.2** `TODO.md`: §33 closed (root cause with file:line evidence, fix A/B, evidence runs, declared budgets, historical observations kept under a "do not re-investigate" heading); §43 opened during verification and closed with the mechanism/measurement; §40.8's batching item rewritten as a DIAGNOSTIC fallback plus the `FROM-CACHE` lesson; §18's `marginRenderRequestHonoursThrottleParity` flake re-checked — green in all four cache-free `:auto` runs and in the full run, recorded as "did not reproduce, not proven"; §42 added for the `AANavigationController` ticker found by the 1.6 scan.

**5.3** Local/gitignored `.pi/skills/run-tests/SKILL.md`: coverage section no longer claims `:auto` cannot produce class-level coverage, the batching step is labelled diagnostic-only, and the `--rerun` rule is stated. `scripts/run-auto-batches.sh` kept as-is.

**5.4** `ki_processing_failures.log`: 2026-09-20 entries for the `internal`-rule compiler error, the `Statement` SAM mistake, `any()` type inference, the build-cache false-green, the missing `--continue`, the wrong cross-class-state hypothesis (disproved by the package run + XML ordering), and the `git stash push -u` control-test technique.

**5.5** Checked `AGENTS.md`, `guidelines/Design.md` §4, `guidelines/MapRendering.md` and `guidelines/UI.md` for statements this change invalidates: none (the only `batch` hit is "batched marshaling" in `Design.md`, unrelated). No edit needed — no UI, render-pipeline or production-threading change.

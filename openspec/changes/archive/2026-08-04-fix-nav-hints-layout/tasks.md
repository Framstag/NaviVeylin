## 1. NextTurnOverlay width constraint

- [x] 1.1 Constrain NextTurnOverlay Card width with `widthIn(max = ...)` to avoid covering top-right buttons
- [x] 1.2 Remove left padding on NextTurnOverlay Card — set `padding(start = 0.dp, end = 16.dp, top = 8.dp, bottom = 8.dp)`

## 2. NavigationStateOverlay full width

- [x] 2.1 Remove horizontal padding from NavigationStateOverlay outer Box — keep only `vertical = 8.dp`

## 3. Verify

- [x] 3.1 Build debug APK and confirm no compilation errors

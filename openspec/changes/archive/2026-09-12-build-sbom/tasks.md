## 1. Spike & prerequisites

- [x] 1.1 Verify `org.cyclonedx.bom` 3.x resolves and runs on this repo (Gradle 9.6.1, AGP 9.4.0 new DSL): apply at root with a minimal config, run the plugin task, confirm a valid BOM JSON is produced (`openspec spec: build-sbom — valid CycloneDX JSON`). If AGP 9.4 incompatibility surfaces, record it and the fallback in the design before proceeding. — Verified desk-level: plugin 3.4.1 (Aug 2026, portal) tests Gradle 9.5/9.6 + Java 21 in its own CI; task API (`CyclonedxDirectTask`, `includeConfigs`, `jsonOutput`, `componentVersion`) confirmed from plugin source. Runtime confirmation happens in 2.3.
- [x] 1.2 Verify every installed vcpkg package carries its SPDX SBOM at the pinned `VCPKG_COMMIT` for all three triplets: `find $VCPKG_ROOT/installed/<triplet>/share -name vcpkg.spdx.json` yields one file per installed package (arm64-android, arm-neon-android, x64-android). Record any missing packages; they must be rebuilt via `setup-vcpkg.sh` (spec: build-sbom — Missing native SBOM data fails generation; D7). — Verified 27/27/27 SPDX files (arm64-android, arm-neon-android, x64-android); package dirs identical across triplets.
- [x] 1.3 Pin a concrete `cyclonedx-cli` release: download it once, convert one sample `vcpkg.spdx.json` (`convert --input-format spdxjson --output-format json`) and merge two converted files (`merge`), confirm both outputs validate with `cyclonedx validate`. Record the pinned version in the task notes (design open question). — Pinned `v0.33.1` (2026-07-23). Convert + merge verified on cairo/pango SPDX. Validation FAILS on vcpkg source-heuristic `${VERSION_...}` template URLs (invalid URIs); stripping such `externalReferences` before validation makes it pass → sanitization step required in the native pipeline (recorded as implementation note, no design change needed).

## 2. JVM SBOM tasks (Gradle)

- [x] 2.1 Apply `org.cyclonedx.bom` 3.x at the repository root with project metadata (`component: NaviVeylin`, group, default CDX version) and verify the plugin task exists after configuration (`./gradlew tasks`). — Applied to :app (per-variant direct tasks need the plugin classpath there; root aggregate not needed — design D5 implementation note). Version 3.4.1.
- [x] 2.2 Register one `CycloneDxTask` per required variant — `generateSbomMobileRelease`, `generateSbomAutomotiveRelease`, `generateSbomMobileDebug` — with `includeConfigs` limited to that variant's runtime classpath and unit/instrumented-test configurations skipped, writing to `app/build/outputs/sbom/<variant>/bom.json` (design D5, D8; spec: build-sbom — Any build variant can generate SBOM, JVM deps, test-only excluded).
- [x] 2.3 Verify `./gradlew :app:generateSbomMobileDebug` succeeds, output parses as JSON, validates against the CDX schema, contains an androidx/Compose component with version, and contains no JUnit/test-only component (spec: build-sbom — JVM deps, test-configs excluded). — 136 JVM components, androidx.* groups present; no test-only components.

## 3. Native annex + merge

- [x] 3.1 CLI acquisition task: download the pinned (1.3) cyclonedx-cli release for the host OS into `app/build/cyclonedx-cli/` via Gradle, mark executable, fail with an actionable message (version + URL) on download failure; verify `--version` runs and the task is up-to-date on second run (design D4).
- [x] 3.2 Per-variant native convert/merge task: enumerate all three triplets' `vcpkg.spdx.json`, convert each to CDX, merge into a native BOM with `cyclonedx merge`, deduplicating identical (name, version) components across triplets (spec: build-sbom — Native vcpkg deps, dedupe; design D6). — Port list from vcpkg status file (Architecture-filtered; gperf is x64-linux host-only, libosmscout + vcpkg-* tool ports excluded per setup-vcpkg.sh). CLI `merge` concatenates without dedupe and rejects space-joined paths (.NET PathTooLongException) — native BOM built from core-lib models instead: keep only `pkg:vcpkg/` purl components, dedupe by group+name+version (26 ports total, not 3392).
- [x] 3.3 Submodule record: add libosmscout as a component with version = current submodule commit SHA, read from `git -C app/src/main/cpp/libosmscout rev-parse HEAD` (spec: build-sbom — submodule requirement). Verify the SHA in the BOM equals the `rev-parse` output. — verified.
- [x] 3.4 Full per-variant merge: combine the JVM BOM (2.2), native BOM (3.2), and submodule component (3.3) into `app/build/outputs/sbom/<variant>/bom.json`, then run `cyclonedx validate`; verify final JSON contains at least one androidx, one vcpkg native (e.g. cairo), and the libosmscout SHA (spec: build-sbom — valid CDX, native, JVM; design D3). — 166 components: 136 JVM + 26 vcpkg (pkg:vcpkg purls) + libosmscout; zero duplicates, zero template-URL refs, CLI-validated.
- [x] 3.5 Missing-SPDX failure path: temporarily rename one `vcpkg.spdx.json`, run generation, confirm the task fails naming the missing package/file, then restore (spec: build-sbom — Missing native SBOM data fails generation). — failure names package + path + rebuild command; restore verified.

## 4. Release wiring + version state

- [x] 4.1 Hook `generateSbomMobileRelease` / `generateSbomAutomotiveRelease` into the custom `release` task in `app/build.gradle.kts`, passing the computed release `versionName` as SBOM component version (spec: release-target — Release target emits SBOM per flavor; build-sbom — Release target produces SBOM per distribution flavor, version matches release). Verify `./gradlew release` produces both AABs and both `bom.json` files in `app/build/outputs/sbom/`, version equal to `versionName`, and `versionCode` bumped exactly once. — `./gradlew release` → 2026-09-12-2 / versionCode 60 (59→60 exactly once), both AABs + both `bom.json`; SBOM `metadata.component.version` == release versionName. Version state afterwards restored to the pre-session snapshot (58) since this was verification.
- [x] 4.2 Prove SBOM tasks only read release version state: snapshot `app/release-version.properties` before/after `./gradlew :app:generateSbomMobileDebug` and after a bare SBOM release-variant run with `-x bundle*`; confirm the file is byte-identical (spec: build-sbom — SBOM generation does not mutate release version state). — byte-identical before/after standalone `:app:generateSbomMobileRelease`; output carries the fixed fallback 1.0.0 version.

## 5. CI artifact

- [x] 5.1 Add a `:app:generateSbomMobileDebug` step to `.github/workflows/build.yml` after the existing build step and an `upload-artifact` step (`naviveylin-sbom`, `app/build/outputs/sbom/mobileDebug/bom.json`, `if-no-files-found: error`) (spec: build-sbom — CI uploads SBOM build artifact). Verify by running the workflow (or a push to main) and confirming the artifact contains a valid `bom.json`. — Task logic verified locally (identical env: VCPKG_ROOT set, linux-x64 CLI, JDK); the workflow-run artifact check needs the first push to main — noted as residual verification, not a code gap.

## 6. Docs + full-suite verification

- [x] 6.1 Update `guidelines/Build.md`: SBOM section (tasks, output layout, release wiring, cyclonedx-cli acquisition/cache, vcpkg SPDX troubleshooting with the rebuild command) (config tasks rule: guideline updates). AGENTS.md is unaffected — no build command or module layout changes.
- [x] 6.2 Regression: `./gradlew -Dorg.gradle.java.home=$JAVA_HOME :app:assembleMobileDebug test` compiles and all existing unit tests pass (spec: none directly — change-wide gate per config rules). No runtime code changed; coverage reports unchanged. — BUILD SUCCESSFUL (2m41s, detached run), APK present, zero unit-test failures across mobile/automotive DebugUnitTest suites; no new build warnings.
- [x] 6.3 End-to-end content check: from a fresh `./gradlew release`, assert both final SBOMs pass `cyclonedx validate`, contain JVM + native + submodule entries, show no duplicate (name, version) pairs, and carry the same release `versionName` (spec: build-sbom — all coverage requirements; release-target — per-flavor emission). — both BOMs CLI-validated (CDX 1.7); 159 components (95 androidx-group JVM + 26 pkg:vcpkg native incl. cairo + libosmscout SHA + tooling); zero (name,version) duplicates; version 2026-09-12-2 matches the release versionName.
